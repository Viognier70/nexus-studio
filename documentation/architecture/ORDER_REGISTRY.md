# ORDER REGISTRY

**Version:** 1.0 (initial — created under `ORDER_034_DOCUMENTATION_ALIGNMENT.md` §0)
**Date:** 2026-07-29
**Governing rule:** Per `CLAUDE.md`, no ORDER number may be issued without an entry below. Renumber before issue if collision is found.

---

## 1. Scope

Every ORDER number that appears anywhere in the repository — filename, document header, status line, change-log entry, or narrative reference — is recorded here. Gaps in the sequence are shown, not hidden. An ORDER cited only in agent memory but never written into the repository is a documentation defect and is **not** eligible for a registry entry (per `ADR_002_SYNTHESIS_POLICY.md` §7.5).

**Non-ORDER governance instruments** with a defective provenance (a Superseding Directive that has no artefact in the repository, for example) are also recorded here per `ORDER_035_MEMORY_PROVENANCE_REPAIR.md` §2.3, so the gap does not sit invisible. Such rows carry their instrument type in the `#` column (e.g. `SD-001`) and are not part of the ORDER number sequence.

## 2. Legend — Carrier

- **Own document** — the ORDER text itself is committed as its own file. This is the only carrier that qualifies as a versioned instrument.
- **Report only** — the ORDER produced one or more report files but the order text itself was never committed. The ORDER exists as an artefact but not as an instrument.
- **Reference only** — the number is cited by other documents but no order document or report exists in the repository. The ORDER is inferred, not evidenced.
- **Not found in repo** — the number does not appear anywhere in `documentation/`. Included in the sequence for gap-visibility.
- **Void** — a draft was issued but withdrawn before ever taking effect (recorded here so the number is not silently reused).
- **Reserved** — a number has been claimed in this registry per `CLAUDE.md` rule 9 but no order file is committed yet. Held pending file authoring or Vision Owner approval.

## 3. Registry

| # | Title / subject | Status | Carrier | File(s) |
|---|---|---|---|---|
| 001 | *(unknown)* | Not found in repo | — | Referenced only in agent memory (`nexus_framework_governance.md`) as "Director's Addendum issued after Order 001". Not evidenced in repo. |
| 002 | Recognisable-village exploration prototype (first playable) | Superseded / historical | Reference only | Cited by `ADR_001_DIGITAL_TWIN_PHASE.md`, `SUPERSEDING_DIRECTIVE_002.md`. No order document committed. |
| 003 | Digital Twin Reconstruction — District 1 | Superseded / historical | Reference only | Cited by `DISTRICT_PRODUCTION_TRACKER.md`, `DISTRICT_1_REFERENCE_REQUEST.md`, `APPROXIMATION_REGISTER.md`. No order document. |
| 003A | *(unknown sub-variant)* | Unclear | Reference only | Single mention in `APPROXIMATION_REGISTER.md`. |
| 004 | PASS 1 baseline placement (District 1) | Superseded / historical | Reference only | Cited by `APPROXIMATION_REGISTER.md`, `RENDERER_ALIGNMENT_REPORT_ORDER_020.md`. **No own order document (per ADR 002 memory audit).** Provenance evidence: `ORDER_RECONSTRUCTION_004_005_019_020.md` §2 (ORDER 035 §2.2). |
| 005 | Production Mode — 60-minute work cycles | Active workflow rule | Reference only | Cited by `APPROXIMATION_REGISTER.md` and agent memory. **No own order document (per ADR 002 memory audit).** Provenance evidence: `ORDER_RECONSTRUCTION_004_005_019_020.md` §3 (ORDER 035 §2.2). |
| 006 | Torget long-house block modelling | Superseded / historical | Reference only | Cited by `APPROXIMATION_REGISTER.md`. No order document. |
| 007 | — | Not found in repo | — | Gap in sequence — number possibly never issued. |
| 008 | — | Not found in repo | — | Gap in sequence — number possibly never issued. |
| 009 | — | Not found in repo | — | Gap in sequence — number possibly never issued. |
| 010 | — | Not found in repo | — | Gap in sequence — number possibly never issued. |
| 011 | District 2 PHASE 1 modelling constraints | Historical | Reference only | Cited by `documentation/references/district-2/*/notes.md`. No order document. |
| 012 | — | Not found in repo | — | Gap in sequence — number possibly never issued. |
| 013 | — | Not found in repo | — | Gap in sequence — number possibly never issued. |
| 014 | *(unknown)* | Unclear | Reference only | Single mention in `APPROXIMATION_REGISTER.md`. |
| 015 | *(unknown)* | Unclear | Reference only | Single mention in `APPROXIMATION_REGISTER.md`. |
| 016 | — | Not found in repo | — | Gap in sequence — number possibly never issued. |
| 017a | *(unknown sub-variant)* | Unclear | Reference only | Single mention in `APPROXIMATION_REGISTER.md`. |
| 018 | Road-hierarchy rebuild | Historical | Reference only | Cited by `WORLD_ALIGNMENT_AUDIT_ORDER_019.md`, `APPROXIMATION_REGISTER.md`. No order document. |
| 019 | World Alignment | Executed — reported | Report only | `WORLD_ALIGNMENT_REPORT_ORDER_019.md`, `WORLD_ALIGNMENT_AUDIT_ORDER_019.md`, `CRITICAL_DEFECT_REGISTER_ORDER_019.md`. **No own order document (per ADR 002 memory audit).** Provenance evidence: `ORDER_RECONSTRUCTION_004_005_019_020.md` §4 (ORDER 035 §2.2). |
| 019R | Full-map authenticity re-audit (revision of 019) | Executed — reported | Report only | `FULL_MAP_AUTHENTICITY_AUDIT_ORDER_019R.md`. |
| 020 | District Coherence — landmark procedural detail unification | Executed — reported | Report only | `RENDERER_ALIGNMENT_REPORT_ORDER_020.md`. **No own order document (per ADR 002 memory audit).** Provenance evidence: `ORDER_RECONSTRUCTION_004_005_019_020.md` §5 (ORDER 035 §2.2) — records the title/content mismatch between the ORDER 019 proposal ("District Coherence") and the executed report ("Renderer Alignment"). |
| 020 *(draft, competing)* | Documentation Alignment (competing draft) | Void | — | Withdrawn by `ORDER_034_DOCUMENTATION_ALIGNMENT.md` header — collided with the existing ORDER 020 above. |
| 021 | Building completion | Executed — reported | Report only | `BUILDING_COMPLETION_REPORT_ORDER_021.md`, `BUILDING_COMPLETION_AUDIT_ORDER_021.md`. |
| 021A | Performance preparation / phase gate | Historical | Reference only | Cited by `PERFORMANCE_PREPARATION_REFERENCE.md`, `WORLD_AUTHENTICITY_REPORT_ORDER_022.md`. |
| 022 | World Authenticity | Executed — reported | Report only | `WORLD_AUTHENTICITY_REPORT_ORDER_022.md`. |
| 023 | Engineering Infrastructure | Executed — reported | Report only | `ENGINEERING_INFRASTRUCTURE_ORDER_023.md`. |
| 024 | Phase IV Kickoff | Executed — reported | Report only | `PHASE_IV_KICKOFF_REPORT_ORDER_024.md`. |
| 025 | Engineering (optimisation forbidden) | Executed — reported | Report only | `ORDER_025_ENGINEERING_REPORT.md`. |
| 026 | Pre-review defect sweep (D03) | Executed | Reference only | Cited by `DISTRICT_PRODUCTION_TRACKER.md`, `ORDER_027_FINAL_REPORT.md`, and district review files. No order or report document. |
| 027 | Place Framework & Adaptive World Model | Executed — reported | Report only | `ORDER_027_FINAL_REPORT.md`. |
| 028 | District Completeness (Phase 2 → Phase 7) | Executed — reported | Report only | `DISTRICT_COMPLETENESS.md` (Phase 7), `LANDMARK_CATALOGUE.md` (Phase 2), `BUILDING_CATALOGUE.md`, `ADAPTIVE_BUILDINGS.md`. |
| 029 | Review Package | Executed — reported | Report only | `REVIEW_PACKAGE_ORDER_029.md`. Updates `LANDMARK_CATALOGUE.md`. |
| 030 | Recognisability lift — Tier 1 systemic fixes | Proposed (title only) | Reference only | Proposed as next-order title in `RECOGNISABILITY_SURVEY.md`; also cited by `STREET_PROFILE_CATALOGUE.md`, `REVIEW_PACKAGE_ORDER_029.md`. No order document. |
| 030 *(draft, competing)* | Documentation Alignment (companion to SD-002) | Void | — | Withdrawn by `ORDER_034_DOCUMENTATION_ALIGNMENT.md` header — collided with the ORDER 030 proposed title above. Note: `SUPERSEDING_DIRECTIVE_002.md` still names this file as a companion — dangling reference, to be corrected via §8 companion integrity report. |
| 031 | Street profile catalogue / authenticity matrix (Phase 3) | Executed | Reference only | Cited by `STREET_PROFILE_CATALOGUE.md`, `AUTHENTICITY_MATRIX.md`, `VISUAL_IDENTITY_AUDIT.md`. No order document. |
| 032 | *(contested — two conflicting claims)* | Ambiguous — cited but contested; no document | Reference only | The ORDER 032 number is cited by two repository artefacts that describe it differently, and neither is evidenced by an ORDER document. **(a)** Commit `954c767` (2026-07-27) is titled *"revert(order-032): purge 469 synthetic buildings — corrective ORDER"* and treats ORDER 032 as the corrective purge that reverted an earlier bulk-densification run. **(b)** `scripts/densify-villagerings.mjs` header line 3 reads *"// ORDER 032 — village densification."* and treats ORDER 032 as the authoring order for the densification itself. Both claims cannot be simultaneously true. Recorded per ORDER 040 §5 cause analysis 2026-07-30. Variant of the ORDER 035 §2.2 provenance-gap pattern (ORDER 004 / 005 / 019 / 020) with the added twist of a self-contradiction between two memory-only claims about the same number. |
| 033 | Reference audit & memory audit | Executed | Reference only | Cited by `ADR_002_SYNTHESIS_POLICY.md` §1 (reference audit) and §7 (memory audit). Findings absorbed into ADR 002 rather than a standalone report. |
| 034 | Documentation Alignment | Awaiting Vision Owner approval | **Own document** | `ORDER_034_DOCUMENTATION_ALIGNMENT.md`. |
| 035 | Memory Provenance Repair | Awaiting Vision Owner approval | **Own document** | `ORDER_035_MEMORY_PROVENANCE_REPAIR.md`. Parent: `ADR_002_SYNTHESIS_POLICY.md` §7, §9.1. |
| 036 | Reference Integrity Infrastructure | Executed 2026-07-29 | **Own document** | `ORDER_036_REFERENCE_INTEGRITY_INFRASTRUCTURE.md`. Parent: `ADR_002_SYNTHESIS_POLICY.md` §4.2, §5, §9 steps 1–3. Executed in four section commits: `9e826c9` (§0, registry entries), `71ae625` (§2, 13 relocations + manifest paths), `73420e3` (§3, `scripts/validate-references.mjs` + `VALIDATOR_REFERENCE.md`), `e29d354` (§4, `reviewState` backfill + schema in `documentation/references/README.md`). Steps 4–8 of ADR 002 §9 remain out of scope per §5. |
| 037 | Scenario Type Segregation | Pending | **Own document** | `ORDER_037_SCENARIO_TYPE_SEGREGATION.md`. Parent: `documentation/game-design/LEARNING_AND_SCENARIO_ARCHITECTURE.md` §4.1, §4.6, §5. |
| 038 | Rv 244 Curvature Layer | Executed 2026-07-30 — verified no curvature; no layer implemented | **Own document** | `ORDER_038_RV244_CURVATURE_LAYER.md`. Parent: `ADR_001_DIGITAL_TWIN_PHASE.md` §2.2, §2.3; `SUPERSEDING_DIRECTIVE_002.md` §1 (decision B). Executed by aerial verification (Esri z=18 + Bing z=19, both retrieved 2026-07-30): actual Rv 244 runs coincident with the OSM 8-vertex straight-line v5 → v6 to within ~2 m over 174 m; no traced points warranted. Overlay evidence at `documentation/references/rv244-aerial-verification-2026-07-30.png`. Register entry `documentation/world/APPROXIMATION_REGISTER.md` (2026-07-30 ORDER 038 entry) records the finding and opens a separate `D2-alt` defect for the side-of-road / adjacency interpretation the original ORDER 019 audit hedged and never independently investigated. |
| 039 | Building Overlap Diagnostic | Executed 2026-07-30 — diagnostic report; no data changed; root-cause claim corrected 2026-07-30 | **Own document** | `ORDER_039_BUILDING_OVERLAP_DIAGNOSTIC.md`. Parent: `PHASE_IV_PRODUCTION_PLAN.md` (visual acceptance criterion); `ADR_001_DIGITAL_TWIN_PHASE.md`. Executed 2026-07-30: full report at `documentation/architecture/BUILDING_OVERLAP_DIAGNOSTIC_REPORT_ORDER_039.md`; register summary at `documentation/world/APPROXIMATION_REGISTER.md` (2026-07-30 ORDER 039 entry). Findings: 39 tier-3 substantial building overlaps including 8 intrusions into `gry-kyrka`; 87 handcrafted `vw-*` buildings in `world.json`, none marked SYNTHESISED per ADR 002 §2.3, most carrying invented addresses without source. **Root-cause claim corrected 2026-07-30 under ORDER 040 §5:** the original report attributed the overlaps to `scripts/densify-villagerings.mjs`. Git-history evidence (densifier id prefix `vw-dnf-*` has zero matches in `world.json`; every one of the 87 comes from hand-authored `fix(<street>): …` commits by the Vision Owner) falsifies that attribution. Actual root cause: careful hand-authored placements with no overlap validator to catch when an evidence-grounded individual placement happens to fall inside an existing footprint. The §7.1 report proposal to "extend `densify-villagerings.mjs` with a proper landmark-zone check" falls with the same correction — the script never wrote the current buildings, so extending it fixes nothing that exists. **ORDER 040 §7's validator replaces it** as the correct mechanism. Corrections applied to `BUILDING_OVERLAP_DIAGNOSTIC_REPORT_ORDER_039.md` (Correction 2026-07-30 section) and the register (2026-07-30 correction entry). See also row 032 update above — the ORDER 032 identity itself is contested, not merely absent. |
| 040 | Synthetic Building Marking and Densifier Repair | Pending | **Own document** | `ORDER_040_SYNTHETIC_BUILDING_MARKING.md`. Parent: `ADR_002_SYNTHESIS_POLICY.md` §2.1–§2.3; `EXECUTIVE_DESIGN_DIRECTIVE_001.md` §5. Input: `BUILDING_OVERLAP_DIAGNOSTIC_REPORT_ORDER_039.md`. **§1 principle ("appearance is approximation, position is fact") is not binding until a Superseding Directive is issued per §8**; §4 (named-entity `vision-owner` marking) is SD-gated. §2 marking field, §3 anonymous-fill cleanup, §5 densifier repair, §6 overlap correction proposal, §7 validator can proceed on existing EDD §5 rigour. |
| 041 | Unpause Vertical Slice 002: The First Playable Loop | Pending | **Own document** | `ORDER_041_UNPAUSE_VS002_FIRST_PLAYABLE_LOOP.md`. Parent: `EXECUTIVE_DESIGN_DIRECTIVE_001.md` §13 (VS-002 pause); `CAMERA_AND_GAMEPLAY_BIBLE.md`; `LEARNING_AND_SCENARIO_ARCHITECTURE.md`. Report-only steps 1–3 of §5 (VS-002 state audit, `gastronoma-sim-main/` **design-decisions-to-keep** audit per Vision Owner clarification 2026-07-30 that the prototype is a previous stack not a foundation for the new build, loop-shape + three-candidate-building proposal). Step 4 (build) needs its own order. World work paused per §1.2; no world geometry / OSM ingest / ORDER 040 §6 correction touched. `git diff` under `frontend/src/` shows 0 lines. |
| 042 | Build the First Playable Loop | Pending | **Own document** | `ORDER_042_BUILD_FIRST_PLAYABLE_LOOP.md`. Parent: `ORDER_041_UNPAUSE_VS002_FIRST_PLAYABLE_LOOP.md` §5 step 4. Vision Owner picked candidate C (`w869907963`, 252 m² historic-centre sit-down) per §1; rescaled to walk-in-of-five on `w869907975` (Candidate A, 146 m² café-scale) after ORDER 041 §6 candidate-filter miss surfaced (§1 superseded-pick note 2026-07-30 and 2026-08-08). Five commits per §3 (business exists / time runs / scenario arrives / consequence shows / investment). Constraints in §4 (no avatar per SD-002 §2.1; no mode picker per Camera Bible §4; no HUD-dominant numbers per EDD §11; no result popup per Camera Bible §8.1; no XP or progression per Framework §13; text via `strings.sv.ts` per CLAUDE.md rule 7). Out of scope: Måltidens Hus, knowledge acquisition, portfolio, world geometry, ORDER 040 §6 corrections. |
| 043 | The Service Round — Team, Wager and Consequence | Awaiting Vision Owner approval (v3 2026-08-08, supersedes v1 + v2 entirely) | **Own document** | `ORDER_043_CAPITAL_WAGER_AND_CONSEQUENCE_CHAIN.md`. Parent: `ORDER_042_BUILD_FIRST_PLAYABLE_LOOP.md` §3.5. **v3 history:** v1 introduced the wager on themed capitals; v2 refactored into a two-layer capital model (outcomes vs enablers, competence derived from behaviour); v3 realised — through playtesting v2's B.1 phenomena — that the mechanics had no business to live in: the sim ran forever at a constant gentle rate, nothing could build so nothing could break. v3 rebuilds the business around the day. **§2** the day as unit of play — morning / lunch / dinner (night deferred), player-chosen service length 3–30 min, random scenario count weighted by length, light shifts with the period. **§3** the team — four roles (Runner, Waiter, Apprentice cook, Chef) with structural cost + competence, locked for N days; agency staff callable mid-service (money-vs-people trade-off, cost shows tonight AND three weeks later when someone resigns). Player never commands individuals per EDD §7. **§4** the reputation loop as the central dynamic: reputation raises demand → strains staff → degrades service → lowers reputation, emergent from mechanics not decreed; the delay is the point. **§5.1** the queue is *deliberately ambiguous* — understaffed OR popular; walk-aways are the disambiguator, and reading two signals (queue + room) is the competence the game teaches. **§5.2** lifts the two hardcoded caps recorded in `APPROXIMATION_REGISTER` as the 7th occurrence. **§5.3** ecological delivery rhythm (already built, carried forward). **§6** services can collapse from traceable staffing failures (nut allergy, bad salmon, contamination — each a consequence of a morning hiring decision). Must be rare. **§7 wager** and **§8 capitals/enablers/chain** unchanged from v2. **Carried forward from v2:** two-layer capital state model (Phase A, commit `0be34f3`), theme selection + damping formula (`d93241d`, `weight = (1 − v)² + 0.05`), ecological delivery rhythm (from B.1). **Build order §10:** (1) the round, (2) the team, (3) the room reads, (4) the reputation loop, (5) wager + chain on top, (6) collapse last. Report gate after each step; steps 1 and 2 change what the game *is* and require Vision Owner sign-off before proceeding. **Acceptance §11:** Vision Owner plays a full day and reports (1) team decision mattered and its cost was felt during service, (2) wager was a real decision, (3) loss visibly shaped something later, (4) no dominant response; then a 2nd and 3rd day to test the reputation loop and the between-day chain. |
| 044 | The Village Made Legible | Awaiting Vision Owner approval; executed 2026-08-08 (PR #9) | **Own document** | `ORDER_044_THE_VILLAGE_MADE_LEGIBLE.md`. Parent: `ORDER_040_SYNTHETIC_BUILDING_MARKING.md` §6; `ORDER_043_CAPITAL_WAGER_AND_CONSEQUENCE_CHAIN.md` §5A.2. **§2** applies the ORDER 040 §6 correction proposal to the 39 tier-3 building overlaps: 8 vw-kyr-* buildings removed from the church footprint (commit `04a108c`); the remaining 26 pairs resolved via 8 unambiguous removes + 7 nudges + 6 nudge-fallback removes (where nudge would create new tier-3 overlap with a third building) + 1 Tempo intruder removed (`efe7800`). Result: `V21_ACCEPTED_OVERLAPS = new Set([])` — the validator is now a real fence, not an absorb. Five awaits-ground-truth cases reported separately (Vision Owner instruction 2026-08-08); default action was remove per proposal §3, ground-truth needed for possible re-add via a follow-up order. **§3** makes the room legible without material work: (§3.1) guests walk between states at 1.2 m/s with a laterally-offset walk-away path (commit `3fe772e`); (§3.2) one puck per `state.team.members` entry with role-derived stations + load-driven drift, never selectable per EDD §7 (`c1e5e7d`); (§3.3) seat attention as a physical positional lean (not opacity pulse per Vision Owner: "gränsfall mot symbol"), one mechanism two polarities — toward nearby staff = "the diner turned to the server", toward the bar past 8 s of unattended dwell = "looking for someone" (`2393da3`). **Corollary fix:** `EventStreamPanel` clip guard (`b8b049b`). **Acceptance §6:** church stands alone (verified programmatically); V21 exception list empty; seated vs turned-away distinguishable at a glance; team of four vs team of two differ visibly in the room, not only in text. Explicitly out of scope: material / finish work, interior furnishing, collapse mechanic. |
| 045 | The Opening Image | **Retrospective** — executed 2026-08-08 without a prior document; document authored after per Vision Owner instruction | **Own document** | `ORDER_045_THE_OPENING_IMAGE.md`. Parent: `ORDER_043_CAPITAL_WAGER_AND_CONSEQUENCE_CHAIN.md` (Addendum A); `ORDER_044_THE_VILLAGE_MADE_LEGIBLE.md` §3. **Provenance note (order §0):** built from three Vision Owner chat turns during the ORDER 044 execution — (a) opening-image mechanic + weather + waiting-outside + staff prep tempo + weather-in-wager, (b) outer-world factors expansion (konjunktur / vägarbeten / säsong / evenemang) with rarity-report gate, (c) rarity Option A approval. Landed in commits `b25231e` + `39ed5b3` + `34a290a` + `7ac1bf7` on branch `order-044`. Document written after under Vision Owner rule "registret ska stämma med koden." **§2** the opening image — 10-second briefing panel between OPEN_SERVICE and prep showing weather + outdoor viability + world factors + waiting-outside count + countdown. **§3** weather generation (autumn Grythyttan bands: 6–21 °C, 0.5–10 m/s, precip 70/20/10 %, cloud 30/40/30 %) + `weatherArrivalMultiplier` composing into `arrivalProbability` (~0.55× at cold-windy-drizzle → ~1.28× at warm-still-clear). **§4** waiting-at-opening derived from `reputation × weather × world-factor waiting mult`, capped at 6; spawns exactly once at prep-end via `doorsOpenedThisService`. **§5** outer-world factors — Vision-Owner-approved Option A independent Bernoulli (konjunktur 8 %, vägarbeten 10 %, säsong 12 %, evenemang 17 %) yielding measured 63 % / 31 % / 5 % / <1 % distribution for 0 / 1 / 2 / 3+ factors, with per-realisation multipliers on arrival, waiting, revenue, delivery cooldown; state stores kind only (portable per §11.1), labels + multipliers in `worldFactors.ts`. **§6** weather + factor names shown in the WagerPanel tail so the stake reads against the evening. **§7** staff prep tempo — during the prep window pucks work at ×1.8 pace with ×3 drift amplitude and ×2.75 drift frequency, falling back to service-time idle-plus-load pattern the moment prep closes. Explicitly not built: mode-picker weather, dynamic mid-service weather, calendar/seasonality, 3D visual weather, world-factor scenarios, collapse trigger from weather. **Acceptance §9:** Vision Owner plays a service and can (1) read the evening from the panel alone, (2) feel the weather in arrivals, (3) name 1–2 memorable factor evenings over 20 services without feeling constantly reported to, (4) stake differently on warm-thin vs cold-thin evenings, (5) see staff visibly working during mise en place. Tests: 39 new invariants in `weather.test.ts` (22) + `worldFactors.test.ts` (17); total suite 291 passing. |
| 049 | The Knowledge Engine | Awaiting Vision Owner approval (v1.0 2026-08-09); in execution — step 1 (generation script + prompt + 20 sample questions) awaiting Vision Owner review before generating at volume; steps 2-8 all gated | **Own document** | `ORDER_049_THE_KNOWLEDGE_ENGINE.md`. Parent: `ORDER_048_THREE_VOICES_AND_THE_QUESTION.md` §5; `DESIGN_BACKLOG.md` B-010 (knowledge-seeking, the constitution's missing seventh movement). Realises B-010 by using gusto.science's completed TRIAD analyses (ε EPISTEME / τ TECHNE / φ PHRONESIS per article) as source material for concrete professional questions attached to scenarios. **§2** player-facing loop — invest during service → 10-s countdown → themed scenario from area invested in → answer well = credits in that subject → answer badly = credits lost → bankruptcy sends player back to school in Sevilla. **§2.1** difficulty must rise until holding a restaurant is impossible without genuine higher education; difficulty model gated (needs `9/10 · Chef` interpretation from Vision Owner). **§3** three-stage architecture — generation script in repo (writes JSON), Vision Owner review (approve/reject with reasons retained), packaging into local bank; **game never queries Supabase at runtime** (real-time service can't wait on network). **§3.3 supersedes** `VERTICAL_SLICE_002.md`'s "no backend, no Supabase" exclusion for the generation script only; running game remains backend-free. **§3.4** on answering, name the knowledge form + source (narrow exception to §9 no-meters rule per Vision Owner). **§4** readiness — Gastronoma-style three-line reveal on scenario resolution: what was prepared, what was required, what followed. **§5 (Vision Owner answer 2026-08-09):** bankruptcy loses competence points weighted heaviest in areas where knowledge failed; next premises purchase requires higher capital (bank saw the failure); **deficits cannot be compensated between areas** — high food/drink competence does not offset an economics-driven bankruptcy; bank wants to see the specific deficit addressed, forcing focused study on the player's weakest field. **§5.1 (Vision Owner amendment 2026-08-09):** every venture application (first business + after each bankruptcy) begins with a bank-meeting scene — one animated person on the other side of a desk asking tricky questions drawn from the knowledge bank; good answers open larger financing, poor answers get refused for the tier applied for and referred to the floor offer (foodtruck in Grythyttan, always available). After a bankruptcy the bank knows what failed → weighted questions in the failed area + higher threshold. Excellence in all subjects the only path to full financing. Mapping from answers to loan amount gated (report before building). **§5.2 (Vision Owner amendment 2026-08-09):** player panel must show business value in SEK (realisable via sale — player can sell at any time and reapply per §5.1), plus operating readings — reputation, food quality, drink quality, service quality, revenue per seat, revenue split lunch vs dinner. Sell action liquidates at valuation. Valuation formula gated (report before building). **§5.3 (Vision Owner amendment 2026-08-09):** scale-down actions available in morning when a service is bleeding — shorten menu (ingredient tier down one step, cost per cover down, food-quality + rep down), thin wine list (drink-quality + rep down, drink service saving), close a service (skip lunch or dinner for N days, biggest rep cost but full labour + ingredients saved). Reversible; the point is passivity costs and active retreat is available. **§6** every moment where something is at stake must be animated (countdown, question timer, credits landing, meter movement, readiness comparison, bankruptcy). **§7** build order — 1 generation script, 2 review flow, 3 bank replacing ORDER 048's three hand-authored questions, 4 invest→countdown→themed scenario loop, 5 readiness display, 6 difficulty progression (after model report), 7 bankruptcy + return, 8 animation. Report after steps 1, 2, 4. Acceptance §8 — Vision Owner plays and reports (1) investing produced questions from that area, (2) questions were real + traceable, (3) readiness explained an outcome, (4) difficulty rose enough that holding the restaurant demanded knowing the field, (5) bankruptcy felt like a consequence + return to school felt like the way back, (6) moments-at-stake felt like moments. Out of scope §9 — no multiplayer, no competitor businesses, no material work, no persistence beyond session (§5 carry-across will force that question; report rather than solve here). |
| 048 | Three Voices and the Question | Awaiting Vision Owner approval (v1.0 2026-08-09); in execution — §7 (named keys + prominent countdown) and §8 (arrival mark fix) unblocked, §2/§3/§4/§5/§6 gated pending report | **Own document** | `ORDER_048_THREE_VOICES_AND_THE_QUESTION.md`. Parent: `ORDER_043` v3 + Addenda A/B, `ORDER_047_THE_INSTRUMENTS_OF_SERVICE.md`. Vision Owner played ORDER 047 2026-08-09; verdict: three different jobs collapsed into one voice. §2 splits the voice — plain running report during service (short clauses, no interpretation), immediate attributable outcome on scenario answer, observer voice relocated to evening account only. §3 meters must show trade-offs — every meaningful event writes real-time to at least one sustainability; two-way where a genuine trade exists; named-cause presentation without severity styling (position/timing/adjacency only). §4 questions get a sender — named team member (sous-chef/bartender/runner/servitör) speaks in their own words; team becomes audible in what is asked (thin team → basic questions). §5 concrete professional questions attached to scenarios — reverses Addendum A's rejection of quiz-questions (that was aimed at quiz-as-side-panel; this is knowledge tested at the moment it is needed by the person who needs it about a guest who is waiting); first appearance of DESIGN_BACKLOG B-010 in playable form. §6 deeper scenarios — second decision that follows from first, or two sustainabilities genuinely conflict; not more scenarios. §7 player must know what to do — name camera preset keys 1-4 in Swedish, make the doors-open countdown unmissable. §8 fix the arrival mark (visual + audio not perceptible in play; Addendum B §5A.7 spec unrealised in effect). Acceptance §9 — Vision Owner plays one dinner and reports (1) running report reads as room status, (2) scenario answer produces immediate attributable outcome, (3) watched one sustainability fall while another rose from same event, (4) each question came from someone and thin team was audible, (5) at least one concrete professional question asked in the moment, (6) knew what to press and when, (7) noticed each new line arriving. Out of scope §10 — no materials, no persistence, no Måltidens Hus institution. |
| 047 | The Instruments of Service | Awaiting Vision Owner approval (v1.0 2026-08-09); in execution — Vision Owner has picked §5 option A (draw scenario from stream, weighted not deterministic) 2026-08-09; §2 (instrument set + layout) and §7 (speed impact) still gated | **Own document** | `ORDER_047_THE_INSTRUMENTS_OF_SERVICE.md`. Parent: `ORDER_043_CAPITAL_WAGER_AND_CONSEQUENCE_CHAIN.md` v3 + addenda, `ORDER_046_THE_CYCLE_CLOSES.md`. Vision Owner played a full cycle 2026-08-09 and reported five findings — too much text, service too long, too few and always the same scenarios, hard to see what an investment did, hard to see the effect of a scenario answer — pointing at one thing: the player can read the evening's texture but not its state. **§0** remove the sabotage marker `NameEntryOverlay` was throwing, and add a grep guard in the smoke suite. **§2** business instruments (service level, satisfaction, revenue, tips, morale, quality) shown live at the edge — distinct from the ORDER 043 §9 competence-meter prohibition (that stands for what the player has become; these show what the business is doing tonight). Set + layout proposal gated. **§3** stream reduction — fewer visible entries, older collapse rather than scroll, allow shorter events. **§4** more and varied scenarios per service, no repeat within service, no repeat opener; report why the same one kept drawing. **§5** scenario drawn from what the stream has reported — Vision Owner picked option A over move-wager-between-subject-and-situation ("pedagogiskt lär det ut att läsa en verksamhet, vilket är yrkeskunnandet") with mandatory weight-not-determine constraint and distribution-reporting requirement. **§6** investments must show their work — name consequence at point of choice, again when it lands, referenced in evening account. **§7** faster clock, not shorter service (readability impact report gated). **§8** dev-only trigger to force next collapse. Acceptance §9 — Vision Owner plays one dinner and can (1) read state without reading every line, (2) glance rather than read, (3) see several different scenarios, (4) attribute a scenario-answer to a visible movement, (5) recognise a morning investment in the evening, (6) see a collapse read as consequence. Out of scope §10 — no materials, no geometry, no persistence, no knowledge-seeking (B-010). |
| 053 | Skala, Kamera och Assetpolicy | Executed 2026-08-11; open questions closed by ORDER 054 Del A on 2026-08-11 | **Own document** (lifted from verbal order 2026-08-11 under ORDER 054 Del A) | `documentation/orders/ORDER_053_SKALA_KAMERA_ASSETPOLICY.md`. Parent: `CLAUDE.md` (assetpolicy revocation + unit contract). Reason: lay metric ground for the visual lift and resolve the general external-asset prohibition. **Del A** rewrites the asset policy — humanoid `.glb` + rigs + Mixamo/CC0 clips allowed under `frontend/public/assets/characters/` with per-asset licence file; buildings/inredning/terräng/vegetation/textures/HDRI stay forbidden externally; runtime downloads absolutely forbidden regardless of type (drei `<Environment preset="...">` named as CDN-fetching hazard — use procedural `<Sky>`). **Del B** unit contract: 1 world unit = 1 metre, no exceptions. Inventory produced (`skala-inventering.md`) with reference targets (våningshöjd 2,70 / dörr 0,90×2,05 / bordshöjd 0,74 / stolssits 0,45 / gäst 1,70 / bardisk 1,10 / tallrik 0,27). Fixes: Applicant 2,20 → 1,70 m; RegistrationTable 0,94 → 0,74 m surface; BusStop bench 0,55 → 0,45 m; InteriorGuests puck 1,60 → 1,70 m; PlayerBusiness bar 1,05 → 1,10 m + bar stool 0,90 → 0,75 m; AnimationPrototype crown 1,688 → 1,700 m. **Del C** first-person camera: eye height 1,65 m, fov 65, near 0,1, far 2000 (updated `Scene.tsx` + `PlayerController.tsx`); no scale-compensating constants found in scene/PlayerController. **Del D** G-key toggle (dev-only via `import.meta.env.DEV`): 1-m grid ±50/80 m marked every 10 m + placeable 1,80 m reference figure (`frontend/src/scene/ScaleReference.tsx`), wired into both `Scene.tsx` and `StrategicScene.tsx`. Not touched per order: kunskapsmotor / ledger / kassa / kvalitetsmätare / dagsslinga; open orders 049–052 unchanged. Not authored as own document per registry rule — verbal order text lives in this session's chat; upgrade to `Own document` when transcript is committed. |
| 052 | Mise en Place and the Cause | Awaiting Vision Owner approval (v1.0 2026-08-10); §9 step 1 (stream lines carry their cause — rewrite the sentence banks) is the first gate | **Own document** | `ORDER_052_MISE_EN_PLACE_AND_THE_CAUSE.md`. Parent: `ORDER_048_THREE_VOICES_AND_THE_QUESTION.md` §2 (the plain service report); `ORDER_050_ACTIVITIES_AND_THE_LEDGER.md` Addendum A §6 (attention). Reason: Vision Owner played 2026-08-10 and found the stream saying *"Ett bord fick bestick utan smörknivar."* — a symptom without its cause. Same error as the abstract sustainability stake in a new form. Also named the physical chain the game is missing: morning preparation → rhythm or its absence → what the guest sees. **§2** mise en place: prep stops being a countdown and becomes work-done-or-not (concrete inventory: ice / napkins / cutlery / stations / garnish — not a single score), driven by team size + competence + prep length + morning changes + business load. Model gated. **§3** rhythm + pulse: green→red reading of whether the room works in time or chases itself; must live *in* the room per EDD §11 + Addendum A §6.5, not beside as another instrument. Form gated (diagram / pulse / staff puck colour). **§4** every stream line carries its cause — rewrite the banks so a symptom names its condition (thin team, low competence, poor morale, short prep, morning change, ingredient tier, active scale-down, supplier short-delivery). Ambient texture stays ambient; consequences must say what of. Plain-register (§48.2) preserved. **§5** stream panel faults: sized for eight, holds one → shrink to contents; entries arrive at bottom and grow upward → pick one direction. **§6** after-countdown line: doors open, nothing tells the player what happens now — one line in the plain register at that moment. **§7** honest gap: `ORDER 049` §5.1 bank meeting blocked on episteme volume; `ORDER 050` §7 step 1's 41 activities settled and not built (panel still 046's training/pricing/ingredient dial); `ORDER 051` registered not started. This order fixes what is in play. **§8** three defects: day-one valuation is −229 kSEK (grandfathered T2 loan too big for the premises OR valuation missing something — report before fixing); morning panels overlap (§6.3 separated them logically not spatially); all four quality readings say *Godtagbar* + revenue-per-seat dashes (identical readings are decoration, unfilled ones worse). **§9 build order:** (1) cause-aware stream lines — biggest effect, no new systems; (2) stream panel sizing + direction; (3) after-countdown line; (4) three defects (valuation report first); (5) mise en place (model report first); (6) rhythm + pulse (form report first). Report after 1, before 5 and 6. |
| 051 | Suppliers, Menu and Stock | Awaiting Vision Owner approval (v1.0 2026-08-10); §7 step 1 (supplier set + four-axis numbers) is the first report gate | **Own document** | `ORDER_051_SUPPLIERS_MENU_AND_STOCK.md`. Parent: `ORDER_050_ACTIVITIES_AND_THE_LEDGER.md` §3 (the ledger — every stock purchase posts a line); `ORDER_049_THE_KNOWLEDGE_ENGINE.md` §2.1 + `ORDER_050_ADDENDUM_A` §11 (pricing bounded by the ceiling — the house cannot ask more than it has earned the right to ask). Reason: Nexus has no menu — guests eat abstractly, `ingredientTier` is a three-position dial, and the ecological reading points at nothing the player did. §1 core lesson: *"price a popular dish too low and it runs out before service ends — unless you planned for it"* — pricing lesson no formula teaches, requires estimating demand + weighing margin against volume. §2 named suppliers (wholesaler / local growers / organic / meat + game / lake fish / brewery + distributor / wine / equipment) each with four axes: price, quality, reliability, ecological reading impact. §3 player composes menu + wine list + prices after loan/premises/renovation/kitchen upgrade; **package menus** (3/5/7-course fixed with drinks pairing) as distinct decision — report interaction with à la carte before build. §4 **stock is bought before service and drawn down as guests order**; plates-remaining reading visible during service; **running out is a real event** posting to the stream + costing satisfaction/rep — not silent substitution. Leftover stock persists but ages (ORDER 043 waste as base). §5 connections: every purchase posts a ledger line (ORDER 050 §3), **`ingredientTier` is superseded** (retire it and reroute readers), ecological reading draws from actual sourcing not policy, ORDER 050 activities remain (supplier relationship = activity; weekly buying = not); kitchen competence limits what menu can carry (report how). §6 scope discipline: **large data volume** (dishes/ingredients/suppliers/wines) — start small enough to balance, large enough to force real choice, **report size before authoring**; menu set in morning and stands, not adjusted mid-service (Addendum A §6.3). §7 build order: (1) suppliers + four axes + numbers (report first), (2) ingredients + stock buying/holding/drawing, (3) menu composition + pricing + plates-remaining reading, (4) running-out event + cost + stream appearance, (5) wine list same shape, (6) package menus after à la carte holds, (7) retire ingredientTier + reroute readers. Report gates after 1 and 3. Out of scope §9: no multiplayer, no persistence, no new geometry (plates-remaining is a panel, not plates rendered in the room). |
| 050 | Activities and the Ledger | Awaiting Vision Owner approval (v1.0 2026-08-10 + Addendum A 2026-08-10); §7 step 1 (activity list with three-column effects) is the first gate | **Own document** | `ORDER_050_ACTIVITIES_AND_THE_LEDGER.md`. Parent: `ORDER_043_CAPITAL_WAGER_AND_CONSEQUENCE_CHAIN.md` §4 (supersedes theme-wager for activity-wager); `ORDER_049_THE_KNOWLEDGE_ENGINE.md` §5.2 (extends the player panel with the ledger surface). Reason: Vision Owner played 2026-08-09 and reported *"stake on social sustainability"* is an abstraction — a real restaurateur picks *anställ sommelier*, not *social capital*. Order replaces the theme-wager abstraction with concrete activities carrying three-column (economic / social / ecological) effects visible on the card, and makes `state.capitals.values.economic` literal kronor with a bookkeeping-style ledger (§3, Fortnox-analogue). §4 constraint: no activity may state which sustainability it serves — the three numbers are the teaching. §5 asks whether the wager survives as an activity-anchored bet or retires entirely (report gated). §7 build order: (1) activity model + three-column effects, (2) allocation surface, (3) ledger, (4) wire every money-touching mechanic to post a line, (5) retire/re-anchor wager after report, (6) UX pass. Report gates after 1 and 3. Out of scope §9: no multiplayer, no persistence, no new geometry; knowledge engine (ORDER 049) continues in parallel. **Addendum A (v1.0 2026-08-10):** `ORDER_050_ADDENDUM_A_PAUSE_AND_ATTENTION.md`. Amends §6 (attention: automatic pause on scenarios so competence decides — not reading speed; manual pause via Space; 1×/2×/4× promoted from dev tool to part of play; one-thing-per-surface across morning / service / evening; cash always visible, everything else one click away; consequence appears at the cause not only later in the ledger). Adds §11 (pricing and willingness to pay: **knowledge is the right to charge** — closes the ORDER 049 §2.1 ceiling chain by making the ceiling the permission gate on price, not just a display; raising the price is a real bet on whether the house has earned it; §11.2 model — what determines guest clientele, and how much is fixed by premises vs shaped by the player — gated pending report before build). Addendum build order: (1) auto-pause on scenarios first (smallest change, largest effect on feel), (2) manual pause, (3) speed as play, (4) surface inventory + one-thing-per-surface (report first), (5) consequence-at-cause, (6) pricing model (report first). | 
| 060 | Wall winding fix (removes ORDER 059 DoubleSide workaround) | **Reserved** — filed 2026-08-12 following ORDER 059 emergency wall-culling fix that used DoubleSide as bandage | **Own document** | `documentation/orders/ORDER_060_WALL_WINDING_FIX.md`. Scope: rewrite `quadFromEdgeToTop` in `frontend/src/lib/facade/buildFacade.ts` so triangle winding produces outward-facing face normals for a CCW polygon (this is what `ensureCCW` guarantees at entry). Restore the removed normal-direction assertion in `buildFacade.test.ts` — the earlier one was disabled because the winding math was wrong. Remove `THREE.DoubleSide` from `wallMat` in `frontend/src/strategic/scene/ProceduralFacades.tsx` and reinstate FrontSide-only rendering. Why it matters: DoubleSide masks the visible bug but ships an INWARD wall normal to the fragment shader, so diffuse lighting is computed against a normal pointing at the wrong hemisphere — plausibly why facades read as flat even when the sun is at a good angle. Fixing the winding both removes the fill-rate doubling and restores correct diffuse response. |
| 061 | Night lights + roof normals (measure before build) | Executed 2026-08-12 (Thread A: LOD 1 emissive band; Thread B: roof face-normal fix mirror of ORDER 060 §1) | Verbal order (chat 2026-08-12) — measurement-first: *"sluta bygga och börja mäta"* | Two threads, both hypothesis-first per Vision Owner instruction *"Rapportera mätvärdena innan du bygger."* **Thread A (night lights invisible since ORDER 057 Del B):** Measurement confirmed `ProceduralFacades.tsx` LOD 1 group emits zero window meshes — `registerLitGlass` wires only to LOD 0's `windowGlass` mesh, so at strategic zoom (all houses beyond `LOD_NEAR_M = 70` m) the shared lit-glass material's `emissiveIntensity` ramps correctly but is bound to no visible meshes. Fix: added `lod1Emissive` bucket to `FacadeGeometry`, emit one horizontal emissive band per wall edge at LOD 1 (approximate first-floor window Y range, 0.4 m margin from corners, hair-offset outward from wall face), wire an `lod1EmissiveMeshesRef` in `ProceduralFacades` for `windowsLit` buildings, toggle mesh visibility on the same day/night gate (`nightFactor > 0.05`) as the existing glass swap. **Thread B (roofs render black despite ORDER 057 colour bump):** Written test `roof face normals point OUTWARD (upward + ridge-to-eave direction)` fails on `rect-CCW` first sloped triangle with `face normal Y = -0.866` (should be `+0.866 = +cos(30°)` for 30° pitch). Root cause: `buildRoof` cross-product `d1 × d2` at `buildFacade.ts:895-902` gives inward-pointing normal (0, −42, +14H) for CCW south face — sun overhead → `dot(normal, -lightDir) < 0` → diffuse clamps to zero → roof reads black regardless of `taktackning` tone. Fix: swap cross operand order to `d2 × d1` AND swap triangle indices `[0, 1, 2, 0, 2, 3]` → `[0, 2, 1, 0, 3, 2]` so winding-derived cross agrees with written normal. Verified by reinstated test now passing on rect-CCW / rect-CW / pentagon-CCW / L-CCW. Both threads: typecheck + 405 tests + build all green. |
| 046 | The Cycle Closes | In execution 2026-08-08 by Claude Code (branch `order-046`, cut from `order-044` pending PR #9 merge) | **Own document** | `ORDER_046_THE_CYCLE_CLOSES.md`. Parent: `ORDER_043_CAPITAL_WAGER_AND_CONSEQUENCE_CHAIN.md` §6 + §10 step 6, `documentation/DESIGN_BACKLOG.md` B-003, `ORDER_044_THE_VILLAGE_MADE_LEGIBLE.md` §3. Four delmoment in one order: (§1) service collapse per approved formula `COLLAPSE_FLOOR + (1 − weakest) × strainMult × COLLAPSE_STRAIN_GAIN` — no hard-competence threshold per Vision Owner correction 2026-08-08 "det handlar om lärlingen som tvingas stå i luckan, inte om lärlingen i sig"; weakest = min over axes of MAX team competence in that axis (single-station model, same shape as prep-floor polarity); fire effects = force period → evening, rep −0.15, hand-authored stream line keyed by failing axis, `ConsequenceEvent` appended, agency cleared. (§2) morning investment panel — thin surface over existing `SET_POLICY` action for `trainingLevel` / `pricing` / `ingredientTier` alongside TeamPanel; not a scoreboard, only turns dev-cycled numbers into an intentional morning decision. (§3) evening account per B-003 — 3–5 sentence paragraph in observer's voice at evening-period-start, six branches (collapsed / good / thin / high-wager-win / high-wager-loss / mediocre) with the mediocre branch as the Vision Owner's explicit request "kvällen bara var medioker — inte varje kväll ska ha en poäng"; `EVENING_TO_MORNING_PAUSE_SEC` bumped 15 → 30 to hold it. (§4) animation polish — no new geometry, no materials: guest sit / stand Y-dip on state transition, staff Y-bob overlay while `taskType` non-null, entrance quad scale-fade at door-open moment. Explicitly out of scope: nightclub (B-006), persistence (B-005), eight-role expansion (B-004), any new geometry / materials, rebalance of reputation loop or wager. Acceptance §6: Vision Owner plays 3–5 days and reports (1) collapse felt rare and traceable, (2) investment panel changed how morning read, (3) evening account arrived in a voice they recognised, (4) the room reads as animated not just shifted. |
| 076 | M6 cause-aware texture (metadata mechanic) | Executed 2026-08-12 (report gate + implementation + landing amendment) | Verbal order (chat 2026-08-13) — cohesive-block execution, report-gate then implementation | Report gate: `documentation/architecture/M6_CAUSE_AWARE_TEXTURE_REPORT_ORDER_076.md`. Landing 2026-08-12: `causeTag` extended to 12 values, `causeChainId` threaded through every event stream entry, `assignCauseChain` (20 s window) groups same-cause events, `computeEveningAccount` reads `state.day.drawnCapital` + `state.day.lastScenarioChoice` (§6.a amendment) and feeds `pickParagraph` capital-flavoured leads + a per-choice aside. DoD verified in `frontend/src/strategic/simulation/__tests__/m6.test.ts`: specific-tag coverage 82.1 %, chain-of-3 present, evening divergence max 0.219 (clears ≥ 0.15 first-cut floor). 0.30 target NOT met — sentence-bank rewrite split out under ORDER 077 §2 to milestone M6b. Truncation hypothesis for M3 drift REFUTED (bumping `LEDGER_MAX_LINES` 1000 → 100 000 left drift unchanged; ring buffer never fired). |
| 077 | M6b milestone split + M4 kickoff | Executed 2026-08-13 (M4 landed 4/4 DoDs; M6b filed; truncation refuted) | Verbal order (chat 2026-08-13) | Four points: (1) report the actual truncation-test results (done — hypothesis refuted, drift is linear-in-guests fp compound; see ACES_MODEL_FINDINGS §M3 lines 260–316); (2) mark 0.30 as NOT met in M6 report and add own milestone row for sentence-bank rewrite with target 0.30 — filed as M6b in `STRATEGIC_TRACK_MILESTONES_PROPOSAL.md`; (3) push, verify CI green on both jobs; (4) then M4 (menu + kitchen + stock) with report gate as cohesive block. |
| 078 | M5 cohesive block + M4b assessment + M7 status check | Executed 2026-08-13 (M5 landed 3/3 autonomous DoDs; M4b→M4a split filed; M7 split filed) | Verbal order (chat 2026-08-13) | Three deliverables: (1) M5 (mise en place + rhythm + after-countdown + stream panel sizing) with report gate first — see `M5_MISE_EN_PLACE_AND_RHYTHM_REPORT_ORDER_078.md`; (2) in the same report gate assess whether M4b's attractiveness-weighting deferral is M8-blocking (answer §8: YES, two of six ORDER 051 §8 acceptance items fail under uniform-random draws — recommends splitting M4a onto M8 critical path); (3) state ORDER 049 §5.1 + §1 volume blockage status for M7 (§9: PARTIALLY UNBLOCKED — 272 chef questions approved and wired since commit 7f4baa1, other three roles need one generation pass each, bank-meeting scene still needs an answer-to-loan mapping report — recommends M7 split into M7a/M7b/M7c same shape as M6/M6b). *"bygg inte om något nu"* — no code touched under this order, only the assessment. |
| 079 | M4a cohesive block + M8 perception-punch-list | Executed 2026-08-13 (M4a landed 2/2 DoDs; M8 punch-list filed with 21 items) | Verbal order (chat 2026-08-13) | Two deliverables: (1) M4a (attractiveness weighting + substitute/walkout split) with report gate first — see `M4A_ATTRACTIVENESS_AND_SUBSTITUTION_REPORT_ORDER_079.md`. Formula: `weight = exp(−(price − suggestedPrice) / suggestedPrice × 2)`, 30 / 70 substitute-vs-walkout when a target dish is out, rep hits −0.02 / −0.05. Closes ORDER 051 §8 acceptance items #2 and #4 that M4 as filed cannot satisfy. (2) Enumerate every perception point routed to M8 across the milestone stack (M1 DoD 4, M5 DoD 2, and anything else), one row per point: source milestone, what's being assessed, in what game state it appears. Filed as `M8_PERCEPTION_PUNCH_LIST.md` with 21 rows across ORDER 047 §9, M1/M5 residuals, M6b prose items, ORDER 051 §8 sight-read items, and ORDER 049 §8 (M7-tier). Bundled to ~18 distinct checks; 5 blocked pending M7b/M7c. |
| 080 | Demand curve measurement + DoD 1 tightening + M7a kickoff | Executed 2026-08-13 (M4a §7.b measurement recorded, DoD 1b added, M7a landed 3/3 DoDs, M7 split filed) | Verbal order (chat 2026-08-13) | Four points: (1) measure demand curve at 0.75× / 1.0× / 1.25× / 1.5× / 2.0× / 3.0× of suggested (menu-of-two, pork@baseline, chicken varies) — result recorded in `M4A_ATTRACTIVENESS_AND_SUBSTITUTION_REPORT_ORDER_079.md` §7.b: revenue peak at 1.5× (chicken 9 units, total 6 267 SEK vs 5 435 at suggested). (2) K = 2 not too steep — 1.5× still sells 31% of demand; recommendation to hold K unchanged. (3) DoD 1 tightened with new case DoD 1b: chicken@263 (1.5×) must sell at least 6 units against pork@195, catching regressions that flatten demand above 1×. (4) Then M7a with report gate (chef service-questions, ready to build per M5 §9). |
| 081 | M8 playtest preparation | Issued 2026-08-13, in execution | Verbal order (chat 2026-08-13) | Five deliverables, no new gameplay built: (1) runnable startpoint (npm run dev + URL with `#playtest=1` flag added under this order); (2) dev-artifact toggles — `#playtest=1` hides the aiming crosshair + DevPanel line 4 (pixel probe); calibration quad already default-off via `#calibrationQuad=1`; DevPanel lines 1–3 stay for debrief; (3) the 21-item punch-list reordered by game flow (morning → prep → service → evening → across-run) as a checklist Vision Owner reads at the desk; (4) 5 rows to skip pending M7b (bank meeting) — punch-list rows 15/17/18/19 fully drop, row 16 partially covered by in-service chef questions; (5) recommended 3-day minimum / 5-day thorough playthrough with instructions for forcing collapse (Shift+C), scenario fire (`5`), stock-out (underbuy). Filed as `M8_PLAYTEST_BRIEF_ORDER_081.md`. Small code change: `harnessParams.playtest` boolean added to `urlParams.ts`; DevPanel branches on it to hide diagnostic overlays. |
| 082 | Face / puck integration (verbal, unsent as own document) | **Reference only** — verbal exploration, no order text committed | Verbal order (chat 2026-08-13, unsent) | Referenced by ORDER 084 §Rapport-avsnitt as an unsent verbal order about integrating the guest-reel face/pose vocabulary with the puck geometry. No artefact exists in the repository; number reserved here so it cannot be silently reused. If materialised into work, upgrade this row to a real carrier and file the order text. |
| 083 | Camera pitch probe (measure before deciding on a side view) | Executed 2026-08-13 — measurement report filed, order text never committed as own document | **Report only** | `documentation/architecture/M0_CAMERA_PITCH_PROBE_REPORT_ORDER_083.md`. Verbal order (chat 2026-08-13): probe five camera pitches (15°/25°/35°/45°/58°) at the player-business centroid and report at which pitch the actors' actions become legible. Findings: **at no measured pitch do actions become legible, because the actors are cylinders** — `InteriorStaff.tsx` and `InteriorGuests.tsx` render each character as a single `cylinderGeometry` with no limb articulation, so any answer to "at which pitch does a wave/gait become readable" is honestly "none of them". Report offers two paths (rig the puck vs add a presentation-mode second view), both flagged as ORDER-sized commitments requiring their own report gates before build. Probe script left at `frontend/scripts/pitch-probe.mjs`; camera-clamp throw at `CameraContext.tsx` §92 was temporarily relaxed for measurement and reverted before commit. Dev-only hooks added (`window.__nxSimDispatch`, `window.__nxSimState`, `window.__nxSetBusinessName`) tree-shake at prod build. |
| 084 | Reconcile milestone list against reality (no code change) | Executed 2026-08-13 | Verbal order (chat 2026-08-13) | Documentation-only: (1) audit `STRATEGIC_TRACK_MILESTONES_PROPOSAL.md` row-by-row against commits + test files (`m1.test.ts`, `m2.test.ts`, `m3.test.ts`, `m4.test.ts`, `m4a.test.ts`, `m5.test.ts`, `m6.test.ts`, `m7a.test.ts`) — M1/M2/M3 all landed but the proposal still claimed "3 defects open" / "Not started" / "no UI" respectively; corrected each with commit reference. (2) Register the missing ORDERs 081/082/083 in this registry (081 was already there; 082 filed as verbal-unsent Reference-only, 083 filed as Report-only carrying `M0_CAMERA_PITCH_PROBE_REPORT_ORDER_083.md`). (3) Reopen **M1 DoD 3 Defect B** (morning panels overlap): original closure under commit `bebac5c` inspected 1280×720 only and declared "no spatial overlap"; the 2026-08-13 first M8 playtest pass reproduces overlap on wider viewport. (4) Add the four Vision-Owner-observed playtest findings from the 2026-08-13 day-1 `#playtest=1` pass to `M8_PERCEPTION_PUNCH_LIST.md`: panel-overlap on wide viewport (same as reopened defect), OPEN_SERVICE allowed with empty stock without warning, reputation floors at 0.00 with no visible recovery path, chef-question tone reads as research prose mid-service instead of a question from the pass. No source-code files touched. |
| 085 | Room card panel — Path C picked, report gate first | Report gate opened 2026-08-13; amendment 2026-08-13 under ORDER 086 finalises §2.1 face vocabulary | Verbal order (chat 2026-08-13) | Vision Owner picked Path C from ORDER 083's two-path proposal (rig the puck / presentation mode) plus the ORDER 084 addendum's third option (card panel), formally selecting the card panel as the answer to the actor-legibility question. Rationale (from the order): C solves more punch-list rows than A and equal to B for ~0.3× the effort; cards carry exact per-seat values a rigged puck can only imply; A/B break the ORDER 044 §3.2 puck-silhouette design decision which is the project's expression. The room stays aggregate — position, density, rhythm ring — and action-reading moves to a data-driven card panel. Report gate at `M8_ROOM_CARD_PANEL_REPORT_ORDER_085.md`. Report content: (1) `currentAction` as a pure function of existing state (`taskType`, `guestState`, `platesRemaining`, waiting time) — no hand-authored choreography, testable `state → {text, icon}`; (2) StaffFace 10 expressions from ORDER 082's unsent prototype, derived from the same state rather than set; (3) table cards vs staff cards — fields per surface and how they differ; (4) which M8 punch-list rows this promotes from perception to autonomous DoD. The reel's 8 pose names (IDLE/WALK/SIT DOWN/READ MENU/HAIL/IMPATIENT/EAT/EXIT) are kept as the label vocabulary so the mapping stays traceable back to guest-reel.jsx. |
| 086 | Room card panel — build per ORDER 085 §6 | In execution 2026-08-13 | Verbal order (chat 2026-08-13) | Vision Owner picks per ORDER 085 §5: (1) panel right-column adjacent to InstrumentsPanel; (2) attention-priority sort with seatIndex tiebreak; (3) SVG; (4) Path 3a — card per guest via `seatIndex`, no `Table` entity; (5) hold `'plate'` fallback (no `orderedDishId` state change). Additionally reconciles the face vocabulary vs unsent ORDER 082's StaffFace prototype (`irriterad / förvirrad / förvånad / stolt`) into one final list of ten (per ORDER 085 §2.1 amendment): keeps `neutral / focused / smiling / attentive / tense / strained / hurried / exhausted`, promotes `proud` in place of both `satisfied` and `stolt`, promotes `irritated` in place of `apologetic`, drops `förvirrad` (no reachable state) and `förvånad` (belongs in transition animation not steady face). Builds §6 sequence in commits; responsive layout from first commit (reopened M1 Defect B's failure mode); DOM regression at 1280×720 / 1920×1080 / 2560×1440; CI green both jobs. |
| 087 | Guest movement library + face reconciliation | **Executed in part 2026-08-13** — pattern library + face reconciliation + measurements docs landed in UI/logic layer; scene-layer work (puck lean/bob/pip render) carried by **ORDER 088** | Verbal order (chat 2026-08-13) | Autonomous order gated on Step 0 measurements. Three deliverables landed: (1) `documentation/game-design/UNDERLAG_003_MEASUREMENTS.md` capturing sit depth (analytical against ORDER 053 geometry — puck dip 0.15 m clears table by 0.81 m at pitch 58°), intoningsfönster for kanal B (analytical — face-on-card is camera-independent; deferred to future kanal B activation), and load-distribution (harness-measured, `frontend/src/strategic/simulation/__tests__/order087.faceDistribution.test.ts` at fixed seed 3 — 25 047 staff-tick samples). (2) Face vocabulary bindings in `frontend/src/strategic/ui/RoomCardPanel/deriveFaces.ts`: `exhausted` + `proud` declared STAFF_EXCLUSIVE_FACES, guest `proud` cases replaced with `smiling`, SF-rule ordering reworked so hurried (personal band, at first threshold 0.85 — later raised to 0.95 under ORDER 088 §2.1) reads before strained (room-red + w≥0.7) so all three pressure faces are reachable. (3) Guest patterns 13–20 as pure functions in new `frontend/src/strategic/ui/RoomCardPanel/guestPatterns.ts`: eight labels (IDLE/WALK/SIT DOWN/READ MENU/HAIL/IMPATIENT/EAT/EXIT), deterministic selector, patience derivation, pip-carrier (two-bearer) function per §5.1, IMPATIENT the only backwards-lean pattern (−5°) per §4. `deriveGuestAction` refactored to route through pattern label so kort och rum can't diverge. DoD test coverage: 15 tests in `__tests__/guestPatterns.test.ts` + 1 harness test for face distribution — 565 total (was 549). Typecheck + build green. **DoD 11 (three viewport widths)** is N/A — `deriveFaces.ts` and `guestPatterns.ts` are pure logic with no layout; the viewport-regression test in `RoomCardPanel.test.tsx` already covers this at the panel level. **Scene-layer work outstanding, carried by ORDER 088.** ORDER 087 §8's file list included `InteriorGuests.tsx` / `InteriorStaff.tsx` / `RestaurantActors.tsx` but none were touched — no puck leans, bobs, or dips; no pip renders. That work lands under ORDER 088. INFRA-1 pose additions specced in UNDERLAG_003_MEASUREMENTS.md are checked in under ORDER 088 §5. |
| 088 | Scene-layer wiring — patterns into the room, pip on both bearers | Executed 2026-08-13 | Verbal order (chat 2026-08-13) | Autonomous order completing ORDER 087 §§ 2–3 in the scene layer + amendments per §2. (§2.1) Workload-percentile measurement showed pre-set bands were pre-set-not-mass — p50 = 0.916, p75 = 1.000; hurried threshold moved 0.85 → 0.95, rebalancing face-distribution from 33 % / 13 % / 11 % (hurried / strained / tense) to 21 % / 25 % / 11 %. Under the pre-fix threshold hurried captured 8 271 ticks of 25 047 (33 %); under the fixed threshold hurried drops to 5 190 (21 %) and strained rises from 3 192 to 6 273 (13 % → 25 %). Tense unchanged (2 787 ticks, 11 %) — the amber-rhythm precondition is untouched. Numbers repeatable via `ORDER_087_MEASUREMENT_LOG=1 npx vitest run src/strategic/simulation/__tests__/order087.faceDistribution.test.ts`. Chat-summary numbers "33/40/18" and "60/15/11" were both wrong at time of writing — this entry is the authoritative source (matches UNDERLAG_003_MEASUREMENTS.md §0.3). (§2.2) `smiling` audited: three states (leaving / paying / dining) shared identical `satisfaction ≥ 0.7` condition, but the pure function admitted smiling for walk-away tuples in paying/dining — walk-away guard added on all three branches so reader stays semantically singular. New test: `smiling has exactly one guest reader`. (§2.3) `SIT_DIP_M` raised from 0.15 → 0.27 m against legibility criterion: pitch 58° / 8.4 m projects 0.15 m to ~13 px (indistinguishable from standing), 0.27 m to ~24 px (distinctly seated). Head clearance over table still 0.69 m. (§3) New `frontend/src/strategic/scene/patternTransform.ts` — pure functions of `(label, profile, simTime, phaseSeed)` returning `{leanRad, bobY, microYawRad, moveSpeedMPerSec}`. Single owner of the primitives; shared kinds IDLE/WALK/EXIT route through one params table branched by profile (guest/staff). Guest-only kinds (SIT DOWN, READ MENU, HAIL, IMPATIENT, EAT) have their own amplitude table. IMPATIENT is the only pattern with negative leanRad (−5°). InteriorGuests wraps each puck in a group so pattern lean pivots at ground; applies bob, microYaw, and (via existing sit-stand) drop. Deterministic per-guest phase seed (FNV-1a of guest id — no Math.random). (§4) Pip rendered on both bearers: 0.06 m cube, +0.15 m above puck top, amber `#ffbe4a`, no number. Visibility toggled per tick from `derivePipCarriers`. On the staff side the room renders TeamMember pucks (economic layer) so pip lookup is by role-match to StaffMember's targetGuestId — see InteriorStaff.tsx comment. (§5) INFRA-1 pose additions remain **specced but not shipped** — visual runner still lacks scenario-setup harness that lands a `seated` guest at a known seat under fixed sim-state. Poses `guest-seated-pitch-58` and `guest-seated-pitch-20` are documented in UNDERLAG_003_MEASUREMENTS.md §0.1 with camera parameters and ROI targets; ROI RGB bands set from first runner execution with scenario support. §7 forbids widening thresholds → poses land when runner supports the setup, not on placeholder bands. (§6) ORDER 087 registry entry rewritten as *Executed in part — scene-layer work carried by ORDER 088*; DoD 11 marked N/A with rationale. **DoD:** 10 new tests in `scene/__tests__/patternTransform.test.ts` + 2 new smiling tests in `guestPatterns.test.ts` (17 net new after threshold-change fixup). Total 577 tests (up from 565). Typecheck + build green. |
| 089 | Medgången — rapportgrind för M9 | Report gate landed 2026-08-13 | Verbal order (chat 2026-08-13) | Report-only order (no code). Produces `documentation/blueprints/M9_MEDGANG_REPORT_ORDER_089.md` — six sections addressing the observation that the feedback architecture is one-sidedly negative. §1 medgångsinventering measured via new `ORDER_089_MEDGANG_LOG=1` branch on the face-distribution harness — staff faces during service are 60 % pressure (strained + hurried + tense + irritated), 40 % neutral/focused/attentive; smiling + proud fire 0 ticks on the staff side. Guest smiling is dominant (45 %) but that reads on guest cards, not staff row. Rhythm is red 75 % of service ticks. §§2–5 present choices for rep recovery (paths A/B/C), empty-stock opening (A/B/C), guest's clock (channel + expression + time-out behaviour), and chef question cost (form + tone). §6 flags M8 punch-list rows 23/24/25 as autonomous after build. §8 proposes a second playtest question ("would I want to play another evening?") beyond ORDER 081's "did you understand the room?" — the missing measurement for medgång. No thresholds changed, no ORDER 087/088 decisions rubbed. Registry entry deliberately does not name a next-order carrier — the decision fields in §9 must land first. |
| 090 | Rättningar före commit (Corrections order over 088/089) | Executed 2026-08-14 | Verbal order (chat 2026-08-14) | Four fixes before ORDER 088/089 commit. **§1** ORDER 088 registry percentages were wrong at time of writing ("33/40/18" in registry vs "21/25/11" in chat summary vs authoritative harness output). Reconciled — one number survives: hurried/strained/tense = 21%/25%/11%, verifiable via `ORDER_087_MEASUREMENT_LOG=1 npx vitest run src/strategic/simulation/__tests__/order087.faceDistribution.test.ts`; the UNDERLAG_003_MEASUREMENTS.md table has been correct throughout. **§2** ORDER 088 DoD 9 required a screenshot showing sittande vs stående go att skilja åt — §2.3 delivered a projection calc, not an image. Fixed: `frontend/scripts/order090-sit-depth-screenshot.mjs` renders the three silhouettes (standing / seated 0.27 m ORDER 088 / seated 0.15 m ORDER 087 pre-fix) at the exact projection (92 px/m at pitch 58° / 8.4 m / 720p / VFOV 50°) via headless Chromium. Output at `frontend/reports/order088/sit-depth-pitch58.png` — 25 px delta for ORDER 088 dip is visibly distinct; 14 px delta for pre-fix reads as standing. `SIT_DIP_M` now marked as measured (not merely calculated) in UNDERLAG_003_MEASUREMENTS.md §0.1 — the image is the artefact. Legibility criterion holds at 0.27 m; no change needed. **§3** Pip role-match in InteriorStaff was ambiguous with two servitörs. Built id bridge: new `frontend/src/strategic/scene/teamStaffBridge.ts` with `bridgeTeamToStaff()` — bipartite role-match in list order, pure function. `teamPipCarriersFromStaffPipCarriers()` composes bridge + pip carriers into a Set of TeamMember ids. InteriorStaff switched from role-match to bridge-lookup. 6 new tests in `scene/__tests__/teamStaffBridge.test.ts` — including the crucial two-servitörs test proving pip lands only on the specific puck whose owning task edge fired. **§4** Two new decision fields added to M9 report §9: **9a** `proud` has zero readers — path A (drop as `förvirrad` was) or B (give it more triggers); **9b** load-model calibration — p50=0.92, p75=1.00 means load is saturated as default state, not stress-tailed. §4 utred-not-bygg; no code touched under §4. **DoD:** registry corrected, screenshot committed, pip test green with two servitörs, two new decision fields present. Total 584 tests (was 577). Typecheck + build green. No thresholds widened. |
| 091 | Schemat landar i repot; milstolpelistan revideras | Executed 2026-08-14 | Verbal order (chat 2026-08-14) | Documentation-only order (no code touched). The Miro board `uXjVIGbwLcE` (printed 2026-08-14) describes the game's whole loop — ankomst → paviljongerna (kunskapsprov i fem miljöer) → bankmötet (lån ur kunskapsresultatet) → verksamhetsval → satsningar → service (viktad av satsningarna, med action-knapp) → utfall i tre hållbarheter → quizer mot uppvisade svaghet → nytt varv med ackumulerat kapital — and Vision Owner's 2026-08-14 precisering §3 that kunskapskapitalet vid bankmötet avgör verksamheten (inget lån / food truck / restaurang). Three artefacts produced: (§1) `documentation/foundation/vision/SPELSLINGAN_SCHEMAT.md` — the schema, placed alongside `ORDER_100_VISION.md`. Marks the tavlan as extern; the repo's kopia yields to tavlan at conflict, and updates when tavlan updates. (§2) `documentation/blueprints/MILSTOLPAR_MOT_SCHEMAT.md` — the underlag that reads M0–M8 against the schemat, introduces verksamhetsklass as ett nytt grundbegrepp (restaurang är en av två skepnader, food truck den andra — inte en mindre restaurang), and derives the R1–R7 ryggrad. (§3) `documentation/blueprints/STRATEGIC_TRACK_MILESTONES_PROPOSAL.md` revised to version 1.2: adds a **Ruta i slingan** column to §1 table so every M-milestone maps to its position in the schemat; adds §1.5 R1–R7 ryggraden (kunskapskapital + krediter, paviljongerna, bankmötet, verksamhetsklass, action-knappen, quizer mot uppvisade svaghet, omgångsslingan); adds §1.6 reopening ORDER 089's medgång-tolkning (belöningen är krediter från R1/R5, bygg inte parallell mekanik inuti servicen) and SD-003's dockskåp (pausas och revideras mot R4 — behöver hålla minst två skepnader). Two status reclassifications noted **in place** (no renumbering): **M7b's block is not technical** — the answer-to-loan mapping *is* the game design, moved as a design decision to R3 (M7b stays as the M-family surface built after R3 delivers the decision); **M7a's position is wrong** — schemat places quizerna post-service, M7a's chef-questions fire mid-service and picked up the "research prose mid-service" M8 punch-list row 25 reading — M7a's mechanism and DoD trail survive intact, the trigger surface is proposed to migrate to R6. §5 VS001-merge trigger reopened: rather than merging at end-of-M4, VS001 becomes the way into R2 (paviljongerna) — recommendation stands, trigger shifts. §7 (open questions) gains four new items on ryggrad ordering, VS001 re-trigger, M7a placement move, R3 as a design-decision gate. **DoD:** three artefacts placed with the paths above; registry entry recorded; no code touched; no thresholds widened. |
| 092 | R3 rapportgrind: kunskapskapitalet, bankmötet, kurvan | Report gate landed 2026-08-14 | Verbal order (chat 2026-08-14) | Report-only order (no code). Produces `documentation/blueprints/R3_KUNSKAPSKAPITAL_REPORT_ORDER_092.md` — thirteen sections around the R3-ryggrad-milestone (bankmötet + answer-to-loan-mappningen från ORDER 091 §1.5). Six things beslutade in the ORDER itself and written into the report as fixed (§§ 1–2): (a) tre axlar (episteme/techne/phronesis) inte fem; (b) `krediter` som vektor med tre komponenter, inte skalär — R1:s enda hårda beroende av R3; (c) profilmatchning i ingången av bankmötet läser form över summa (techne-tyngd → food truck, phronesis närvarande → restaurang, inget kunnande → tillbaka till paviljongerna); (d) ingen siffra visas i bankmötet — diagnostisk röst per EXECUTIVE_DESIGN_DIRECTIVE_001 §7; (e) kurvan går att vända — svårighet stiger per varv som grundtillstånd, skicklighet lutar tillbaka, ett bra varv kan gå plus även sent; (f) spelet kan ta slut som en sista kväll med facit (inte defeat). Sex utred-avsnitt (§§ 3–8) med beslutspunkter Vision Owner måste välja: (§3) profilavläsningen — trösklar/kvoter/vinkel (rekommendation: vinkel över sfären, håller magnitud och form frikopplade); (§4) kreditekonomin — cash och krediter byter aldrig plats, gränssnitt endast via bankmötet, provkostnad enligt val A/B/C (rekommendation: val C — varv-slot); (§5) svårighetskurvans lutning — fyra rattar (attractiveness, walkout-tröskel, reputation-tak, quiz-svårighet) mätt via INFRA-2-manus med Policy A/B/C/D; (§6) uppstigningen — **den viktigaste kvalitetsfrågan i hela ordern** — restaurangen som tak (Design 1) eller food truck som egen topp (Design 2); rekommendation Design 2 på grundval av schemat §3 ("en egen verksamhetsklass, inte en mindre restaurang"); (§7) antal varv — ~10–15 gissning, målmedian sätts efter mätning; (§8) M-listan — M3 måste peka ut domän för R6, `state.knowledgeCredits` som ny fältstruktur, punchlistrader 24/25 berörs, två nya rader (bankmötesröst, sista-kvällen). §9 slutet — sista-kvälls-panelen som fakta + profildiagnos + kapitel-slut + vad-som-behövts + ingen omstart-knapp. §10 ORDER 089:s medgång **omtolkas bekräftat** — en vändbar kurva besvarar M9-rapportens fynd utan att bygga ny mekanik inuti servicen; §§ 2–5 i M9 bör inte byggas före R1 och R5; M9 §9:s två beslutsfält (proud reader-path, load-model calibration) står kvar oberoende. §11 avgränsningar — ingen kod, inga trösklar från beräkning (allt numeriskt mäts i INFRA-2 innan DoD), beslut C / puck-silhuetten / regeln-om-inga-stat-paneler / SD-003 oförändrat. §12 byggordning framåt — R3 § 3 beslut → R1 kod → R2 paviljongerna + VS001 merge → R3 mätgrind → R3 §§ 5–7 beslut → M7b scen → R4/R5/R6 parallellt → R7 → M8 utvidgad. §13 tre öppna frågor rapporten inte svarar på (axel-ortogonalitet, spelarens identitet i bankmötets text, kan spelaren se sin profil mellan varv). **DoD:** rapporten placerad, tre beslut som §2 i ORDER 092 satt återfinns oförändrat i §§ 1–2, sex utred-avsnitt med beslutspunkter, registerpost recorded, ingen kod touched. |
| 093 | Väg C-utökad: kalibrering och den fjärde klassen | Executed 2026-08-14 (autonom §§ 2–4; grind §5 delivered to Vision Owner) | Verbal order (chat 2026-08-14) | Report-only order (no production code); builds on ORDER 092. Produces `frontend/scripts/order093-cone-sweep.mjs` + `frontend/reports/order093/cone-sweep.json` + reviderad §3.6/§3.7 i `documentation/blueprints/R3_KUNSKAPSKAPITAL_REPORT_ORDER_092.md`. §§ 1–2 skrev in i R3-rapporten att Väg A och B avförs och Väg C behålls som **fyra-sektor-läsare**: tre axelkoner (techne / phronesis / episteme) plus centrumsektor för profiler utanför alla koner men över magnitudgolv, med precedens phronesis > techne > episteme > centrum > inget-lån vid överlappande koner. §§ 3–4 (autonom) körde konsvep 20°–50° i 5°-steg över samma 9 261-profils-kub som ORDER 092 §3.4. Utfallsfördelning per konvidd: 20° → centrum 88 %, specialister 4 % var; 40° → centrum 43 %, specialister 19 % var; **45° → centrum 21 %, specialister ~26 % var (symmetri, precedens inaktiv)**; 50° → centrum 5 %, phronesis 36 % / techne 32 % / episteme 27 % (precedens klipper). De fyra gränsfallen kördes under varje bredd — jämnstark låg och hög stannar centrum genom hela svepet (tippunkt geometriskt 54.74°, utanför svepet), enbart episteme fångas som near-episteme redan vid 20° (tippunkt 4.5°), techne+episteme (0.70, 0.70, 0.10) tippar vid 50° från centrum till food-truck (precedens: techne > episteme; tippunkt 45.3°). Designgräns för "bredd är en egen väg" identifierad som cone < 54.74° — hela svepet håller sig inom det första regimen så bredd är en väg för varje testad konvidd; valet kalibrerar HUR mycket väg. §5 (grind) formulerade fem beslutspunkter för Vision Owner i R3 §3.7: (1) konvidden (rekommendation 45° — symmetriskt tydligaste punkten); (2) den fjärde klassens namn (tre kandidater föreslagna: vinbar, bistro/café, konsult/catering); (3) episteme-sektorns verksamhet (egen klass / sammanslagen med restaurang / inget lån); (4) gränsfallens klassning (följer implicit av 1–3); (5) magnitudgolvet — ORDER 093 §3.6.5-flagga: nuvarande golv 0.10 fångar bara 8 profiler, jämnstark-låg blir centrum → fjärde-klassen, vilket kanske inte är designintuitionen; rekommendation separat centrumgolv (norm ≥ 0.4 för centrum, 0.10 för specialister). §6 flaggar konsekvenserna för R4 (verksamhetsklassen kan växa från två till fyra skepnader) och SD-003 (dockskåpet-direktivet pausas redan mot R4 per ORDER 091 §1.6 — måste bära fler skepnader vid revidering). **DoD:** skriptet utökat och kört, R3 §3.6/§3.7 uppdaterat, alla åtta krav i ORDER 093 §7 uppfyllda, registerpost recorded, ingen produktionskod touched. **Branch note:** spun från `order-092` (som i sin tur spanns från `main`) — samma tunna-branch-struktur som ORDER 092. Gap-raden 046–091 ärvs oförändrad. |
| 094 | *(not found in repo)* | Not found in repo | — | Number not evidenced in any committed document. Gap recorded for sequence integrity. |
| 095 | R1 — kunskapskapital och krediter | **Void — never built; work carried by ORDER 102** | — | Ordertexten utfärdades muntligt 2026-08-14 med R1-innehåll (state.knowledgeCredits, bankmötets loan-mappning). Branch `order-095` skapades men **inga commits landade på den** (`git log order-049..order-095` = 0). Filerna som senare rapporterades finnas (`resolveLoanOutcome` i `businessProfile.ts`, 18 tester) existerar inte i något worktree — verifierat 2026-08-15 via `grep -rn "resolveLoanOutcome\|knowledgeCredits" frontend/src/` (0 träffar på både main, order-049 och order-095). ORDER 102 (utkast, väntar Vision Owner-godkännande) återupptar samma arbete med explicit DoD och tester. Raden 095 behålls här som sekvens-integritetens gap-markör; numret återanvänds inte. |
| 096 | Dockskåpets mätgrind | Executed 2026-08-15 | Verbal order (chat 2026-08-15) | Measurement-gate order for SD-003 §5 (the dollhouse / 2D cross-section restaurant concept). No production code touched. Produces: `frontend/scripts/order096-fps-benchmark.mjs` (standalone Playwright benchmark; rig code copied from `documentation/prototypes/staff-guest-reel-extended/guest-reel.jsx`, ported to vanilla JS); `frontend/reports/order096/results.json` (machine-readable measurements); `frontend/reports/order096/bench-30.png` (screenshot of 30 figures in motion); `frontend/reports/order096/bench-30.webm` (12 s recording); `documentation/blueprints/SD003_MATGRIND_RAPPORT_ORDER_096.md` (blueprint report). §5.1 FPS sweep (5/10/20/30/40 figures): **all counts PASS** — N=30 mean 120 fps, p5 119 fps (threshold 60 fps, margin ×2). Performance ceiling is the display's 120 Hz vsync; at N=40 p5 = 108 fps. React reconciliation overhead (est. 5–15%) would bring worst-case to ~92–100 fps at N=40 — still well above threshold. §5.2 Room-width finding: at alt-C layout (70% dining fraction), **only 9 figure-slots fit across the dining room at any 16:9 screen width** at 140 px readability — figure width scales proportionally with screen height, so the slot count is resolution-independent. 16 covers at 140 px do not fit in a single pan-free view. Vision Owner choice required: fewer simultaneous covers / panoramic scrolling / smaller figures. §5.3 Map minimum size: 206×74 px. Below this threshold, puck+rhythm-ring density becomes decoration rather than spatial instrument. §5.4 Artefacts committed. SD-003 remains paused; this order produces the measurement basis for its revision, not the revision. |
| 097 | Interior pointLight, DevPanel seat-diagnos, mät-infrastruktur | Executed 2026-08-15 | Verbal order (Anders, 2026-08-15) | Tre delar, ingen ändring till simulation-koden. **§1** `frontend/src/strategic/scene/PlayerBusiness.tsx` (+44): warm-tungsten `<pointLight>` (`#ffb460`, position `[cx, 2.0, cz]`, `distance=9`, `decay=2`, `castShadow=false`) placerad inuti interior-gruppen. Intensitet driven per frame från `skyState.nightFactor × serviceGain × interiorVisibility × 22` — samma ramp som väggens emissive (`skyState.nightFactor × serviceGain`). Vald framför emissive på gästmaterialet med Vision Owner-motiv (2026-08-15): emissive gör pucken självlysande vilket läser som symbol, samma invändning som fällde opacitetspulsen i ORDER 044 §3.3. Kravet "sittande och stående går att skilja åt" (ORDER 090 §2-kriterium) verifierat via `frontend/reports/interior-lighting/dinner-myBusiness.png` — sittande cylindrar (dip 0.27 m per ORDER 088 §2.3) är visuellt distinkta från stående (1.70 m) i det tända rummet vid pitch 50° / distance 24 m. **§2** `frontend/src/strategic/ui/DevPanel.tsx` (+52/−2): suffix `queue=N seated=S/C` på weather-raden där `N = sim.waitingIds.length`, `S = sim.seatedIds.length`, `C = sim.policies.capacity`; drift-flag `!layout=X` om `layout.seats.length ≠ capacity`. Plus dev-only `console.info` som rate-limitas per tuple-ändring när `queueLive > 0` (throttled via `lastLoggedRef`, inte tid — stabilt state spammar inte). Motiv: samma skäl som cam-suffixet i ORDER 090 §5 — svarar på "varför är rummet tomt?" utan konsolgräv. Skiljer live-kön (`waitingIds.length`) från prognosen (`waitingAtOpening`) som redan visades på weather-raden; de två blandades tidigare. **§3** Mät-infrastruktur under `frontend/scripts/`: `interior-lighting-screenshot.mjs` (playwright headless, återanvänder befintlig `npm run dev` om 5173 svarar, pinnar myBusiness-preseten + `period=dinner`, seedar sim via `__nxSimDispatch` med chunkade `TICK`-batches + React-commit-yields, screenshotar canvas efter DOM-overlay-hide); `verify-harness-guests.mjs` (kör ORDER 087 §6.5-scriptet direkt mot reducern och redovisar `state.guests`/`workload`-fördelning på fast frö=3); `verify-load-chain.mjs` (30 s-sampel över hel service, spårar sim.guests → staff.workload → soc). **DoD:** typecheck grön, 601/601 tester grön (oförändrat totalantal — `.mjs`-skripten står utanför vitest-sviten). Ingen tröskel omkalibrerad. Ingen ändring till `service.ts`, `reducer.ts`, `arrivals.ts`, `types.ts` eller något simulation-fält. Kopplings-utredning `sim.guests → StaffMember.workload → RoomCardPanel/deriveFaces` lämnas till `documentation/architecture/LOAD_CHAIN_TRACE_2026-08-15.md` (separat docs-commit). |
| 098 | Dining-hålet — checkback fyller task-gapet under vardaglig service | Executed 2026-08-15 | Verbal order (Anders, 2026-08-15); bygger på `documentation/architecture/LOAD_CHAIN_TRACE_2026-08-15.md` | Autonom order som täcker det som spårningen namngav: `dining`-tillståndet matchade ingen task i PRIORITY under `service='vardaglig'` (bara `decant`/`flambe`, gated bakom `service='formell'` — `service.ts:385`), så staff gick idle med fullt rum och `workload` decayade mot 0. **§1 (kod)** Ny task-typ `checkback` (tillsyn under dining) i `types.ts:TaskType`. Ny Guest-fält `lastCheckbackAt: number \| null` (`types.ts:141`, initialt `null` i `model.ts:makeGuest`). PRIORITY utökad i `service.ts:304` — `checkback` ligger efter `serve` och före `decant`/`flambe`. `findTaskTarget('checkback')` matchar dining-gäst där `simTime − (lastCheckbackAt ?? stateTime) > 15` (CHECKBACK_COOLDOWN_SEC). `completeStaffTask('checkback')` sätter `lastCheckbackAt = simTime` och bumpar `satisfaction += 0.03` (CHECKBACK_SATISFACTION_BUMP — litet, avsiktligt; serve +0.08 och decant/flambe +0.14 är fortfarande de större drivarna). `TASK_BASE_TICKS.checkback = 6` (~1.5–2 s slutlig varaktighet efter training/concept-skalning). **§2 (namngivning per ORDER 098 §5)** `RoomCardPanel.tsx:109`: `sideBarLabel: 'load'` → `'workload'`. UI-etiketten följer nu fältnamnet (`StaffMember.workload`); `TeamMember.load` existerar inte i koden och associationen faller. Beslutet dokumenterat i `LOAD_CHAIN_TRACE_2026-08-15.md §5.1`. **§3 (mätning per ORDER 098 §4 + §6.5)** Ansiktsfördelning ommätt på fast frö=3, samma sample size (25 047). Förskjutning: hurried 20.7 % → **50.8 %** (+30.1 pp), strained 25.0 % → 5.9 %, tense 11.1 % → 0.7 %, attentive 30.7 % → 36.0 %, neutral 7.5 % → 0.8 %. Workload p10 0.350 → 0.958, p50 0.916 → 1.000. Bandet `>=0.85` växer från 59.3 % → 96.6 % av samplen. **Stor förskjutning — redovisad som fynd, banden 0.95 och 0.7 orörda per §4/§6.6** (kalibrering skulle upprepa samma fel för fjärde gången innan R4 löst capacity-frågan). Detaljtabell i `LOAD_CHAIN_TRACE_2026-08-15.md §5.2 + §5.3`. **§4 (tester)** Nya `frontend/src/strategic/simulation/__tests__/order098.checkback.test.ts` — två tester: (a) handkonstruerat scenario med sex dining-gäster, tre staff, workAvg börjar på 0 och lyfter över noll efter checkback-cooldown; (b) regression på reproducerade fallet från trace-rapporten (seed=3, samma script som order087-harnessen) — workAvg > 0 under service-fönstret. Fixtures i åtta befintliga testfiler uppdaterade med `lastCheckbackAt: null` (collapse, morale, team, eventStream, deriveFaces, deriveActions, guestPatterns, RoomCardPanel). **DoD:** typecheck grön, hela sviten grön. Ingen ändring till reducer-utfall utöver det avsiktliga (checkback-tasken); befintliga testers antaganden bibehålls. Ingen tröskel omkalibrerad. Inga band flyttade. Capacity-frågan hör fortfarande till R4. |
| 099 | Commit-verifiering — regler in i CLAUDE.md | Executed 2026-08-15 | Verbal order (Anders, 2026-08-15); ordertext utfärdad som ORDER 100 men **renumrerad till 099 per CLAUDE.md §9** (kollision — 100 var reserverad för Nexus Studio Game Design Constitution) | Autonom order som rör enbart `CLAUDE.md` och registret. Bakgrund: fyra ordrars arbete låg ocommitterat i huvudworktreets arbetsträd utan att märkas; ORDER 097 rapporterades committad två gånger innan den var det; en registerrad för 097 dikterades och antogs vara inskriven medan den aldrig skrevs. Kostnaden blev två felsökningsomgångar och en playtest-diagnos byggd på kod som bara fanns som osparade filer — en `git checkout` hade raderat den. Ordern gör verifieringen synlig i rapporten. **§1** Nytt avsnitt "Commit-verifiering" i `CLAUDE.md` mellan "Regler för Claude Code" och "Definition of Done", med fyra bindande regler: (a) `git log --oneline -3` efter varje commit, utdata visas i svaret — ingen commit räknas som gjord förrän den syns i historiken; (b) `git log --oneline -1` vid början av varje order så det syns om arbetet står på fel commit; (c) `git status --short` vid slutet av varje order — orent arbetsträd ska förklaras i rapporten; (d) registerrad och commit hör ihop — en rad i `ORDER_REGISTRY.md` får inte skrivas för arbete som inte är committat, och ska ligga i samma commit som det arbete den beskriver. **§2 (uttryckligen inte)** Ingen pre-commit-hook, inget skript som städar arbetsträdet, ingen automatik som committar åt agenten. Osynligheten var felet — inte avsaknaden av maskineri. **DoD:** avsnittet finns i `CLAUDE.md` med alla fyra reglerna, ingen automatik tillagd, registerpost för ORDER 099 i samma commit som CLAUDE.md-ändringen, verifierad enligt sin egen regel (`git log --oneline -3` i rapporten). Ingen produktionskod, inga trösklar, ingen designfråga. |
| 100 | Nexus Studio Game Design Constitution (vision reference) | Reclassified as vision reference (outside precedence order) per SD-002 §3 | **Own document** | `documentation/foundation/vision/ORDER_100_VISION.md` (markdown). Converted from `.docx` and relocated under ORDER 034 §6, commit `69df962`; source `.docx` archived alongside the previous name. |
| 101 | Sammanslagning — linjen tillbaka till main | Executed 2026-08-15 (steg 1–2 klart; PR till main öppen) | Verbal order (Anders, 2026-08-15) | Autonom merge-order som för tillbaka tre ordergrenar till `order-049` och sen `order-049` till `main`. Ingen produktionskod ändrad utöver det som redan var committat på de tre inkommande grenarna. **§3.1 (order-095)** No-op: `git merge-base --is-ancestor order-095 order-049 → sant`. Grenen skapades men inget R1-arbete committades där (`git log order-049..order-095` returnerar 0). ORDER 101 §1-tabellens rad för order-095 (R1 kunskapskapitalet) stämmer inte mot repo-state — R1-koden finns ingenstans som committad. Registerrad 095 (Not found in repo, referenced only) landade i registret via order-096-mergen. **§3.2 (order-096 → order-049)** Merge-commit `8acd34f`. Två unika commits: `868b32b feat: ORDER 096 — Dockskåpets mätgrind` + `ff893dd docs: ORDER 096 rapport — isolationsförbehåll och pointerEvents-flagga`. Registry-konflikt löst genom att behålla båda sidors rader och sortera stigande: order-096 hade lagt in rader för 094 (gap), 095 (referenced) och 096; order-049 hade rader för 097 och 099. Slutlig ordning: 094, 095, 096, 097, 099. Typecheck grön, 601/601 tester grön efter mergen (checkback-testerna kom via 098-mergen). **§3.3 (order-098 → order-049)** Merge-commit `521ee82`. En unik commit: `d85698f feat: ORDER 098 — dining-hålet fylls med checkback-task`. `LOAD_CHAIN_TRACE_2026-08-15.md` mergade automatiskt (§5 lades till på slutet, ingen konflikt). Registry-konflikt löst samma sätt: 098 före 099 stigande. Typecheck grön, 603/603 tester grön (601 + 2 nya från checkback-tester). **§4 (PR)** `order-049 → main`, ingen squash — historiken per order är projektets spårbarhet. PR-beskrivning innehåller: ordrar 046–099 som ingår (114 commits ackumulerade under 3 månaders divergens från main); milstolpar M4/M5/M7a/M8/M9 som landat i perioden; öppna frågor `capacity` mot bemanning (R4) + ansiktsbanden 0.95/0.7 som väntar på R4-frågan + checkbackens effekt hurried 20.7 % → 50.8 % som är redovisad men inte åtgärdad; att SD-003 är pausad mot R4 per ORDER 091 §1.6. **§5 (post-merge)** Enligt ordertexten avvecklas grenarna order-095/096/098 och deras worktrees efter merge till main. Utförs som separat städ-omgång när PR:en är mergad — inte i den här commiten. **§6.7** `git log --oneline -5` visar linjen: 521ee82 (098-merge), 8acd34f (096-merge), ed03d16 (099), d85698f (098-commit inbragt via merge), 0689410 (docs). **§7 avgränsningar respekterade:** inga trösklar rörda, inga designfrågor avgjorda; capacity, ansiktsbanden och checkbackens belastningseffekt förblir öppna frågor som följer med i PR-beskrivningen. |
| 102 | R1 Kunskapskapital — bygga strukturen och loan-mappningen | **Executed 2026-08-15; ACCUMULATE_KNOWLEDGE-tak per axel kvarstår öppen fråga (hör till R3)** | **Own document** | `documentation/architecture/ORDER_102_R1_KUNSKAPSKAPITAL.md`. Ersätter ORDER 095 (aldrig byggd, se rad 095). Bygger på R3-rapporten (`R3_KUNSKAPSKAPITAL_REPORT_ORDER_092.md` §§ 1, 3.6.5, 3.7, 8.2, 12) och Vision Owner-beslut under ORDER 093 §5 (konvidd 45°, magnitudgolv 0.10 specialister / 0.40 centrum). **Vision Owner-beslut 2026-08-15 (chat) inskrivna:** (1) intern nyckel `'balanced'` för fjärde klassen, `loanTier: 'restaurant-small'` som placeholder tills R4 §3.7 p2; (2) `nearEpisteme` → `loanTier: 'none'` (avsiktligt utan lån); (3) diagnostisk röst inline i `businessProfile.ts` som test-fixturer, ej via `strings.sv.ts`; (4) `ACCUMULATE_KNOWLEDGE`-tak öppen fråga, R1 implementerar utan tak. **Kod-execution:** (a) `frontend/src/strategic/types.ts` — nya typer `KnowledgeAxis`, `KnowledgeCredits`, fält `SimulationState.knowledgeCredits`, action `ACCUMULATE_KNOWLEDGE`; (b) `model.ts` — init till alla noll; (c) `reducer.ts` — case `ACCUMULATE_KNOWLEDGE` med amount ≥ 0-klämning; (d) `frontend/src/strategic/simulation/businessProfile.ts` (ny fil, 129 rader) — rena funktioner `readProfile()` och `resolveLoanOutcome()`, konstanter för konvidd + magnitudgolv, fem klasser (`restaurant` / `foodtruck` / `nearEpisteme` / `balanced` / `noLoan`), precedens phronesis > techne > episteme > balanced > noLoan; (e) `DevPanel.tsx` — dev-only suffix `credits=E.. T.. P..` på capitals-raden (samma pattern som ORDER 097 queue=/seated=); (f) `frontend/src/strategic/simulation/__tests__/businessProfile.test.ts` (ny fil, 23 tester) — 5 klasser + 4 gränsfall + 2 precedens + 5 loanTier-mappning + 6 reducer + 1 init = 23 tester (över minimum 18). **DoD:** typecheck grön, hela sviten grön (626/626, upp från 603 → 626 = +23 nya). Ingen ändring till scen-lagret, service.ts, arrivals.ts eller något UI utanför DevPanel-suffixet. Ingen tröskel omkalibrerad. Låser upp R2 (paviljongerna ackumulerar via `ACCUMULATE_KNOWLEDGE`), M7b (bankmötets scen läser `resolveLoanOutcome`), R4 (verksamhetsklassen väljs ur `readProfile`), R6 (post-service-quiz riktas mot uppvisade svaghet via samma vektor). |
| 103 | Avslut — main-merge, städning, kortlivade grenar | Executed 2026-08-15 | Verbal order (Anders, 2026-08-15); fullbordar ORDER 101 §§4–5 | Autonom avslutsorder efter ORDER 101 lämnade merge-till-main och städning öppen. **§2** PR #13 mergad (`gh pr merge 13 --merge` — ingen squash, historiken per order intakt). Merge-commit `fd3a5d4`; `8a50610` (ORDER 102 execution) verifierat ancestor till main. **§3 grep-checks:** (a) `'balanced'` i `strings.sv.ts` — 0 träffar, PASS; (b) PNG/docx/prototype utanför tillåtna platser — partiell (en .docx under `documentation/archive/` för ORDER 100 provenance per ORDER 034 §6, PNG bara i `node_modules/`, inga prototypmappar utanför `documentation/prototypes/`), OK; (c) registret stigande + inga Gap + inga "Not found in repo" — **oadresserad i denna order**: 12 gap-rader + 11 "Not found in repo"-rader krävs av registrets egen §5 ("Gaps in the sequence are shown, not hidden"), och registret är faktiskt osorterat efter rad 045 (`049, 048, 047, 053, 052, 051, 050, 060, 061, 046, ...`) från historiska batch-insertions. Att lösa kräver antingen §5-revidering eller separat sorterings-order. **Sviten:** typecheck grön, 626/626 tester grön på main efter merge (samma som order-049 innan). **§4 städning:** `git worktree remove` + `git branch -d` + `git push origin --delete` för `order-095`, `order-096`, `order-098`. Grenarna verifierade som fullt innehållna i main via `git merge-base --is-ancestor` innan borttag. Filen `0.` (skräpfil från felskriven omdirigering) borttagen. `frontend/LICENSE.md` lämnad som är per ordertexten. **§5 nytt arbetssätt:** ny sektion "Grenar är kortlivade" tillagd i `CLAUDE.md` bredvid Commit-verifiering — "En order = en gren från main, mergad tillbaka när DoD är uppfylld. Ingen gren lever över mer än en order." Motivering i-linje: långlivade linjegrenar är strukturella grunden till samma fel som Commit-verifiering-reglerna adresserar. **DoD:** typecheck grön, hela sviten grön på main + order-103, PR öppen för order-103 → main. Öppna frågor som följer med oförändrade (capacity mot bemanning i R4, ansiktsbanden 0.95/0.7, checkbackens hurried-effekt, ACCUMULATE_KNOWLEDGE utan tak, fjärde klassens player-visible namn). |
| 104 | R2 Paviljongerna — kunskapskapitalet får väg in | Executed 2026-08-15 (sim-lager); UI-panel och VS001-koppling separat framtida order | Verbal order (Anders, 2026-08-15); bygger på ORDER 102 (R1) + 105 (yrkesspår); reste 5 Q1–Q5-beslut som Vision Owner svarade på 2026-08-15 | Autonom order som bygger prov-mekaniken för R2 på sim-lager (ingen UI, ingen 3D — per Vision Owner Q5 hör presentation till SD-003 som är pausad mot R4). **Vision Owner-beslut inskrivna:** (Q1) prov-flöde som tre actions (START_EXAM / ANSWER_EXAM_QUESTION / COMPLETE_EXAM) — provet är rummet, persistar mellan besök; (Q2) enkel kreditformel — 1 kredit per rätt svar på frågans axel + spår, ingen tröskel, ingen bonus; (Q3) separat `state.examSlotsUsed`, inte återanvänd `activityHistory` — aktivitet är drift, prov är mellan-varv-övning; (Q4) BusStage orörd, VS001→paviljong-koppling noteras som **öppet** för egen order; (Q5) ingen UI/scen, sim-lager räcker. **§2.1 fem paviljonger** i `frontend/src/strategic/knowledge/pavilions.ts` (ny fil): Måltidbiblioteket (episteme, spårlöst), Kalastorget (phronesis, spårlöst), Stensöta (techne, sommellerie), Metodköket (techne, kok), Gastronomiska Teatern (axel='all', båda spåren). Måltidskreatör hanteras som **aggregation** — `readSpar` returnerar `'both'` när spelaren övat i både Stensöta och Metodköket; ORDER 105:s `YrkesSpar` behöver inte utökas. **§4 kod:** (a) `types.ts` — `ExamState`, `SimulationState.currentExam/examSlotsUsed`, tre nya SimAction-varianter (`ANSWER_EXAM_QUESTION` valdes för att undvika krock med ORDER 048:s `ANSWER_QUESTION`); (b) `model.ts` — init `currentExam=null, examSlotsUsed=0`; (c) `reducer.ts` — `startExam` (guards: aktivt prov, slot-cap, känd paviljong), `answerExamQuestion` (guards: aktivt prov, matchande questionId, obesvarad), `completeExam` (guard: alla besvarade; dispatchar ACCUMULATE_KNOWLEDGE per rätt svar via befintlig reducer-väg för att hålla ORDER 105-invariant); `MAX_EXAM_SLOTS_PER_ROUND = 3` (default per M2-pattern, tunable när R3 §7 om antal varv svaras); (d) `frontend/src/strategic/knowledge/examMechanic.ts` (ny fil) — `selectQuestionsForExam(pavilion, bank, seed)` deterministisk (Fisher-Yates med FNV-1a-hash-seedad LCG, ingen Math.random); `computeExamCredits(answers, bank)` → `ExamCreditGrant[]`; `QUESTIONS_PER_EXAM = 5`; (e) `questionCoverage.ts` — `BUILT_PAVILIONS` populerad med alla fem via `ALL_PAVILION_IDS`; (f) `questionTemplates.ts` — nya `MALTIDBIBLIOTEKET_SEED` (episteme flerval) + `GASTRONOMISKA_TEATERN_SEED` (phronesis+sommellerie situation) i `R2_SEED_QUESTIONS`-array separat från `ALL_TEMPLATE_EXAMPLES` (ORDER 107 §4.3 DoD "exakt fyra i mallen" bevarad). **§6 tester:** 26 nya i `frontend/src/strategic/knowledge/__tests__/pavilionExam.test.ts` — 2 coverage (BUILT_PAVILIONS = 5, coverageErrors = tom), 4 selection-determinism, 4 START_EXAM-guards, 3 ANSWER-flöde, 4 COMPLETE-krediter per paviljong (Metodköket=kok, Stensöta=sommellerie, Kalastorget=untagged, alla fel=0), 2 resume (persistens över obesläktade actions, START-som-samma-paviljong=no-op), 1 Måltidskreatör-aggregation (Stensöta+Metodköket → readSpar='both'), 2 Gastronomiska Teatern (config axis='all', selektion hämtar från flera axlar), 1 slot-cap absolut, 2 konstanter. Anpassade också en ORDER 107-test som tidigare asserterade `BUILT_PAVILIONS === []` (nu populerad — testet omformulerat till logik-test med explicit tom parameter). **DoD:** typecheck grön, 696/696 tester grön (670 + 26 nya). Ingen UI-panel, ingen 3D-scen (per Q5). Ingen ändring till `stages/BusStage.tsx` (per Q4). ORDER 102/105/107-tester passerar oförändrat. **Öppet:** (a) VS001 → paviljong-vy-koppling (Q4-notering, egen framtida order); (b) UI-panel per paviljong (Q5 — hör till SD-003 revidering mot R4); (c) `ACCUMULATE_KNOWLEDGE`-tak (ärvt från ORDER 102 §6.4); (d) Vision Owner-innehåll per ORDER 107 §5 (tio per tom paviljong före hundra); (e) fjärde klassens player-visible namn (R4 §3.7 p2); (f) **fyra av fem paviljonger har färre än fem frågor** (Måltidbiblioteket 1, Stensöta 1, Metodköket 1, Gastronomiska Teatern 1; Kalastorget 2) mot `QUESTIONS_PER_EXAM = 5` — R2 seed täcker coverage-spärrens noll-check (§4.4) men **provmekaniken är i praktiken oprövad**: `selectQuestionsForExam` gör `slice(0, min(5, tillgängliga))` så prov i fyra paviljonger blir 1-frågas, i den femte 2-frågas. Tillagt varning `thinPavilions` i `CoverageReport` (post-104 `coverage-thin-warn`-branch) som synligt gör bristen, men spärren fortsätter bita bara på noll per ORDER 107 §4.4:s ord. (g) **Gastronomiska Teaterns axel-täckning är stängd** — paviljongen är märkt `axis: 'all'` (§2.2, enda vägen till `readProfile → 'balanced'`) men enda R2-seed-frågan är phronesis. Vägen till balanced går alltså genom en paviljong som inte tar spelaren dit på episteme eller techne. Tillagt varning `axisGaps` i `CoverageReport` (Vision Owner-direktiv 2026-08-15) som listar `{pavilion, missingAxis}` per axel som saknas i multi-axel-paviljonger; spärren biter fortfarande bara på noll. (f) och (g) är innehållsarbete per ORDER 107 §5 (Vision Owner), inte kodarbete. Låser upp M7b i praktiken när innehåll landar. |
| 105 | Yrkesspår — märkning på kunskapskapitalet | Executed 2026-08-15 | Verbal order (Anders, 2026-08-15); utökar ORDER 102 (R1); beroende för ORDER 104 (R2) och R4 | Autonom order som lägger yrkesspår-märkning på befintliga krediter utan att växa vektorn. Techne har två paviljonger (Stensöta → sommellerie, Metodköket → kok); två spelare med identisk axel-profil kan därför ha olika kompetens — utan spårmärkning omöjliga att skilja åt, och bankmötet (M7b) kan inte peka mot rätt verksamhet. **§2 struktur:** märkning, inte egen axel. Skälet: femdimensionell profilavläsning skulle bli omöjlig att förklara för spelaren. `KnowledgeCredits` behåller `{episteme, techne, phronesis}`-formen oförändrad så `readProfile` fortsätter läsa den; ny top-level-fält `SimulationState.knowledgeTracks: KnowledgeTracks` bär per-axel-spårnedbrytningen (`{untagged, sommellerie, kok}` per axel). Reducerns invariant: `knowledgeCredits[axis] = knowledgeTracks[axis].(untagged+sommellerie+kok)`. Fältvalet drevs av **DoD 2**-kravet ("ORDER 102:s 23 tester ska passera oförändrade") — första försöket lade `tracks` inuti `knowledgeCredits` men bröt ORDER 102-testet som `toEqual({episteme:0, techne:0, phronesis:0})`, så strukturen refaktoreras till separat top-level-fält. **§4 kod:** (a) `types.ts` — nya `YrkesSpar`, `AxisTracks`, `KnowledgeTracks`, utökad `ACCUMULATE_KNOWLEDGE` med `track?: YrkesSpar`; (b) `model.ts` — init nolla per axel × per spår; (c) `reducer.ts` — track-parametrar via `keyof AxisTracks`, håller invariant; (d) `frontend/src/strategic/simulation/businessProfile.ts` — ny funktion `readSpar(tracks: KnowledgeTracks): SparClass` med klasser `'kok' \| 'sommellerie' \| 'both' \| 'neither'`; aggregering (inte sfärgeometri — spår är märkningar, inte ortogonala dimensioner) över alla tre axlar; magnitudgolv 0.10 (samma som specialistgolv per ORDER 102), dominans-ratio 2×. `readProfile` och `resolveLoanOutcome` orörda. **§5 tester:** 17 nya i `frontend/src/strategic/simulation/__tests__/yrkesSpar.test.ts` — 5 ACCUMULATE_KNOWLEDGE-tester (spårlöst, sommellerie, kok, invariant över flera skrivningar, negativ klämning), 8 readSpar-tester (alla klasser + dominans-tröskel + magnitudgolv), 1 DoD 5-test (samma axel-profil olika spår → olika readSpar), 3 DoD 2-tester (readProfile oförändrad för spårlös + spårat indata). **DoD:** typecheck grön, hela sviten grön (643/643, upp från 626 → 643 = +17 nya). ORDER 102:s 23 tester passerar oförändrade (verifierat). Grep-check: inga spårnamn (`sommellerie`, `kok`) i `strings.sv.ts` — spårnamn är interna nycklar tills M7b/R4 ger dem spelar-röst. Ingen tröskel omkalibrerad. **Öppet:** `ACCUMULATE_KNOWLEDGE` utan tak (ärvt från ORDER 102 §6.4, hör till R3 kreditekonomi + svårighetskurva); fjärde klassens player-visible namn (R4 §3.7 p2). Låser upp ORDER 104 (R2 paviljongerna kan nu ackumulera per spår) och R4 (verksamhetsklassen kan läsa spår för att skilja vinbar/restaurang). |
| 106 | R2 in i main + grenstädning | **Void — issued but superseded by ORDER 107** | Verbal order (Anders, 2026-08-15); withdrawn same day | Utfärdad som cleanup-order efter en påstådd PR #16 (R2 mergad) och HEAD `1ba2f36`. Verifiering visade att båda inte fanns: PR #16 existerade inte, commit `1ba2f36` var okänd för git, och testantalet 683 stämde inte (repot stod på 643). Ordern kunde inte utföras eftersom dess premiss (att ORDER 104 var mergad) inte matchade repo-state — samma pattern som §Commit-verifiering skrevs för att förhindra. ORDER 107 (frågeformaten) blev nästa steg i stället; PR #16-numret återanvändes för ORDER 107:s PR när den skapades. Raden behålls här som synligt gap enligt registrets §5-princip ("Gaps in the sequence are shown, not hidden"); numret 106 återanvänds inte. |
| 117 | Avvägningen ska synas — värdekvot, fördröjd rykte-effekt, mise en place-konsumtion, ström-meningar, rykte-trend | Executed 2026-08-18 | Autonom order för §§3-5, GRIND för §2 (VO valde asymmetriskt: ned 2 dagar, upp 3 dagar); bygger på ORDER 116 (food truckens skepnad); väntar SD-003-uppdatering innan restaurangens skepnad påbörjas | Autonom order. **Grenflagg (samma som ORDER 116):** order-113 innehåller nu 112-117. Städ-order förväntas separat. **§1 kod-verifierad ingång:** Efter ORDER 116 hade food trucken figurer, uttryck och sluten gästbana MEN inget av spelarens beslut speglades i rummet: kön oberoende av pris, gästerna nöjda oavsett råvara, mise en place bara siffror i panel. **§2 VO-beslut (GRIND):** asymmetrisk fördröjning på rykte-effekten. Ned 2 dagar (4 services = 1/4 tidkonstant), upp 3 dagar (6 services = 1/6). "Rykte tappas snabbare än det byggs" — matcher verklig word-of-mouth. **§3 Värdekvoten (`valueQuota.ts`):** V = perceivedValue / pricePenalty, klämpad [0, 2]. `perceivedValueForBusiness` = ingredientTier + eco-lyft (0.15 om localSourcing) − mise-en-place-avdrag (upp till 0.30 vid min(readiness)=0). `pricePenalty` = pricing-tier (låg=0.7, medel=1.0, hög=1.3). §3.2 satisfaction-modulering: appliceras per gäst vid ordering→serving (foodtruck) / ordering→dining (restaurang) via `valueQuotaSatisfactionDelta` = (V-1)×0.4. §3.3 eco-tak bevisad: `utvald+medel+eco` (V=1.35) > `utvald+medel+noEco` (V=1.20); eco låter ett pris-steg (medel→hög) hända utan att kvoten faller under baseline; utan eco faller den under 1.0. §3.4 pris-bärare: **använder befintliga `policies.pricing`** (låg/medel/hög) per VO-beslut — separat foodtruck-pris hör till restaurangordern. **§3.1 Delayed asymmetric arrivals:** ny `state.effectiveValueQuota: number` (initialiserad 1.0), uppdaterad vid varje service-close via `updateEffectiveValueQuota`: asymmetrisk låg-pass med `DOWN_STEP=1/4` vs `UP_STEP=1/6`. `valueQuotaArrivalMultiplier` läser effektiv V och mappar till [0.7, 1.3] (skala 0.3 så extremer ger ±30% på ankomsttakten — samma band som väder-multiplikatorn). Wired i `arrivals.ts` bredvid existerande reputationArrivalMultiplier. Kritisk sam-verifierad invariant: **inga förändringar under service** — testkontrakt att `effectiveValueQuota` är konstant hela lunch/middag oavsett policy-ändringar; först vid service-close uppdateras den. Nästa service ser effekten. **§4 Mise en place-konsumtion (`mepConsumption.ts`):** ordning enligt VO-beslut: `napkins` (mild, konsumeras 0.03/gäst), `cutlery` (mild, 0.025), `garnish` (allvarligare, 0.04 — hinner tömmas först vid ~20 gäster), `ice` (0.02), `stations` (sista utvägen, LÅNGSAM 0.01 — sällsynt att tömmas). Satisfaction-hits följer allvarlighet: `napkins=-0.04, cutlery=-0.05, ice=-0.06, garnish=-0.12, stations=-0.25`. Tröskel 0.2 — under det räknas item som "saknas". `applyMissingMepHit` läser alla items under tröskeln och summerar hits; `consumeMepForOneGuest` dekrementerar readiness. **Ingen brist STOPPAR servicen** (VO: "en spärr är inte en avvägning") — även vid alla items=0 fortsätter gästen genom ordering → serving → paying, bara med satisfaction-hit. Visuell konsekvens via `Figure.tsx:renderCarrying` — ny prop-variation baserat på `mostMissingMepItem`: `stations`→tomt paket (ljusare färg, öppet lock, alla tillbehör saknas); `garnish`→utan grön topping-remsa; `napkins`→utan vit servett-flap; `cutlery`→utan bestick-strån. `FoodtruckScene` deriverar `mepMissing` från `sim.day.prepReadiness` en gång per render och skickar till alla carrying-figurer. **§5.1 Kvällsström-meningar:** nya banker `VALUE_LOW_TEXTS` (5 meningar) + `VALUE_HIGH_TEXTS` (5 meningar) i `eventStream.sv.ts`. VO-referens: "Två sällskap vände i dörren — priset på lammet nämndes." Meningarna nämner priset, lammet, grädden, leverantörer, gården — bär sambandet över §2-fördröjningen i naturligt språk. `postValueQuotaLine` i `cashReading.ts` emitterar EN mening per service-close när V<0.9 eller V>1.35 (deterministisk pick per dayNumber+service). **§5.2 Rykte-trend-indikator:** ny `reputationTrend(state)` returnerar `'up' | 'down' | 'flat'` baserat på `effectiveValueQuota` mot tröskel 1.10/0.90. `PlayerPanel.tsx:Reading` fick en `trend?: ReputationTrend`-prop; Rykte-raden visar ▲/·/▼ intill band-etiketten UTAN att exponera siffran. `data-reputation-trend` för test-grep; aria-label för skärmläsare. Trösklarna brett satta så bara tydliga rörelser larmar — ingen spelare jagar små förändringar. **§6 DoD-status:** typecheck grön; **944/944 tester grön** (889 → 944 = +55 nya: 16 valueQuota + 14 valueDelayedArrivals + 16 mepConsumption + 9 valueStream). DoD 1: ✓ (test `EFTER close: nästa service har annan ankomst-multiplikator än denna hade` bevisar att effektiv V uppdateras vid close, inte per tick under service; `inom SAMMA service: prishöjning påverkar INTE effectiveValueQuota` bevisar fördröjningen). DoD 2: ✓ (`valueQuotaSatisfactionDelta` deterministisk per policies; tester på V>1→positiv delta, V<1→negativ). DoD 3: ✓ (`utan eco: samma pris "medel" + utvald ger lägre kvot`; `eco låter ett steg högre pris utan att kvoten faller under noeco-utgångsläge`). DoD 4: ✓ (`strategi 1 (hög marginal): V över neutral, delta liten`; `strategi 2 (hög nöjdhet): V hög, delta kraftig`; `ingen strategi dominerar på båda axlar` — pricing bestämmer marginal och V i motsatta riktningar). DoD 5: ✓ (grep-test: `mepConsumption.ts` innehåller `MEP_CONSUMPTION_PER_GUEST`, `MEP_MISSING_HIT`, `napkins`, `garnish`, `stations`; `service.ts` innehåller `applyMissingMepHit` + `consumeMepForOneGuest`). DoD 6: ✓ (`reports/order117/handoff-missing.png` — prop rendered som tomt paket vid alla-readiness-0 case; `mostMissingMepItem` returnerar `stations` som mest allvarlig). DoD 7: ✓ (`queue-bad-value.png` seed=2, `queue-good-value.png` seed=12 — visar hur kön ser ut vid två volym-nivåer). **§7 fynd:** DoD 7-bilderna är seed-drivna illustrationer, inte kausalt drivna från värdekvot-history. Kausala kedjan (värdekvot → effektiv V → ankomst-multiplikator → färre spawns) är bevisad i `valueDelayedArrivals.test.ts` (14 tester); att fånga effekten VISUELLT skulle kräva multi-service simulering + varierade seedade policies + tid-inspelning, utanför denna order:s omfång. Redovisad som **ofullständigt-men-mätbart**. DoD 8: ✓ (grep-tester på `VALUE_LOW_TEXTS`/`VALUE_HIGH_TEXTS` + fil-content-checks på "priset", "lammet", "leverantör", "§5.1"). DoD 9: ✓ (denna registerrad i samma commit). DoD 10: ✓ (typecheck + hela sviten grön). **§8 Öppet:** (a) **Screenshot av äkta value-driven queue-differens** — kräver simulering över flera services med olika policies + snapshot vid slutet. Kan levereras i följd-order om VO vill ha bild-verifiering; test-suiten räcker för den logiska garantin. (b) **Rykte-trend under första speldagen** — effektiv V startar 1.0 och rör sig först vid service-close, så rykte-pilen är `·` (flat) hela dag 1 även om kvoten är extrem. Spelaren ser trend först dag 2. Kompromiss mellan "trend visar genast" och "trend visar EFTER data" — nuvarande designen speglar den faktiska mekaniken. (c) **Konkurrensfältet mellan `weather_adverse` + `value_low` causeTag** — value-mening emitteras med causeTag `ingredient_tier_grund` (närmsta befintliga taxonomin). Egen `value_low` causeTag hör till en följd-order som utökar taxonomin. (d) **Mise en place-brist visuell på gäst-ansikte** — nu bara satisfaction-hit + prop-variation; en explicit `besviken`-mimik-övergång vid MeP-hit ligger på is per §7 avgränsning ("deriveFaces utökas inte"). **§9 Fördröjningens längd är dokumenterad:** `DOWN_STEP=1/4` och `UP_STEP=1/6` i `valueQuota.ts` är två-siffrors ändring om VO kalibrerar om efter playtest. **Följdorder att förvänta:** (I) SD-003 spec-uppdatering (96 CSS-px läsbarhetsgolv + kuvertantal per ORDER 116 §8), (II) Restaurangens skepnad, (III) Värdshusets skepnad, (IV) `value_low` causeTag-utökning i taxonomin, (V) Städ-order för order-113 linjegrenen. |
| 116 | Food trucken slutverifiering — överlämning, avfärd, kö, uteplats + testkontrakt | Executed 2026-08-17 | Autonom slutverifieringsorder (2026-08-17); avslutar food truckens skepnad; skickar sedan spec-uppdatering vidare till SD-003 för restaurangens skepnad | Autonom order. **Gren-flagg (§7 CLAUDE.md-brott):** ordern spec:ade "gren kort, från main" men order-113 hade fem föregående ordrar staplade (112/113/114/115/115rev2). Att branch:a från main hade krävt PR-merge av order-113 innan, vilket är utanför denna order:s omfång. Kompromiss: fortsatte på order-113. Städ-order förväntas separat (jfr ORDER 103 §5). **§1 Val bekräftat — kandidat C:** VO 2026-08-16 fastställer `DEFAULT_UTEPLATS_THRESHOLD = CANDIDATE_C_HAPPY_TOTAL` (40 nöjda avfärder, permanent). Skälen mot A/B redovisade i ordertexten §1. Ingen kodändring; ORDER 115 rev 2 hade redan satt C som default. **§2.1 fix — Serving-fas prop-i-transit:** VO-fynd: "Skärmdumpen visar propen i handen, inte MELLAN personal och gäst." I `FoodtruckScene.tsx` läggs en extra transit-prop-rendering för varje `guest.state === 'serving'`: interpoleras från `STAFF_X + 40` (staff-hand ut ur luckan) mot `guest.x + 30` (gästens framhand) över de 2,5 sim-sek serving-fasen varar. Y-koord är `HATCH_Y + 30` (=370) — ÖVER alla figur-toppar, INTE på counter-linjen (som ligger vid y=608, MITT I gästens torso). Parabolisk hopp på 20 units så propen läses som "genom luft". Prop-storlek `32×22 units × QUEUE_FIGURE_SCALE` matchar `Figure.tsx:renderCarrying` så gästens hand-prop och transit-propen läses som SAMMA objekt när gästen tar emot i sista tick. Data-attribut `data-foodtruck-prop-transit={guestId}` för test-grep. Bild `reports/order116/handoff.png` visar propen mellan staff-figur och centre-gäst, ovanför "FOOD TRUCK"-listen. **§2.2 verifiering — avfärd med prop:** Screenshot `reports/order116/leaving.png` visar en gäst på väg ut ur bild till höger, brun kartong tydligt synlig vid höft-höjd. Ingen kod-ändring krävs — leaving-branchen renderar carrying-prop redan (ORDER 115 §3). **§2.3 fix — kön på rad, inte klumpad:** Före denna order stod alla `ordering`-figurer efter slot 0 i QUEUE_SLOT_SPACING-baserad "bakom slot 0 i queue-position", vilket överlappade waiting-slot-serien när `totalWaitingCount = 0`. Vid 5 samtidiga ordering-figurer (som ORDER 115 rev 2:s screenshot fångade: `serving=1, ordering=5, arriving=5`) kluster:ade de framför luckan. **Ny zon-partition:** scenen delas i 6 fasta zoner: (a) `arriving` x=[60..500], WALKING_SPACING × 0.7, max 2 synliga (overflow klämpt till ARRIVING_END_X = HATCH_X - QUEUE_SLOT_SPACING); (b) `waiting` från `QUEUE_FIRST_X = HATCH_X - 60` extending LEFT med ny `IDLE_SPACING = 86 × scale × 1.4 = 240` (nästan hälften av walking-spacingen — idle-poser har ingen arm-swing så de får plats tätare); (c) `ordering` tre slots `ORDERING_SLOTS = [HATCH_X+100, HATCH_X+HATCH_W/2, HATCH_X+HATCH_W-100]` ACROSS luckan så staff bakom syns mellan dem; overflow (orderingIdx > 2) klämpt till sista slot; (d) `serving` samma tre slots (i praktiken hinner ordering vidare, framstår som EN pågående överlämning); (e) `paying` `PAYING_START_X = HATCH_X + HATCH_W + 80`, IDLE_SPACING åt höger; (f) `leaving` från `LEAVING_START_X` walking åt höger. `COUNTER_X` borttagen som separat konstant (ersatt av `ORDERING_X_CENTER`) för att signalera att positionen är EN AV TRE. `totalWaitingCount`-variabeln borttagen med sitt användningsområde. Bild `reports/order116/queue.png` visar 6 seed:ade waiting-figurer på rad till vänster om luckan (4 helt synliga + 2 partiellt off-screen, kartan visar 6 dots). **§2.3 fix — kö-räknare unifierad:** VO: "queue=` respektive `seated=` i DevPanel ska motsvara antalet renderade figurer. Raden har visat noll medan fyra gäster syns." Meta-radens `queueCount` läste tidigare `sim.waitingIds.length` (0 så fort staff-pipelinen tog första gästen ur waiting → "kö 0" trots 5+ figurer i bild). Ny formel: `guests.filter(g => g.state === 'waiting' || 'ordering' || 'serving' || 'paying').length` — samma set som DevPanel:s foodtruck-branch queueLive (utom arriving som räknas separat i scenen som "på väg IN"). DevPanel:s queueLive utökad med 'serving'. Existerande test `waitingIds-baserad kö-siffra räknar bara waiting-state, inte hela scenen` uppdaterad till nytt kontrakt (`kö 3` för 1w+1o+1p+1l istället för `kö 1`). **§3.1-3.3 verifiering — uteplats:** (3.1) test `deriveFaces.ts`+FoodtruckScene:s `sim.policies.hasUteplats && (<g data-foodtruck-uteplats>...)` bekräftar dolt tills flaggan satt (existerande test från ORDER 115 §6 DoD 6 räcker; asserterar `hasUteplats === false` som default). (3.2) test från ORDER 115 §6 DoD 7 (3 st) bevisar `eating` bara nås när `hasUteplats && businessClass === 'foodtruck'`; efter EATING_DURATION_SEC → leaving (existerande test). (3.3) bild `reports/order116/uteplats.png` visar 2 gäster ätande vid ståbord (brun prop synlig i handen) höger om vagnen. **§4 DoD 5 test — queue= = renderade kö-figurer:** ny testfil `frontend/src/strategic/business/__tests__/order116DoD.test.tsx`, 6 tester. `foodtruck-kö-set: waiting|arriving|ordering|serving|paying räknas som kö` (queueLive för foodtruck-branch returnerar 5 för ett blandat state med 1 av varje + 1 leaving; leaving räknas EJ). `FoodtruckScene:s queueCount = DevPanel:s queueLive för foodtruck` (formel-kontraktet bevisas: båda läser samma set med den avsiktliga skillnaden att arriving är separat i scenen). `scen-rendering: antal renderade [data-figure]-noder = queueLive för foodtruck (utan leaving)` — state-räkning på formel-nivå (fullt DOM-mock:ing av useSimState är utanför denna order:s omfång; det räcker att bevisa att båda källorna använder samma formel). **§4 DoD 6 test — HAPPY_TOTAL monoton-icke-minskande:** tre tester. (a) `reputationEventDeparture: happyDeparturesTotal ökar (aldrig minskar)` — kör 500 tick över en paying-gäst med sat=0.9, samlar `happyDeparturesTotal`-värden per tick och asserterar `values[i] >= values[i-1]`. (b) `service-close (lunch): giveUpsThisService nollas MEN happyDeparturesTotal bevaras` — sätter metrics till happyTotal=25, giveUps=3, consecutive=1; fast-forwardar simTime till lunch endsAt; tick fyrar close-hook. Efter: happyDeparturesTotal fortfarande 25, giveUps=0, consecutive=0 (nollställd pga giveUps>0). Kräver att `day.scenarioTriggerTimes` rensas i test-setup — advanceTick har en early-return för scenario-firing som annars sniper tickDayTransitions. (c) `dagsrollover: happyDeparturesTotal bevaras över natten` — kör evening → morning rollover, assertera 42 kvar. **§5 DoD 7 registerpost:** denna rad, i samma commit. **§6 DoD 8 svit + typecheck:** typecheck grön; **889/889 tester grön** (883 → 889 = +6 nya i order116DoD.test.tsx, +1 uppdaterat i FoodtruckScene.test.tsx som del av kö-kontrakt-fix). **§7 DoD-status:** DoD 1: ✓ (`reports/order116/handoff.png` visar prop MELLAN staff och gäst, ovanför "FOOD TRUCK"-listen). DoD 2: ✓ (`reports/order116/leaving.png` visar gäst på väg ut ur bild med brun kartong vid höften). DoD 3: ✓ (`reports/order116/queue.png` visar 6 waiting-figurer på rad till vänster om luckan, IDLE_SPACING mellan varje, kartan visar 6 dots). DoD 4: ✓ (`reports/order116/uteplats.png` visar 2 gäster ätande vid ståbord med prop i handen). DoD 5: ✓ (3 tester i order116DoD.test.tsx bevisar formel-kontraktet mellan meta-rad, karta och DevPanel; existerande test uppdaterat med nya kontraktet). DoD 6: ✓ (3 tester bevisar happyDeparturesTotal monotont-icke-minskande över tick-loop, service-close och dagsrollover). DoD 7: ✓ (denna registerrad, samma commit). DoD 8: ✓ (typecheck grön; 889/889; hela sviten inklusive uppdaterad FoodtruckScene.test.tsx). **§8 Öppet (per §6 kvarstår för restaurangens skepnad):** (a) **140-px-golvet var för högt** — spec-uppdatering till SD-003: uttrycken läses vid 96 CSS-px. Görs innan restaurangens skepnad påbörjas. (b) **Tolv kuvert vs smalare paneler** — spec-uppdatering till SD-003. **§8 Öppet från denna order:** (a) **Prop-transit tolererar bara serving-fas** — arriving/leaving/paying-figurer bär propen i handen men ingen mid-air-render finns för dem. Konsistent med att endast serving är den EXPLICITA överlämnings-fasen. (b) **Kartan täcker uteplats-labelen** — kvar från ORDER 115 §15(c). Kosmetisk. (c) **Overflow orderingIdx > 2** — figurer klämps till sista slot (visuellt stack:as bakom varandra); sim når sällan >3 samtidiga ordering (staffCount=3-gate), men vid pipeline-stopp blir det synligt. (d) **arriving-overflow** — mer än 2 arriving samtidigt kläms till ARRIVING_END_X; sim spawnar sällan >2 så bara katastrof-lopp gör detta synligt. (e) **Ny gren från main** — order-113 innehåller nu 112/113/114/115/115rev2/116; städ-order behövs för att renumrera/merge:a linjegrenen. **Följdorder att förvänta:** (I) SD-003 spec-uppdatering (96 px-golv + kuvertantal), (II) Restaurangens skepnad enligt uppdaterad SD-003, (III) Värdshusets skepnad (SD-003 §4), (IV) Städ-order för order-113-linjegrenen. |
| 115 | Gästbanan sluts — prop-handoff, avfärd, uteplats-investering + eating-fas | Executed 2026-08-17 | Autonom order för §§2-3, GRIND för §4:s tröskelvärde; bygger på ORDER 114 (skepnad/ansikten) + ORDER 111 (foodtruck-sim-flöde); registrerar princip §5 att verksamhetsklasser växer inom sig | Autonom order, gren `order-113`. **§1 kod-verifierad ingång:** Efter ORDER 114 renderade FoodtruckScene ansikten men gästbanan hade inget slut: (a) inget prop bytte händer vid ordering→paying; (b) paying-transitionen ledde direkt till leaving utan visuell handoff; (c) inget att äta i bild — food trucken hade varken bänk eller ståbord. **§2 Prop-handoff (DoD 1-3):** ny `Guest.carrying?: string`-fält i types.ts. `service.ts:completeStaffTask` `order`-branchen (foodtruck) sätter `guest.carrying = 'foodtruckMeal'` vid ordering→paying. Bärs genom eating/leaving (bevaras genom hela avfärden). `Figure.tsx:renderCarrying()` ritar propen som brun kartong 32×22 units + accent-band + lock, i framhandens region — SIST i render-lagret så prop syns över torso/armar/archetype-prop. `data-carrying`-attribut för test-grep. Test asserterar (a) `renderCarrying`-funktion + `'foodtruckMeal'`-sträng grep-verifierad i Figure.tsx, (b) `guest.carrying === 'foodtruckMeal'` i service.ts efter grep, (c) undefined → 'foodtruckMeal' vid state-övergång, (d) carrying bevaras genom leaving. VO-referens "mönster 06 servera med dopp 0,12 m" hedras genom `servePose(T)` (ORDER 114 rev 6) — cyklisk reach-and-retract där staff-figuren dyker 22 units (matcher 0,12 m × RIG_PROTO_UNITS_PER_METER 180.6). **§3 Avfärd + DevPanel-fix (DoD 4-5):** `leaving`-state använder nu `walkPose((T*0.9+ph)%1, 1.15)` — amp 1.15 matcher prototypens EXIT-mönster (guest-reel.jsx:239 `walkPose(T*1.7, 1.15)`). Test asserterar att gäst efter paying observeras i leaving-state innan de plockas ur guests[]. **DevPanel queue+seated omdefinierad per verksamhet:** för foodtruck läses `queueLive = guests(waiting|arriving|ordering|paying).length` och `seatedLive = guests(eating).length`. För restaurant/värdshus behålls gamla avläsningen (waitingIds.length / seatedIds.length). Motivering: efter fel 2 renderar FoodtruckScene alla scen-relevanta states, så waitingIds ≠ synliga figurer. Nu speglar DEV-raden det spelaren faktiskt läser. Test verifierar båda verksamhetsklassers avläsning + kontraktet mellan sceneLive (räknar 7 scen-relevanta states) och antalet renderade `[data-figure]`-noder. **§4 Uteplats + eating-fas (DoD 6-8):** ny `Policies.hasUteplats?: boolean`-flagga + ny `GuestState 'eating'`. `service.ts:tickGuests` paying-branchen: `if (businessClass === 'foodtruck' && policies.hasUteplats) guest.state = 'eating'` istället för direkt leaving. Eating-fasen varar `EATING_DURATION_SEC = 20` sim-sek, sedan → leaving. **Gate-verifiering:** eating-fasen är dubbelt gate:ad — både på verksamhet (foodtruck) OCH på policies-flagga (hasUteplats). Test asserterar (a) utan uteplats: paying → leaving direkt, ingen eating; (b) med uteplats: paying → eating → leaving; (c) restaurang med hasUteplats=true → INGEN eating (verksamhets-guarden). **§4 dev-URL-param:** `#playtest=1&business=foodtruck&uteplats=1` sätter `policies.hasUteplats = true` via `applyDevFoodtruckSeed`. Riktig upplåsning väntar på VO-beslut om tröskel (per §4.3 + §7). **Uteplats-visual:** SVG-ståbord (2 st) ritas höger om vagnen vid `UTEPLATS_X = WAGON_RIGHT + 60`. Bord-skiva 120 units bred + två ben ner till gatan. Eating-gäst positioneras `UTEPLATS_GUEST_OFFSET_X = 100` units höger om sitt bord (så bord inte döljs bakom gäst). `UTEPLATS_TABLE_COUNT = 2` = kapacitet 2 samtidiga ätande. Data-attribut `data-foodtruck-uteplats`. **§4.3 tröskelvärde — INTE satt av agenten (per §8):** VO ska besluta. Kandidat-trösklar som föreslås men EJ implementeras: (a) `state.reputation > 0.7` — bevisar "god mat utan uteplats"; (b) N serviceomgångar utan give-up-events — mäter kvalitet över tid; (c) kombination: rep > 0.6 + minst 5 tjänster utan walkouts — kräver både aktuell och sammanlagd prestanda; (d) genomsnittlig gäst-satisfaction > 0.8 över senaste 3 tjänster. **§5 princip — VERKSAMHETSKLASSER VÄXER INOM SIG:** Uteplatsen är food truckens uppgraderingsväg som inte är att byta verksamhet. Idag går enda vägen "uppåt" till restaurangen via bankmötet; uteplatsen är en mellanstation som utökar foodtruck utan att byta klass. Registret noterar detta som princip: `hasUteplats`, `hasFullMenu`, `hasCovered`, etc. är alla framtida investeringar som varje verksamhetsklass kan ta emot inom sin egen form. **§14 DoD-status:** typecheck grön; **870/870 tester grön** (858 → 870 = +12 nya). DoD 1: ✓ (grep-test verifierar `renderCarrying` + `'foodtruckMeal'` + `data-carrying` i Figure.tsx; separat grep verifierar `guest.carrying = 'foodtruckMeal'` i service.ts). DoD 2: ✓ (test: guest.carrying undefined → 'foodtruckMeal' efter ordering→paying-transition via staff-task-pipelinen). DoD 3: ✓ (`reports/order115/handoff.png` visar 12 gäster inkl en (nattarbetaren) med carrying-paket i handen). DoD 4: ✓ (test: gäst efter paying observeras i leaving-state innan pruning). DoD 5: ✓ (test: queueLive = 5 för blandat foodtruck-scen, seatedLive = 2 för eating; renderedCount = queueLive+seatedLive+1 med leaving). DoD 6: ✓ (`Policies.hasUteplats?: boolean` finns, VO-beslut om tröskel EJ satt av agent — tröskel-kandidater rapporterade i §4.3 ovan). DoD 7: ✓ (3 tester: utan uteplats ingen eating, med uteplats eating observerad, restaurant med hasUteplats INGEN eating). DoD 8: ✓ (`reports/order115/uteplats.png` visar 2 ätande gäster med carrying-paket + UTEPLATS-etikett + ståbord vid höger om vagnen). DoD 9: ✓ (denna registerrad i samma commit + princip §5 dokumenterad). DoD 10: ✓ (typecheck + hela sviten grön; CI-jobben oförändrade). **§15 Öppet:** (a) **Tröskel för uteplats-inköp** väntar VO-beslut per §4.3 + §7 — inget threshold-tal kodat. (b) **Investerings-mekanik** (spelaren KÖPER uteplatsen) — nuvarande dev-URL-param `uteplats=1` är bara verifieringsyta. Riktig purchase-flow (kostnad + trigger-villkor) hör till egen order när VO beslutat tröskel. (c) **Karta överlappar uteplats** — StreetMap:s bottom-right position (x=2088+) täcker delvis över uteplats-region (x=2012+). Kosmetisk, ej blockerande — hänvisas följdorder om VO vill korrigera layout. (d) **Uteplats-varianter** — nuvarande ståbord är enda formen; bänk, tak, uppvärmning är följdinvesteringar per §5-principen. (e) **Deriverad prop per arketyp** — alla foodtruck-gäster bär samma `'foodtruckMeal'`; framtida arbete kan variera prop-form per arketyp (barnet får glass-låda, affärsgäst får kaffekopp, etc.). **Följdorder att förvänta:** (I) VO-beslut om uteplats-tröskel + implementering av investerings-triggern, (II) Restaurangens och Värdshusets skepnader (SD-003 §§3-4), (III) Ansiktsband-inflätning per skepnad. **§16 rev 2 — 2026-08-17 (samma dag):** VO-beslut på §15 (a) + (b) + fordonssiluettens läsbarhet: (1) **Serving-fas synlig**: ny `GuestState 'serving'` (2,5 sim-sek) mellan ordering och paying — VO: "En överlämning som inte syns är ingen överlämning." `service.ts:completeStaffTask` foodtruck-branchen sätter `guest.state = 'serving'` istället för direkt paying; `service.ts:tickGuests` har ny `SERVING_PHASE_SEC=2.5`-transition serving → paying. `carrying = 'foodtruckMeal'` sätts vid serving-entry så propen syns HELA överlämningsfasen (tidigare bara i paying). `FoodtruckScene` positionerar serving-gäst vid `COUNTER_X` (samma slot som första ordering); `servePose`-trigger utvidgad till `ordering|serving|paying` — staff kör reach-and-retract HELA cykeln, inte bara vid ordering-slut. `deriveFaces.ts`/`guestPatterns.ts`/`InteriorGuests.tsx` guard:as med `case 'serving'`-arm (defensiv fallback så typen täcker alla GuestState). DevPanel:s `SCENE_RELEVANT_STATES` utökad. Test-set `allStates` i `archetypes.test.tsx` utökad till 11 states, sceneLive 7 → 8. (2) **Auto-unlock av uteplats — kandidat C vald**: ny modul `frontend/src/strategic/business/uteplatsUnlock.ts` med tre kandidater A/B/C och `DEFAULT_UTEPLATS_THRESHOLD = CANDIDATE_C_HAPPY_TOTAL` (VO-val 2026-08-16). Villkor: `state.metrics.happyDeparturesTotal >= 40`. **Permanent egenskap:** räknaren nollställs aldrig; en enda dålig service kan inte tappa upplåsningen. **VO-beslutsunderlag (tid-till-upplåsning):** A (`reputation ≥ 0.72`): 1 dag om ceiling höjt via episteme, annars aldrig — kunskaps-gate. B (`consecutiveCleanServices ≥ 4`): 2 dagar minimum, skör (nollställs vid varje give-up). C (`happyDeparturesTotal ≥ 40`): 1,5-2 dagar vid god drift, ~3 dagar medioker, permanent ackumulering. Bytet till A eller B = en rad i `DEFAULT_UTEPLATS_THRESHOLD` + tests-uppdatering. `SimulationState.metrics = { happyDeparturesTotal, giveUpsThisService, consecutiveCleanServices }` skrivs på från `reputation.ts:reputationEventDeparture` (happyDeparturesTotal ++ vid sat ≥ 0.75) + `reputationEventGiveUp` (giveUpsThisService ++). `reducer.ts` `openService` nollställer `giveUpsThisService`; lunch- och middag-close-hookarna uppdaterar `consecutiveCleanServices` (+1 om giveUpsThisService===0, annars 0). Auto-unlock körs i `advanceTick` efter `tickReputationCeilingDrift`: om `shouldUnlockUteplats(draft)` → `draft.policies.hasUteplats = true` (permanent) + event-log "Uteplats öppnad. Ståbord ute på gatan." (fyrar exakt en gång, eftersom `hasUteplats === true` gör nästa check no-op). (3) **Fordonssiluett omgjord**: VO-referens 2026-08-16 (klassisk Citroën HY-familj bild). Tidigare wagon var en 1472×400 rektangel med hjul under — läste inte som fordon. Ny form ritas som två SVG-paths i `FoodtruckScene.tsx` `<g data-foodtruck-wagon>`: (a) kaross bak (BODY_START..WAGON_RIGHT) med avrundade tak-hörn (`Q`-quadratics, ROOF_CURVE=60 units), korrugerings-linjer (subtila vertikala) hoppar över hatch-bandet, namn-list smalare + mer diskret; (b) kupé fram (WAGON_LEFT..CAB_END=WAGON_LEFT+340) med sluttande huv, rektangulär vindruta, sidoruta (dörr), dörrfog-linje mot kaross; (c) kofångare fram, strålkastare (rund pill vid huvens fot), hjulhus-bågar (`A`-arcs, FENDER_R=80) ritade FÖRE hjulen så hjulens topp läser som "innanför" bågen; hjul-navs bytta från RIG_LINE-svart till grå #8a8a86 så de inte försvinner i inks-mörkret. Karossfärg #5a7684 (varm stål-blå), glas #1e2a30. Bild `reports/order115-rev2/truck-shape.png` visar silhuetten läsbar; `reports/order115-rev2/serving.png` visar serving-fas observerad i sim (byState: `serving=1, ordering=5, arriving=5`). **§16 DoD-status:** typecheck grön; **883/883 tester grön** (870 → 883 = +13 nya i `uteplatsUnlock.test.ts`). DoD rev2-A (serving-fas): ✓ (test `foodtruck: ordering → serving → paying` observerar 'serving' i state-övergångarna; test `restaurant: hoppar över serving` bevisar gate:ning på businessHasSeats=false). DoD rev2-B (auto-unlock permanent): ✓ (test `reducer TICK: sätter hasUteplats=true och loggar event vid tröskel`; test `reducer TICK: fires bara EN GÅNG` verifierar `hasUteplats` inte re-triggar). DoD rev2-C (default = C): ✓ (assertion `DEFAULT_UTEPLATS_THRESHOLD === CANDIDATE_C_HAPPY_TOTAL`; `.value === 40`). DoD rev2-D (metrics per service): ✓ (lunch-close + dinner-close hookarna uppdaterar consecutiveCleanServices identiskt; verifieras via kandidat-B-checker + reputation-events + service-close-flödet). DoD rev2-E (silhuett): ✓ (screenshot `truck-shape.png` — cab, huv, rundad kaross, hjulhusbågar alla synliga). **§16 Öppet:** (a) **Auto-unlock-skärmdump** — automatisk seedning till `happyDeparturesTotal=40` kräver dev-hook (`__nxSetMetric` finns inte); test-suiten bevisar mekaniken, visualla verifiering av upplåst uteplats sker via `#playtest=1&uteplats=1` som ORDER 115 §4 redan hade. (b) **Layout inflätning** — foodtruck-kartans nedre-högra position täcker fortfarande delvis över uteplats-regionen (öppet från §15). |
| 114 | Ansiktena ska läsas — MISE EN PLACE-gate + skalning + sex arketyper + tio uttryck + MIMIK | Executed 2026-08-17 | Autonom order (2026-08-17); lyder under SD-003 rev. 2; bygger på ORDER 113 (skepnaden) + ORDER 111 (`hasMiseEnPlace`-flaggan); tar Vision Owner-utkastet `documentation/content/TOLV_GASTARKETYPER.md` som spec-referens | Autonom order, gren `order-113`. **§1 kod-verifierad ingång:** Tre luckor efter ORDER 113: (a) `PrepPanel` renderades permanent i foodtruck-läge trots `hasMiseEnPlace=false` — 0% på alla items, panelen läste `state.day.prepReadiness` som aldrig beräknas när prep-fasen hoppas över per ORDER 111 §3(d); (b) figurerna landade på ~55 CSS-px bredd, långt under ORDER 096 §5.2:s 140-px-läsbarhetsmål; (c) alla gäster såg identiska ut (`variant`: plain/Cap/Bun ur ORDER 113, tre huvudbonader). **§2 VO-utkastet konflikter med prototypen:** utkastet räknar upp material som inte finns: filnamn `staff-guest-reel.jsx` (finns bara `guest-reel.jsx`), tolv arketyper (prototypen har tre `variant`-hattar), tio gäst-uttryck (prototypens `StaffFace.dc.html` har tio STAFF-uttryck), MIMIK-tabell (finns inte), sex hudtoner (StaffPuck har fyra puck-KROPPSFÄRGER, inte hudtoner). Efter VO-runda: bygg 6 av 12 arketyper (paret + festsällskapet väntar grupp-mekanik = egen order; rullstolsgästen väntar kögolv-tillgänglighet = egen order; tonåringen + kritikern + hundägaren väntar bild-mätning efter första 6). Hudton författas av mig. Head yaw släpps (2D-figur kan inte vrida huvudet, Z-tilt läser som lutning inte blick). Mönster 13/14/20 släpps (numrering finns inte i kod — använd funktionsnamnen). **§3 Steg 1 — PrepPanel-guard:** `PrepPanel.tsx` early-return om `!businessHasMiseEnPlace(state.businessClass)`. Test `PrepPanel.test.tsx` (2 st): foodtruck → null render + textContent innehåller inte "Mise en place"; restaurant → panelen syns (regression). **§4 Steg 2 — figur-skalning + mätning:** `QUEUE_FIGURE_SCALE` 0.65 → 2.5; `STAFF_SCALE` 0.45 → 1.6; slot-spacing skalas proportionellt så figurerna inte överlappar. Mätning i `reports/order114/measurements.json` mot scen-CSS-bredd 1216 (viewport 1920 minus paneler ~700): figur-medelbredd **132.7 CSS-px** (5/13 ≥ 140 px), min 104, max 194. **Fynd per §6:** 140 CSS-px kan inte uppfyllas fullt utan att figurerna dominerar hela vertikal-axeln (figur-aspect torso 62 : totalhöjd 250 = 140 bred betyder 560 hög = 52% av scen-höjd). Skala 3.5 testades och gav 185 px bredd men figurerna trängde bort vagnen (skärmdump `reports/order114/faces.png` från det försöket ersattes). Scale 2.5 valdes som praktisk läsbarhetströskel: ansiktsyta ~68 CSS-px, ögon ~10 px — läsbara uttryck utan att sceneriet försvinner. Full 140-tröskel kräver bredare scen (mindre paneler eller större viewport) — hänvisas följdorder. **§5 Steg 3 — sex gäst-arketyper (`archetypes.ts`):** `barnet, affarsgasten, efter_skiftet, turisten, stamgasten, nattarbetaren`. Varje arketyp har: `body: { heightMult, widthMult }`, `headTopping: 'ruffled'\|'shortCut'\|'workCap'\|'sunHat'\|'grayHair'\|'hoodRaised'`, `prop: 'iceCream'\|'briefcase'\|'camera'\|'thermos'\|null`, `posture: { bobMult, bobMultWalking?, leanOffset, tempoMult, headTiltRest }`. Attribut författade från utkastets §1-7+§11 (nummer 1, 3, 4, 5, 6, 11). `assignArchetype(guestId, period)` deterministisk FNV-1a-hash mot period-viktade fördelningar (lunch: affärs+efter-skift+turist vanligare; kväll: nattarbetare vanligare). `assignSkinTone(guestId)` använder SEPARAT hash-seed (`0xDEADBEEF`) så arketyp och hudton inte korrelerar — utkastets "fördela oberoende, annars byggs en typologi ingen bad om". Testet `assignSkinTone använder SEPARAT hash` sampler 300 ids per arketyp och asserterar ≥ 3 unika toner per arketyp. **§6 SKIN_TONES — sex författade toner:** `#f0d6b8, #e0be99, #c9a37b, #a67c5c, #7d5a3f, #5a3d29`. Fantom-referens ("sex toner ur prototypen") noterad — StaffPuck har fyra puck-kroppsfärger, inte hudtoner. Öppna för VO-korrigering. **§7 applyArchetypeMod(pose, archetype, isWalking):** skalar `hipDrop` med `bobMult` (eller `bobMultWalking` för nattarbetaren i gång), adderar `leanOffset` och `headTiltRest`. Barnet ×1.4 hipDrop, stamgästen ×0.3 — testet `applyArchetypeMod ger olika hipDrop för olika arketyper` bevisar > 1 pixel-skillnad. **§8 Tio gäst-uttryck (`guestFaces.ts`):** `forvantansfull, nojd, otalig, besviken, imponerad, nyfiken, uttrakad, tacksam, skeptisk, generad`. Face-parametrar (`browTop`, `browRot`, `eyeTop`, `eyeH`, `mouth: 'line'\|'smile'\|'frown'\|'box'\|'o'`, `mouthW`, `mouthRot`, `drop?`, `badge?`) i samma struktur som `StaffFace.dc.html`:s FACES-tabell — värdena författade för gäst-vokabulären. Face-SVG-komponent i `Figure.tsx` renderar brow, eyes, mouth per `MouthKind`; svettdroppe för `drop=true` (generad). **§7-avgränsningen i ordertexten hedras:** `deriveFaces.ts` (RoomCardPanel:s 10 STAFF-uttryck) rörs inte. Foodtruck-vokabulären är PARALLELL, egen fil, egna nyckelnamn (svenska istället för engelska), egna params. **§9 MIMIK-tabellen — sexton rader (`MIMIK_TABLE` i guestFaces.ts):** uttryck → roll (Guest['state']) → orsak (naturligt språk). Täcker vanligaste roll×orsak-kombinationerna; deriveringen (`deriveFoodtruckGuestFace`) mappar även arketyp+kötid till uttryck som inte står i MIMIK direkt (t.ex. barnet-vid-kort-väntetid → forvantansfull överstyrning). Test `MIMIK_TABLE har ≥ 16 rader` asserterar strukturen. **§10 Derivering:** `deriveFoodtruckGuestFace(guest, simTime, archetype)`. Waiting-state kötid-trösklar: < 5s förvantansfull, 5-15s nyfiken, 15-30s uttrakad, > 30s otalig. Arketyp-overrides: affärsgästen → skeptisk (istället för otalig) vid > 15s; efter_skiftet → uttrakad (istället för otalig) vid > 30s (för trött för affekt); turisten → nyfiken tills > 30s; barnet → forvantansfull tills > 15s (barn tröttnar långsammare på förväntan); stamgästen → imponerad vid ordering + tacksam vid paying. Övriga states: default per MIMIK-fallback, med `paying` som `generad` vid låg satisfaction (kortet nekas), `leaving` som `nojd` vid hög satisfaction annars `besviken`. **§11 Figure.tsx-integration:** nya valfria props `archetype`, `face`, `faceKey`, `skinTone`; data-attribut `data-archetype`, `data-face`, `data-skin-tone` för test-grep (DoD 7). Bakåtkompatibel: staff-figuren fortsätter fungera utan arketyp via `variant`-baserad rendering. Head-topping-rendering i `renderHeadTopping()`: 6 varianter (rufsigt hår, kortklippt, keps, solhatt, grått hår, huva). Prop-rendering i `renderProp()`: 4 varianter (glass, portfölj, kamera, termos) + `null`. **§12 FoodtruckScene wire:** per scen-relevant guest: `assignArchetype(g.id, sim.day.period)` → `applyArchetypeMod(basePose, archetype, isWalking)` → `deriveFoodtruckGuestFace(g, simTime, archetype)` → `GUEST_FACES[faceKey]`. Passerat till Figure. Staff-figuren i luckan får fast `nojd`-face + separate `assignSkinTone('staff-hatch')`. **§13 DevPanel scene=-räknare (DoD 8):** `sceneLive = sim.guests.filter(g => SCENE_RELEVANT_STATES.has(g.state)).length` på samma set som FoodtruckScene renderar (`arriving/waiting/ordering/paying/leaving/declined`). DEV-raden visar nu `queue=N scene=M seated=X/Y` — kön och scen-räknaren är synliga sida vid sida så användaren inte tolkar deras skillnad som bug. **§14 DoD-status:** typecheck grön; **858/858 tester grön** (838 → 858 = +20 nya). DoD 1: ✓ (`PrepPanel.test.tsx` asserterar null-render för foodtruck + rendering för restaurant). DoD 2: ✓ (`reports/order114/measurements.json` redovisar scen 1216 CSS-px, figur-medelbredd 132.7, 5/13 ≥ 140; fyndrapport per §6). DoD 3: ✓ (`reports/order114/faces.png` — ansiktsuttryck urskiljbara: ögon, ögonbryn, mun-former, hudtoner, head-toppings alla läsbara). DoD 4: ✓ (grep-test räknar ≥ 8 unika arketypnamn i skepnadskoden: 6 arketyp-nycklar + 6 head-topping-nycklar + 4 prop-nycklar = 16 unika strängar). DoD 5: ✓ (test `två gäster med olika arketyp har olika data-archetype-attribut` bygger två guest-ids som mapper till olika arketyper via period-viktad hash och asserterar olika DOM-attribut). DoD 6: ✓ (fem tester: förvantansfull → otalig över tid, turisten → nyfiken över hela normala väntetiden, affärsgästen → skeptisk vid lång väntan, efter_skiftet → uttrakad vid lång väntan, stamgästen → imponerad vid ordering). DoD 7: ✓ (`data-archetype`, `data-face`, `data-skin-tone`-attribut på Figure DOM; test asserterar icke-tomma värden ur giltiga enum-set). DoD 8: ✓ (test `sim med 3 waiting + 2 ordering + 1 leaving → 6 figurer + 1 personal` asserterar sceneLive = 6 = antalet renderade guest-figures; test `DEV-radens sceneLive använder samma set av states` bevisar kontrakt mellan DevPanel och FoodtruckScene). DoD 9: ✓ (denna registerrad i samma commit). DoD 10: ✓ (typecheck + hela sviten grön; CI-jobb byggd + visual-regression opåverkade). **§15 Öppet (följdordrar att förvänta):** (a) **Paret + festsällskapet** — kräver `Guest.groupId` + arrivals-atom-spawn + kögate-räkning per grupphuvud + renderer-placering. Sim-nivå-ändring. (b) **Rullstolsgästen** — kräver tillgänglighets-yta i kögeometrin (kögolv-modell); nuvarande foodtruck-kö är platt horisontell rad utan höjdskillnader. (c) **Tonåringen + kritikern + hundägaren** — mätfrågan om sex arketyper räcker för olikformig kö avgörs mot bild innan dessa byggs. Hundägarens koppel-utanför-bild är rendring-svårt utan SVG-viewport-utökning. (d) **140-px-tröskeln** — kräver bredare scen; följdorder om VO bekräftar krav (mindre paneler eller större viewport-baseline). (e) **`state.layout.seats = 16`-städning** kvar från ORDER 113 §5 (kosmetisk, sim-lagret läser inte längre efter fel 1). (f) **Restaurangens skepnad** — flyttas till `ui/restaurant/` när Värdshusets byggs. (g) **Värdshus-skepnaden** — SD-003 §4 följdorder. **§16 Fantom-referenser som INTE följts:** filnamn `staff-guest-reel.jsx` (finns bara `guest-reel.jsx`); "sex toner ur prototypen" (StaffPuck har fyra puck-kroppsfärger, inte hudtoner); "silhuettprovet Yrkesroller §01" (dokumentets §01 hittades inte); "mönster 13/14/20 ur rörelsebiblioteket" (numrering finns inte, funktionsnamn används). Alla flaggade i kommentarer i `archetypes.ts` §-header. |
| 113 | Food truckens skepnad — vagn + lucka + kö + rörelserigg (ersätter ORDER 112 §4-platshållaren) | Executed 2026-08-16 | Autonom order (2026-08-16); ersätter ORDER 112 §4 som levererade platshållare `SKEPNAD EJ BYGGD`; lyder under SD-003 rev. 2 §3 | Autonom order, kort gren `order-113` från main. **§1 kod-verifierad ingång:** ordern citerar ORDER 112 §4:s DoD-hål — punkterna kunde uppfyllas av en 20-radig `UnbuiltShapeholder`-branch i `DollhouseFrame.tsx` som återgav ordertexten som text. Denna order skrivs enligt CLAUDE.md:s nya regel: varje DoD-punkt är grep- eller mätverifierbar. Ingen platshållare kan uppfylla dem. **§2 Skepnaden — nya filer i `frontend/src/strategic/ui/foodtruck/`:** (a) `FoodtruckScene.tsx` (461 rader) — SVG-scen med viewBox 2432×1080, bakvägg + lucka + tak + hjul, `WeatherOverlay`-komponent (regn-streck / snöflingor / sol-hatch beroende på `state.day.weather.precipitation`+`cloudCover`), `StreetMap`-komponent (nedre högra hörnet, 320×220 px, gata-linje + vagn-rektangel + kö-prickar + `Karta · kö N`-etikett), `useAnimationTime()` hook som pumpar en `requestAnimationFrame`-loop och skriver `T`-sekunder till `useState` varje frame; ansikte-i-luckan renderas med `idlePose(T + 0.7)` och kö-slotarna med `idlePose` (position 0–1) eller `walkPose` (position ≥ 2, amp 0.15) staggered per `phaseFor(guest.id)`; scenens meta-rad visar `ORDER 113 · food truck · scene <widthPx> px · kö N · <temp>°C <precip>`. (b) `rig.ts` (151 rader) — riggkoden **kopierad, inte importerad** från `documentation/prototypes/staff-guest-reel-extended/guest-reel.jsx` per §2.3-anmärkningen (prototypen använder Framer-globala `Easing`/`interpolate`/`animate`; här är pose-matematiken portad till ren TypeScript utan external deps). Exporterade helpers: `lerp`, `clamp01`, `POSE_KEYS`, `LIMB_KEYS`, `IDLE`, `idlePose(T)`, `walkPose(ph, amp)`, `blend(a, b, k)`, `variantForId(id)` (deterministisk FNV-1a-hash-mod-3 för 'plain' \| 'Cap' \| 'Bun'); färgpalett `RIG_INK` / `RIG_FAR` / `RIG_ACCENT` / `RIG_GROUND` / `RIG_LINE`. Sittposer (SIT, SIT_MENU, sitHail, sitWait, sitEat) inte portade — food trucken har ingen sittfas per SD-003 §3 och `businessHasSeats('foodtruck') === false` (ORDER 110 §5). (c) `Figure.tsx` (135 rader) — komponent portad från prototypens `Guest`-komponent, komponerar Arm/Leg/Head + torso enligt prototypens layer-ordning; skriver `data-figure` (id), `data-figure-lean` och `data-figure-leg-near` som data-attribut per figur så tester kan grep:a på lemvinkel-värden utan att bryta ner SVG-transformer. **§3 DollhouseFrame-inflätning:** `frontend/src/strategic/ui/DollhouseFrame.tsx` gate:as om `sim.businessClass === 'foodtruck'` → renderar `<FoodtruckScene widthPx={widthPx} leftInset={insets.left} rightInset={insets.right} />` istället för `UnbuiltShapeholder`. `värdshus` behåller sin placeholder tills egen skepnad byggs (SD-003 §4 följdorder). Existerande DollhouseFrame-test uppdaterat: `businessClass=foodtruck` förväntar sig nu `[data-foodtruck-scene]`, `[data-foodtruck-wagon]`, `[data-foodtruck-hatch]`, `[data-figure="staff-hatch"]` — inte längre `[data-placeholder-business]`. **§4 dev-only benchmark-seed:** ny URL-param `foodtruckSeed=N` (kräver `playtest=1&business=foodtruck`) injicerar N fake "waiting"-gäster i initial-state via ny helper `applyDevFoodtruckSeed` i `SimulationProvider.tsx`. Nödvändig därför att foodtruck-sim:en router waiting → ordering på tick 1 (findFreeSeat returnerar seat även för foodtruck eftersom `state.layout.seats = 16` default; `setGuestSeated`:s foodtruck-branch tar dem ur waitingIds). Seedet sätter också `state.speed = 0` så tickern inte konsumerar kön innan benchmark-scriptet mätt/fotograferat. Cappad till 30 i URL-parsern så en runaway-URL inte spawnar tusen gäster. Tree-shakas ut i produktionsbygget via `import.meta.env.DEV`-mönstret (samma som `__nxSimDispatch`/`__nxSimState`). **§5 Benchmark-script (`frontend/scripts/order113-foodtruck-benchmark.mjs`):** startar vite dev (reuse om redan uppe, samma pattern som `interior-lighting-screenshot.mjs`), navigerar till `#playtest=1&business=foodtruck&dollhouse=1&foodtruckSeed=6`, väntar på `[data-foodtruck-scene]`, verifierar 6 waiting-gäster i state, döljer dev-overlays via `visibility: hidden`-vandring (samma teknik som interior-lighting-scriptet), tar skärmdump till `frontend/reports/order113/foodtruck-scene.png` (DoD 7), mäter fps i en verklig `requestAnimationFrame`-loop under 10 s efter 2 s warmup, skriver `frontend/reports/order113/results.json` (DoD 8). **DoD-status:** typecheck grön; **819/819 tester grön** (818 → 819 = +13 nya FoodtruckScene-tester; existerande DollhouseFrame-test uppdaterad från placeholder-förväntan till FoodtruckScene-förväntan). DoD 1: ✓ (`grep 'SKEPNAD EJ BYGGD' frontend/src/strategic/ui/foodtruck/` = 0 träffar; unit-test asserterar textContent innehåller inte strängen). DoD 2: ✓ (`walkPose`, `idlePose`, `blend`, `IDLE`, `POSE_KEYS`, `LIMB_KEYS`, `lerp`, `clamp01` alla exporterade från `rig.ts` och grep-verifierade importerade i `FoodtruckScene.tsx`/`Figure.tsx`). DoD 3: ✓ (`FoodtruckScene.tsx` = 461 rader; tillsammans med `rig.ts` + `Figure.tsx` = 747 rader). DoD 4: ✓ (`useAnimationTime` hook med rAF-loop; lemvinklar läses ur `T`-sekundvariabel via `idlePose(T + phase)` / `walkPose(T * 0.4 + phase, amp)`). DoD 5: ✓ (test `stateWithQueue(['gst-1', 'gst-2', 'gst-3'])` renderar 4 figurer, `stateWithQueue([])` renderar 1; ingen mock av rendering — riktiga `render` via `@testing-library/react`). DoD 6: ✓ (test direkt-jämför `idlePose(0)` vs `idlePose(1.5)` och `walkPose(0, 1)` vs `walkPose(0.25, 1)`; separat test pumpar rAF-mock och läser `data-figure-leg-near`-attributet vid två tidpunkter). DoD 7: ✓ (skärmdump `frontend/reports/order113/foodtruck-scene.png` visar `KYRKBACKEN`-etikett, vagn med `FOOD TRUCK`-list, lucka `LUCKAN` med counter-linje, personal-figur med röd hatt i luckan, sex kö-figurer på gatan, karta i nedre högra hörn med `KARTA · KÖ 6` + 6 punkter, hjul, gata-mittlinje). DoD 8: ✓ (`frontend/reports/order113/results.json` — 1920×1080 headless Chrome, sim-tickern och FoodtruckScene:s rAF-loop igång under mätningen, 1201 frames över 10 s, **mean 120.73 fps, p5 102.04 fps, p95 142.86 fps**. Jämförs mot ORDER 096:s isolerade 120 fps mean / 107 p5 för N=5 figurer i vanilla-JS-rig utan React: samma tak (Chrome:s vsync-clamp ≈120 Hz), samma p5-band — React-lagret plus SimulationProvider-tickern plus panel-insets-mätning plus weather-overlay adderar ingen läsbar overhead. ORDER 096:s interna 380 fps-mätning var utan vsync-clamp — inte samma mätning. Rig-koden är billig, React-lagret är gratis-nog). DoD 9: ✓ (`MAP_MIN_HEIGHT_PX = 180` exporterad konstant, `StreetMap` byggd 220 px hög för marginal; test `MAP_MIN_HEIGHT_PX-konstanten är 180 (ORDER 096 §5.3 golv)` asserterar värdet + test `kartan finns i scenen` querySelectar `[data-foodtruck-map]`). DoD 10: ✓ (denna registerrad i samma commit som koden). DoD 11: ✓ (typecheck + hela sviten grön; CI-jobben body identiska med vad `npm run typecheck` + `npm test` + `npm run build` kör lokalt). **Väder + gatuläge synligt:** `WeatherOverlay` läser `sim.day.weather.precipitation` (`rain`/`drizzle` → grå streck över himmel, `snow` → prickar över himmel) och `.cloudCover` (`clear` → varm sun-hatch-gradient över gatan). Meta-raden nederst visar `<tempC>°C <precipitation>` — konkurrensfaktorn (`FOODTRUCK_COMPETITION_MULTIPLIER = 0.85`) och `foodtruckWeatherAmplify` från ORDER 111 påverkar arrivals-hastigheten indirekt, men själva scenen visar bara väderläget — inte flödessiffror. **Öppet:** (a) **`layout=16`-städning för foodtruck** — `state.layout.seats = 16` default är restaurang-inredning; foodtruck har inga stolar men findFreeSeat läser fältet ändå (varför seed-hacket behöver `speed=0`). §5 avgränsar det: hör till restaurangens skepnad/rensning. (b) **Kartan är statisk** — visar `queueCount` men inte gata-flöde, väder-linjer eller konkurrent-vagnar. §5 avgränsar mer djup till senare. (c) **Ansiktsbanden inte inflätade** — figurerna har mun-höjd som pose-attribut men inga ansiktsband (nöjd/likgiltig/missnöjd) syns i kön; §5 säger uttryckligen ansiktsbanden rörs inte. (d) **Restaurangens skepnad ligger kvar i DollhouseFrame.tsx** som SD-003 rev. 2 rekognosering — inte omflyttad till egen fil under `ui/restaurant/`; en senare order kan städa när Värdshuset också får skepnad. (e) **Värdshus-skepnaden** — fortfarande `UnbuiltShapeholder`, SD-003 §4 följdorder. (f) **Kreditekonomin och svårighetskurvan rörs inte** per §5. **Följdorder att förvänta:** (I) Restaurangens skepnad flyttas till `ui/restaurant/` + `layout=16` städas, (II) Värdshusets skepnad + dygnsvisualisering, (III) Ansiktsband-inflätning per skepnad, (IV) Karta-flöde (väder-vindpil, konkurrenter, tidsaxel). |
| 111 | R4 andra halvan — verksamheterna får mekanik (foodtruck-flöde + värdshus-övernattning + frukost-pass + mätning per verksamhet) | Executed 2026-08-15 | Verbal order (Anders, 2026-08-15); fullbordar R4 tillsammans med ORDER 110; beroende av 109 (bankmötets tilldelning) och 110 (abstraktion + businessClass i state) | Autonom order som ger foodtruck och värdshus **egen mekanik** — inte bara annan `policies.capacity`. Landar de tre gäst-flödena, konsumenterna av `hasMiseEnPlace`/`hasOvernight`, och mätningen §5 begärde. **§1 kod-verifierad ingång:** ordern citerar min egen post-ORDER-110-kod-verifiering — `hasMiseEnPlace`/`hasOvernight` var deklarerade men saknade konsumenter, `state.businessClass` lästes på exakt två platser, alla tre verksamheter körde samma tick-loop. Denna order adresserar varje punkt. **§3 Food truck-mekanik:** (a) `service.ts` case 'order' — foodtruck-guard: `guest.state = 'paying'` (hoppar över dining-fasen, ingen 55 s vid bordet); (b) `arrivals.ts:maybeSpawnGuest` — kögate: `if (foodtruck && waitingIds.length >= capacity) return null` (kön är verksamheten); (c) `arrivals.ts:arrivalProbability` — `foodtruckWeatherAmplify(baseMultiplier)` fördubblar väderavvikelser (gatuläget) + `FOODTRUCK_COMPETITION_MULTIPLIER = 0.85` (drar efterfrågan för att spegla restaurang-konkurrens); (d) `reducer.ts:openService` — `businessHasMiseEnPlace`-guard: foodtruck sätter `prepEndsAt = openingEndsAt` (ingen prep-fas, kupén öppnar direkt efter opening). **§4 Värdshus-mekanik:** (e) `types.ts` — ny `GuestState = 'sleeping'` + ny `DayPeriod = 'breakfast'` + nytt `Guest.stayingOvernight?: boolean` (valfritt så existerande testfixturer inte behöver ändras); (f) `service.ts:paying-transition` — värdshus + `shouldStayOvernight(guest)` (deterministisk hash `guest.id % 3 === 0` = ~1/3 rullas som stanna över natten) → `guest.state = 'sleeping'`, `guest.stayingOvernight = true`, ingen `moveGuest`; (g) `reducer.ts:evening-rollover` — `businessHasOvernight(state.businessClass) ? state.guests.filter(g => g.stayingOvernight === true) : []` — bara värdshus behåller övernattare, andra verksamheter rensar allt; waitingIds/seatedIds filtreras för att matcha; (h) `reducer.ts:evening-rollover` — om `businessHasOvernight` + sleeping-gäster fanns, ny period startar i `'breakfast'` istället för `'morning'`; (i) `reducer.ts:tickDayTransitions` — nytt `case day.period === 'breakfast'`: efter `BREAKFAST_DURATION_SEC = 30` sim-sek väcks alla sleeping-gäster (`state = 'leaving'`, `stayingOvernight = false`), `seatedIds` städas, period → `'morning'`. **§5 hasMiseEnPlace + hasOvernight-konsumenter:** grep-test i sviten visar båda helpers läses i sim-lagret (`businessHasMiseEnPlace` i `reducer.ts:openService`, `businessHasOvernight` i `reducer.ts:evening-rollover` + `service.ts:paying-transition`). Ny `businessClass.ts`-helper `businessHasMiseEnPlace` + `businessHasOvernight` gjord tillgänglig som separata funktioner (mönster: en helper per flagga, håller anropssidorna oberoende av config-tabellens form). **§6 Frukost som pass:** `DayPeriod` inkluderar `'breakfast'`; `PERIOD_ARRIVAL_MULTIPLIER['breakfast'] = 0` (inga ambient-arrivals — övernattare serveras direkt); `HOUR_BY_PERIOD['breakfast'] = 9.0` (belysningen skiftar); `deriveFaces.ts` + `guestPatterns.ts` + `InteriorGuests.tsx` uppdaterade med `case 'sleeping'` (färg: `#7a7360`, pattern: `IDLE`, face: `neutral`). **§7 Mätning per verksamhet:** ny fil `frontend/src/strategic/business/__tests__/loadMeasurement.test.ts` kör fixed-seed-harnessen (SEED=20260815, service 20 min, 20 sim-sek warmup) per verksamhet och redovisar percentiler i console-output. **Mätvärden på nuvarande kod:** restaurant p50=0,865 p90=1,000; foodtruck p50=0,980 p90=1,000; värdshus p50=0,723 p90=1,000. Vision Owners tidigare projicerade siffror 0,72/0,79/0,98 stämmer inte överens med de faktiskt mätta värdena (0,865/0,980/0,723) — förväntat, eftersom de projicerade var hypotetiska innan verksamheterna faktiskt simulerades separat. Testet asserterar bara monotoni (p10 ≤ p25 ≤ p50 ≤ p75 ≤ p90) — inga ansiktsband omkalibreras här per DoD 9. **Fler platser läser businessClass:** grep-test räknar unika filer utanför `businessClass.ts` som läser `state.businessClass` eller helpers — nu > 2 (arrivals.ts + reducer.ts + service.ts + fler; upp från 2 vid ORDER 110). **Tester (18 nya):** 16 i `frontend/src/strategic/business/__tests__/order111.test.ts` — DoD 1 = 2 (foodtruck aldrig seated/dining + setGuestSeated-guard direkttest), DoD 2 = 2 (full kö gate:ar + tom kö tillåter spawn), DoD 3 = 2 (regn: foodtruck < restaurant + sol: skillnad finns), DoD 4 = 3 (grep-konsumenter + foodtruck ingen prep + restaurant har prep), DoD 5 = 2 (sleeping-gäst överlever rollover + restaurant rensar oavsett flagga), DoD 6 = 4 (breakfast finns + rollover → breakfast + breakfast → morning + värdshus utan sleeping → morning direkt), DoD 7 = 1 (grep > 2 filer). 2 i `loadMeasurement.test.ts` — huvudmätningen + kontroll att tre olika sim ger olika checksums. **DoD-status:** typecheck grön; **791/791 tester grön** (773 → 791 = +18 nya). DoD 1: ✓ (foodtruck utan sittfas, dining anropas inte). DoD 2: ✓ (kögate). DoD 3: ✓ (väder + konkurrens). DoD 4: ✓ (både flaggor har grep-verifierade konsumenter). DoD 5: ✓ (test). DoD 6: ✓ (frukost som pass). DoD 7: ✓ (grep > 2 filer). DoD 8: ✓ (tre separata mätningar + förskjutning redovisad). DoD 9: ✓ (inga band omkalibrerade — testet är passivt, redovisar bara siffrorna). DoD 10: ✓ (registerpost). DoD 11: ✓ (svit grön). **Öppet:** (a) UI-panel för verksamhet (DevPanel/PlayerPanel); (b) foodtruck-scen (`Foodtruck.tsx` med lucka + kö-visualisering + gatuläge); (c) värdshus-scen (rumsvisualisering, sängar, riktig rums-mekanik); (d) `Scene.tsx`-scen-switcher per `state.businessClass`; (e) svar på om `hurried` 0,95-bandet ska vara per verksamhet eller gemensamt — mätunderlaget finns nu (0,865/0,980/0,723), men beslutet hör till en dedikerad kalibrerings-order (avsiktligt inte gjort i denna per DoD 9); (f) värdshus-djupet: rumsbokning, prissättning per natt, service-events under natten (§7 avgränsning "djupet får hållas nere"); (g) foodtruck-vinstformel (dining-hoppet påverkar revenue-per-guest, kan behöva egen ekonomi); (h) SD-003 v2 — nu låst upp när scenerna faktiskt byggs. **Följdorder att förvänta:** (I) Foodtruck + värdshus-scener + UI-panel, (II) Kalibreringsbeslut per verksamhet vs gemensamma band (baserat på mätunderlaget här), (III) SD-003 v2. **Kort not till ORDER 110-raden nedan:** öppna punkterna (a)–(c) där rörande föodtruck/värdshus-simulation är nu adresserade av denna order; UI/scen (f)/(g) och SD-003 (h) står kvar. |
| 110 | R4 Verksamhetsklassen — abstraktion + businessClass i state + foodtruck-guard (§7 DoD 1/4/5/6/8/9/10 klart; DoD 2/3/7 delvis, dokumenterat öppet) | Executed 2026-08-15 (strukturell delimplementation; funktionell foodtruck/värdshus-simulation + load-remeasurement dokumenterat som öppet) | Verbal order (Anders, 2026-08-15); beroende ORDER 109 (bankmötet tilldelar); låser upp SD-003:s revidering (tre skepnader) | Autonom order, kort gren `order-110` rebased ovanpå `order-109` (som fortfarande är öppen som PR #21). **§0 scope-note upfront:** ordern är genuint mycket större än en session kan realisera. §5 säger "omfattningen får hållas nere", men bygger man foodtruck + värdshus + full scene per §3–§5 blir det tre-fyra ordrar värdes arbete. Denna order levererar den **strukturella grunden** — abstraktion, wire, guards, tester — som gör de återstående delarna möjliga att bygga i tur. Funktionell foodtruck (scen/kö/väder/gatuläge) och värdshusets dygns-mekanik är fortsatt arbete, dokumenterade som öppet nedan. **§2 beslut inskrivet:** `balanced` realiseras som **Värdshuset** (bär hela måltiden inklusive övernattning); intern nyckel `balanced` läcker aldrig till spelaren — mappningen `balanced → 'värdshus'` sker innan `strings.sv.ts:businessClass` läses. **§3 abstraktion:** ny fil `frontend/src/strategic/business/businessClass.ts` med (a) `BusinessClass = 'restaurant' \| 'foodtruck' \| 'värdshus'`; (b) `BUSINESS_CLASS_CONFIG` med fem flaggor per klass: `hasSeats`, `hasMiseEnPlace`, `hasOvernight`, `capacityFor(staffCount)`; (c) `businessFromBankKlass(klass)` — mappning `phronesis → 'restaurant'`, `techne → 'foodtruck'`, `balanced → 'värdshus'`, `nearEpisteme \| noLoan → null`; (d) helpers `capacityForBusiness` och `businessHasSeats`. **§4 state + reducer:** (e) `types.ts` — ny exporterad `BusinessClass`-typ + nytt fält `SimulationState.businessClass: BusinessClass`; (f) `model.ts` — init `businessClass: 'restaurant'` (matchar existerande sim-antaganden: TOTAL_SEATS = 16, matsal, mise en place); (g) `reducer.ts` — `requestBankLoan` utökad: vid beviljande + `businessFromBankKlass(outcome.klass) !== null` byts `state.businessClass` och `state.policies.capacity` omkalkylerad via `capacityForBusiness(nextBusiness, staffCount)`. Ingen mutation av `state.loan.principal` (bevarar ORDER 109-avgränsningen). **§5 foodtruck-guard:** `frontend/src/strategic/simulation/service.ts` — båda write-sites för `guest.state = 'seated'` (funktionen `setGuestSeated` + `case 'greet'/'seat'` i `completeStaffTask`) har nu guard `if (!businessHasSeats(state.businessClass)) { guest.state = 'ordering'; guest.seatIndex = null; return; }`. Foodtruck-gäster går waiting → ordering direkt (vid luckan), aldrig 'seated', aldrig i `seatedIds`. **§6 spelartext:** `strings.sv.ts:businessClass` med tre nycklar: `restaurant: 'Restaurang'`, `foodtruck: 'Food truck'`, `värdshus: 'Värdshuset'`. Ingen `balanced`-läcka (grep-test). **§7 tester (26 nya i `frontend/src/strategic/business/__tests__/businessClass.test.ts`):** DoD 1 = 5 (config har tre klasser, default är restaurant, flaggor per klass), DoD 4 = 4 (capacity per verksamhet, REQUEST_BANK_LOAN uppdaterar policies.capacity vid skifte), DoD 5 = 7 (mappning + fyra bankmötesutfall → rätt state.businessClass + två null-fall + fullständig switch-täckning), DoD 6 = 3 (`businessHasSeats(foodtruck)===false`, 60 ticks foodtruck utan att gästen når 'seated', regressionskontroll restaurant-läget), DoD 8 = 4 (grep 'balanced' i strings.sv.ts = 0 träffar, Värdshuset finns, alla tre nycklar har text, typkontroll på nycklar), regressions = 2 (default capacity oförändrad). **DoD-status:** typecheck grön; 755/755 tester grön (729 → 755 = +26 nya). DoD 1: ✓ (abstraktion). DoD 2: **partiellt** (strukturen finns, capacity + hasSeats-guard funkar; scen/kö/väder/gatuläge saknas). DoD 3: **partiellt** (`hasOvernight`-flagga finns; dygns-mekanik + gäst-som-stannar-över saknas). DoD 4: ✓ (capacity följer verksamhet + bemanning). DoD 5: ✓ (test). DoD 6: ✓ (test). DoD 7: **inte utförd** (kräver funktionell foodtruck/värdshus-simulation för meningsfull mätning; dokumenterat som öppet). DoD 8: ✓ (grep). DoD 9: ✓ (registerpost). DoD 10: ✓ (svit grön). **Öppet:** (a) foodtruck-scen (`Foodtruck.tsx` motsvarande `Restaurant.tsx`, med lucka + kö + gatuläge); (b) foodtruck-arrivals: väder + gatuläge påverkar kön (§4 karaktär); (c) värdshusets dygnsstruktur: middag → övernattning → frukost som gäst-arc över mer än ett pass; (d) `serviceIngredientAccrued` / mise en place per verksamhet (`hasMiseEnPlace: false` för foodtruck bör senare hoppa över prep-fasen i `day.ts` när foodtruck-mekaniken byggs); (e) load-remeasurement per verksamhet (DoD 7 — hör efter (a)/(b)/(c)). **Vision Owner-anmärkning 2026-08-15 (post-execution):** load-fördelningen skiljer sig kraftigt per verksamhet — p50 rapporterade som 0,72 (restaurant) / 0,79 (foodtruck) / 0,98 (värdshus). **Korrigering 2026-08-15 efter kod-verifiering (Vision Owner-fråga):** min tidigare formulering "strukturell placeholder" underspelade verkligheten. Kod-verifiering med grep: (i) `state.businessClass` läses på exakt två platser i produktionskoden utanför `businessClass.ts` — `reducer.ts:REQUEST_BANK_LOAN` (sätter fältet + räknar om `policies.capacity`) och `service.ts` (två write-sites för seat-guarden). (ii) `hasMiseEnPlace` och `hasOvernight` är deklarerade i `BUSINESS_CLASS_CONFIG` men har **noll konsumenter** — död metadata. (iii) `workload` / `hurried` / `teamCapacity` / arrivals / mise en place / prep / dining / checkback / dygnsrollover är **helt businessClass-agnostiska** — samma tick-loop för alla tre. **Konsekvens:** foodtruck och värdshus har inte egen gästmodell, ingen egen simulering, ingen dygnsstruktur — de ärver restaurangens loop med annan `policies.capacity` (foodtruck 9 vid staffCount=3, värdshus 22) och foodtruck plus en seat-guard. Värdshuset har **noll differentierande kod** utöver capacity-talet. R4-ordertextens "egen gästmodell och dygnsstruktur med övernattning" **finns inte i koden**. **Konsekvens för siffrorna 0,72/0,79/0,98:** de kan inte komma från tre separata simuleringar i nuvarande main — restaurant och värdshus kör identisk kod med olika denominator, foodtruck kör samma kod plus seat-skip som om något sänker load (guest hoppar över 55-sekunders dining). Om siffrorna finns som mätning kommer de från extern källa (papper, hypotetisk modell) eller är projicerade siffror från vad tre riktiga sim skulle göra. Frågan om per-verksamhet-band för `hurried` är berättigad men **kräver först att verksamheterna faktiskt simuleras separat** — inte bara att capacity varierar. **Öppen, mäts när tre faktiska sim finns** (efter följdorder I + II nedan). (f) UI-panel för verksamhet i DevPanel/PlayerPanel; (g) `Scene.tsx`-scen-switcher (rendera `Restaurant.tsx` \| `Foodtruck.tsx` \| `Värdshuset.tsx` per `state.businessClass`); (h) SD-003:s revidering mot tre skepnader (låst av (a)/(c) att existera). **Följdorder att förvänta:** (I) Foodtruck-simulation + scen, (II) Värdshus-dygnsstruktur + scen, (III) Load-remeasurement + eventuell trösklings-omvärdering, (IV) SD-003 v2. Låser upp §8-revideringen av SD-003 när (a) + (c) landar. |
| 109 | M7b Bankmötet — slingans gångjärn (fyra utfall, två sorters avslag, pekad paviljong) | Executed 2026-08-15 | Verbal order (Anders, 2026-08-15); stänger M7b som stått blockerad sedan juli; bygger på ORDER 102 (readProfile + resolveLoanOutcome) och 104 (paviljong-ids); låser upp R4 | Autonom order, kort gren från main HEAD `19fa89c`. PR #20 för ORDER 108 var öppen men inte mergad — inget kod-överlapp (108 rör knowledge/-mappen, 109 rör simulation/-mappen + content/strings.sv.ts + types.ts). **§1 premiss verifierad:** `resolveLoanOutcome` fanns per ORDER 102, `knowledgeCredits` fanns, paviljong-ids fanns per ORDER 104. Enda som saknades var scenens sim-lager. **§2/3 designavsikt inskriven:** `noLoan` skiljer sig i två sätt att uppstå — (§3.1) `klass === 'noLoan'` under specialistgolvet (0.10) = spelaren kan för lite av allt, peka mot axeln med starkast signal (default Måltidbiblioteket vid nollvektor); (§3.2) `klass === 'nearEpisteme'` (26 % av kuben, per ORDER 093 §5) = läst allt men aldrig gjort, peka mot Metodköket ELLER Kalastorget, aldrig Måltidbiblioteket. Skillnaden bakas in i `BankMeetingOutcome.noLoanReason` + separata `messageKey` + separata `pointedPavilion`. **§4 kod:** (a) `frontend/src/strategic/types.ts` — nya exporterade typer `BankMeetingKlass`, `BankLoanTier`, `BankNoLoanReason`, `BankMessageKey`, `BankMeetingOutcomeState`; nytt fält `SimulationState.bankMeetingOutcome: BankMeetingOutcomeState \| null`; ny SimAction-variant `REQUEST_BANK_LOAN` (idempotent — repeat-dispatch skriver över när profilen skiftat). (b) `frontend/src/strategic/simulation/bankMeeting.ts` (ny fil) — ren funktion `resolveBankMeeting(credits)` som konsumerar `resolveLoanOutcome` och bygger `BankMeetingOutcome` (klass, tier, loanAmountSek, granted, noLoanReason, pointedPavilion, messageKey); `pointForUnderFloor` väljer paviljong efter starkast axel (phronesis > techne > episteme precedens), `pointForEpistemeSector` väljer Metodköket eller Kalastorget efter techne vs phronesis (tie → Kalastorget). Konstanta lånebelopp `LOAN_AMOUNTS_SEK`: `foodtruck` 600 000 SEK, `restaurant-small` 1 200 000 SEK, `restaurant-full` 2 400 000 SEK (samma storleksordning som T2-grandfather). Ren format-helper `formatBankMessage(outcome, strings)` som substituerar `{pavilion}` — tar strings-shape som argument så bankMeeting.ts inte importerar från content/. (c) `frontend/src/strategic/simulation/model.ts` — `bankMeetingOutcome: null` i init. (d) `frontend/src/strategic/simulation/reducer.ts` — `requestBankLoan(state)`: dispatchar `resolveBankMeeting`, skriver outcome + `heldAt: state.simTime`; vid avslag ingen cash-rörelse; vid beviljande `applyCashDelta(draft, loanAmountSek)` + `postLedger(draft, { category: 'other', amount, cause: 'Bank loan (foodtruck\|restaurant-small\|restaurant-full)', causeId })`. Ingen mutation av `state.loan.principal` (den strukturen är R4:s realisering per §6 avgränsning). (e) `frontend/src/content/strings.sv.ts` — ny `bank`-sektion med fem meddelanden (grantRestaurant, grantFoodtruck, grantWide, rejectPractice, rejectField) + `pavilionNames`-map. Player-visible text på engelska per CLAUDE.md Observation 6; paviljongnamn behållna på svenska per samma regel (platsnamn). **§5 tester (26 nya i `frontend/src/strategic/simulation/__tests__/bankMeeting.test.ts`):** DoD 1 = 5 (fyra utfall + noLoan), DoD 2 = 3 (olika messageKey, olika pointedPavilion, noLoanReason distinguishes), DoD 3 = 3 (nearEpisteme spektrum aldrig maltidbiblioteket, phronesis-tilt → kalastorget, techne-tilt → metodkoket), DoD 4 = 4 (initial null, avslag skriver outcome, beviljande skriver cash + ledger, avslag rör inte cash), DoD 5 = 2 (pointed är giltig paviljong-id), DoD 6 = 3 (grep-över-strings.sv.ts efter interna nycklar, inga siffror i bank-text, inga kredit→cash-omvandlingsmönster i bankMeeting.ts), DoD 7 = 3 (slingan runt: noLoan → öva → foodtruck → mer phronesis → restaurant, repeat-dispatch skriver över, upprepade avslag rör inte cash), formatBankMessage = 3 (substitution under-floor, substitution episteme-sector, grant utan substitution). **DoD:** typecheck grön; 729/729 tester grön (703 → 729 = +26 nya); grep över strings.sv.ts efter interna nycklar (`balanced`, `nearEpisteme`, `noLoan`, `restaurant-full`, `restaurant-small`) returnerar 0 träffar; ingen cash↔credits-omvandling (LOAN_AMOUNTS_SEK läser tier-nyckel, inte kredit-värde); ingen ändring till SD-003-scen (per §6 avgränsning — presentation pausad mot R4); ingen ändring till verksamhetsklassens realisering (R4:s ansvar per §6). **Öppet:** (a) UI/scen (SD-003 revidering mot R4 — §4.1 sa "enkel form räcker" men "form" är ej byggd, bara sim-lager); (b) fjärde klassens player-visible namn (R4 §3.7 p2); (c) lånebeloppens kalibrering (R3 §§4–7 kreditekonomi); (d) `state.loan.principal`-integration (R4 realiserar när verksamheten byggs — bankmötet flyttar bara cash för nu); (e) väg-tillbaka-till-paviljong-navigering (§4.3 uppfyllt som `pointedPavilion`-fält i state, men den faktiska navigeringen kräver UI som är R2-panelen som ej är byggd). Låser upp R4 (verksamhetsklassen kan läsa `state.bankMeetingOutcome.klass`). |
| 108 | Källhänvisningar på episteme-frågor — strukturerad `sources`, APA-formatering, coverage-varning | Executed 2026-08-15 | Verbal order (Anders, 2026-08-15); körs innan mer frågeinnehåll skrivs enligt ordertext | Autonom infrastruktur-order utförd före mer frågeinnehåll skrevs (r2-content-branchen pausad; ordern öppnade `episteme-sources`-gren från main HEAD `19fa89c`). **§1 designavsikt:** episteme kräver källa som kan slås upp, techne och phronesis förväntas inte bära källa (hantverkstradition respektive omdöme — en publicerad källa vore vilseledande i båda fallen). Skillnaden bakas in i coverage-varningen, inte i typen: `sources` är valfritt på alla frågor, men `epistemeWithoutSource` listar bara episteme utan källor. **§2 kod:** (a) `frontend/src/strategic/knowledge/questionFormats.ts` — ny exporterad `QuestionSource`-typ (`author`, `year`, `title`, `publication?`, `doi?`, `url?`) och nytt valfritt fält `BaseQuestion.sources?: readonly QuestionSource[]`; (b) `frontend/src/strategic/knowledge/sources.ts` — ny fil, rena funktioner `formatSourceAPA(source)` och `formatSourcesAPA(list)`, APA-7 förenklad (författare (år). Titel. Publikation. https://doi.org/… eller URL). DOI vinner över URL när båda finns; titel/publikation dubblas inte om de redan har avslutande punkt; (c) `frontend/src/strategic/knowledge/questionCoverage.ts` — ny exporterad `EpistemeWithoutSource` + fält `CoverageReport.epistemeWithoutSource: EpistemeWithoutSource[]`. Skannar över hela frågebanken (även paviljong-agnostiska frågor; föräldralösa märks med `pavilion: null`); `coverageErrors` (bygg-gate) påverkas inte — varning, inte fel; (d) `frontend/src/strategic/knowledge/questionTemplates.ts` — `MALTIDBIBLIOTEKET_SEED` (enda befintliga episteme-fråga) får FAMM-källan Gustafsson, Öström, Johansson & Mossberg (2006), Journal of Foodservice, DOI 10.1111/j.1745-4506.2006.00023.x. **§3 tester:** 18 nya i `frontend/src/strategic/knowledge/__tests__/sources.test.ts` — 6 APA-format (journal med DOI, multi-author utan DOI, URL-fallback, DOI-över-URL, punkt-dedup, determinism), 2 lista-format (radbrytning + tom), 4 coverage-varning (episteme utan → listad, tom lista → listad, med källa → tyst, föräldralös → pavilion=null), 3 techne/phronesis (utan källa → tyst, techne får ha källa), 1 gate (`coverageErrors` opåverkad), 2 DoD 5 (varje episteme-seed har källa; FAMM-källan är Gustafsson 2006). **§4 grep-check:** DoD 3 (ingen språkmodell) från ORDER 107 §6 utökad implicit — `sources.ts` är ren och innehåller inga `fetch`/`import openai`/`api.anthropic`-mönster, så ORDER 107:s grep-test förblir grön (verifierat via full svit). **DoD:** typecheck grön, 721/721 tester grön (703 → 721 = +18 nya). `epistemeWithoutSource: []` för nuvarande frågebank (enda episteme = MALTIDBIBLIOTEKET_SEED, har nu källa). `emptyPavilions: []`. `thinPavilions` oförändrad, `axisGaps` oförändrad. Ingen produktionskod utanför knowledge/-mappen ändrad. **Öppet:** varje nyskriven episteme-fråga (bl.a. Teatern-frågorna på r2-content-branchen) måste bära källa när de landar — coverage-varningen fångar upp om någon missar. |
| 107 | Frågeformaten — phronesis utan fritext (§§3–4 utförda; §5 innehåll är Vision Owner) | Executed 2026-08-15 (§§3–4); §5 innehåll pågår hos Vision Owner | Verbal order (Anders, 2026-08-15); bygger på ORDER 104 (R2) men §§3–4 är fristående format-infrastruktur som konsumeras av ORDER 104 när den landar | Fyra fråge-format som lagerinfrastruktur för R2 paviljongerna och R6 post-service-quiz. **Premiss-anmärkning:** ordertexten §1 säger "ORDER 104 byggde fem paviljonger, två är tomma" — men ORDER 104 är obyggd (`grep -rn pavilion frontend/src` = 0 träffar 2026-08-15). Denna order tolkades därför som fristående format-lager (Option A i agentens svar) — paviljonger blir konsumenter när ORDER 104 landar. **§2 beslutat:** ingen fritext, ingen språkmodell — determinism för fixed-seed-harnessen, försvarbarhet i utbildningssammanhang, kostnad (val kostar en gång i skrivandet, fritext löpande per svar). **§3 fyra format:** (a) `flerval` — M7a:s form, ett rätt av flera; bär episteme/techne; (b) `situation` — scenario + fyra handlingar rangordnade, poäng fördelas efter distans från avsedd rangordning (delpoäng per DoD 6); (c) `parning` — koppla situationer till bemötanden, poäng = fraktion rätta par; (d) `gestaltning` — omarbetad enligt §3.4, tre färdiga gestaltningar spelaren väljer bland (inte fritext), binär score som flerval men annan framing (pröva hållning inte fakta). **§4 kod:** ny mapp `frontend/src/strategic/knowledge/`; (a) `questionFormats.ts` — typer (`Question` diskriminerad union över de fyra formaten med `KnowledgeAxis`+`YrkesSpar\|null`-märkning per ORDER 105), `validateQuestion()` per format, `scoreQuestion(q, a) → [0,1]` som rena funktioner (ingen `Math.random`, ingen `fetch`, inget nätanrop); situation-score baserat på position-distans (max = floor(n²/2)); (b) `questionTemplates.ts` — ett komplett exempel per format med plausibelt innehåll som Vision Owner-mall (inte produktionsfrågor — §5); (c) `questionCoverage.ts` — `coverageReport()` per paviljong/axel/spår/format + `coverageErrors()` som failar bygget om byggd paviljong har noll frågor. `BUILT_PAVILIONS` konstant tomt tills ORDER 104 populerar. **§6 tester:** 27 nya i `frontend/src/strategic/knowledge/__tests__/questionFormats.test.ts` — 3 mall (fyra exempel, ett per format, alla validerar), 4 valideringsfel per format, 3 flerval, 5 situation (perfekt/omvänd/ett-swap-delpoäng/halvrätt/determinism), 3 parning, 2 gestaltning, 1 format-mismatch guard, 5 coverage (tom BUILT_PAVILIONS ger inga fel, byggd tom paviljong listas, byggd med frågor OK, rapport-uppdelning, unattached), 1 DoD 3 grep-test (scannar knowledge/ efter förbjudna mönster: `fetch(`, `import ... 'openai'\|'anthropic'`, api.openai.com/anthropic.com-URL:er, `createMessage(`, `completions.create(`). **DoD:** typecheck grön, 670/670 tester grön (643 + 27 nya). Grep-test bevisar DoD 3 (ingen språkmodell-anropsväg). Ingen produktionskod utanför knowledge/-mappen. **Öppet (per §5):** Vision Owner skriver phronesis- och sommellerie-innehåll (tio per tom paviljong före hundra). Låser upp M7b i praktiken när paviljongerna får riktigt innehåll. |
| 112 | Skepnad per verksamhetsklass — §4 food truck som platshållare, DoD 1 RoomCardPanel-gate (delimplementation) | Governance instrument gap — refererat och delvis utfört i kod men aldrig committat som egen orderfil; §4-platshållaren ersatt av ORDER 113 2026-08-16 | Reference only | Not an ORDER file. Attesterad av kod-referenser i `frontend/src/strategic/StrategicApp.tsx:287` (`// ORDER 112 DoD 1 (delimplementation): RoomCardPanel renderas inte`), `frontend/src/strategic/ui/DollhouseFrame.tsx:242` (`// Riktig skepnad-per-klass hör till ORDER 112 §4 (food truck) och …`), `frontend/src/strategic/ui/DollhouseFrame.tsx:460` (sträng `'ORDER 112 §4 — vagn som bakvägg, luckan som öppning, kön på gatan som scen'`), och av ORDER 113-radens explicita "ersätter ORDER 112 §4 som levererade platshållare `SKEPNAD EJ BYGGD`". Utfördes under SD-003 rev. 2 (se separat rad) via commits `8c26f6c`, `219cc86`, `51829af`, `da4711b`, `21bbf43`, `040697e` (2026-08-15/16). Samma defektmönster som SD-001 och SD-003 rev. 2: refererat, kod-verifierbart, men inget instrument i repot. Vision Owner-godkänd 2026-08-29 (ORDER 118 audit) att gå in som `Reference only` med attestation, inte som `Gap — ej utredd`. |
| 017 | *(vakant — endast citerad indirekt via `017a`-varianten)* | **Vakant (ORDER 129 audit)** | — | Sekvenshål utredd 2026-08-29 via ORDER 129. Inget arbete gjordes under numret 017: `grep -rn "ORDER 017 " {frontend,documentation}` = 0 direkta träffar, endast `017a`-referenser i `APPROXIMATION_REGISTER.md`. `git log --all --grep="ORDER 017"` = 0 commits. Sannolikt fantomhål — antingen hoppades numret över eller `017a` var det avsedda numret från början. Fullständig utredning i `documentation/architecture/GAP_AUDIT_017_075.md`. |
| 054 | Skala/kamera/assetpolicy Del A — verbalt utfärdad 2026-08-11 | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-11) | Utredd 2026-08-29 via ORDER 129. Del A stängde sju öppna frågor från ORDER 053 (paviljonghöjd 4,5 m, `PlayerBusiness` 6,5 m-dekomposition, dörr-typologi, personalstängd till 1,70 m, registreringsbord-namngivning m.fl.). Refererad 43 gånger. Attestering: `documentation/architecture/skala-inventering.md:128` ("Stängda frågor (ORDER 054 Del A, 2026-08-11)"), ORDER 053-radens body ("closed by ORDER 054 Del A on 2026-08-11"), `CLAUDE.md` §Enhetskontrakt (1,70 m personal-policy). **Inget dedicerat commit-meddelande** — `git log --all --grep="ORDER 054"` = 0; arbetet ligger under ORDER 053-commits. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 055 | Del A — renderregler för `transparent`-opacity och `castShadow` | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-11) | Utredd 2026-08-29 via ORDER 129. Geometri med `transparent` opacity som kan nå 0 får aldrig ha statisk `castShadow`; skuggkartans depth-pass ignorerar alpha, så en fullt utfejdad mesh stämplar sin silhuett på marken. Toggla `mesh.castShadow` i samma `useFrame` som styr opacity, med samma tröskel som `depthWrite` (typiskt `opacity > 0.5`). Refererad 21 gånger. Attestering: `CLAUDE.md` §Renderregler (hela stycket citerar `ORDER 055 Del A`); referenser i `frontend/src/scene/PlayerBusiness.tsx`, `StrategicScene.tsx`, `FpsProbe.tsx`, `lighting/SunLightRig.tsx`. `git log --all --grep="ORDER 055"` = 0 commits. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 056 | Parametrisk fasadgenerator, Del A–F | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. Del A: dev-only toggles (H-tangent för årstid). Del B: fasadschemat för svenska trähus. Del C+D: fasadbyggare. Del E: deterministiska parametrar från OSM-ID. Del F: instanserad rendering. Refererad 23 gånger, 18 filer i `frontend/src/`. Attestering: `buildFacade.test.ts:1` (`ORDER 056 approval criteria`); referenser i `skala-inventering.md` och blueprints. `git log --all --grep="ORDER 056"` = 0 commits. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 057 | Night lights (Del B), roof colours + eave (Del C), rutigt tak-fix (Del D), humanoid tolerensband 1,55–1,90 m (§3) | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. Del B: night lights (skyState, moon-hemi, lit-glass swap på ~60% flaggade hus). Del C: roof colours bump (tegel, plåt, tjärpapp), eave 0,60→0,40 m. Del D: rutigt tak-fix via per-face-normaler. §3: humanoid tolerensband **1,55–1,90 m**. Refererad 28 gånger. Attestering: commit `c32930d` ("ORDER 057 delvis + ORDER 058", 2026-08-12); `CLAUDE.md` §Enhetskontrakt (`ORDER 057 §3`); `frontend/src/scene/ProceduralFacades.tsx:1`, `PlayerBusiness.tsx:151`. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 058 | Fasadgeometri-invarianter — två buggar fixade i samma commit som 057 | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. (1) Fönster-rotationsbugg (`atan2`-argument 90° fel). (2) Nockspets-bisektorklamp (`MAX_BISECTOR_SCALE=2` för akuta hörn). Refererad 13 gånger. Attestering: commit `c32930d` ("ORDER 057 delvis + ORDER 058", 2026-08-12 — samma commit som 057); `buildFacade.test.ts:341` (`every window centre lies within 0.1 m — ORDER 058 §1`); `buildFacade.test.ts:268` (`roof vertex escapes polygon by — ORDER 058 §2`). Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 059 | Wall-culling workaround (nödrättning) — DoubleSide-bandage tills ORDER 060 reparerar | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. 31/138 OSM-polygoner är CW-vridna medan koden antog CCW överallt. `ensureCCW` normaliserar vridning vid entry; DoubleSide används temporärt för att gömma återstående normal-riktningsbugg. Refererad 13 gånger. Attestering: commit `fdfb8e6` ("ORDER 059: walls invisible on 31 of 138 houses — polygon winding fix", 2026-08-12); `buildFacade.test.ts:391` (`CW-wound OSM polygon produces equivalent wall geometry — ORDER 059 §3`); ORDER 060-radens body ("removes ORDER 059 DoubleSide workaround"). Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 062 | *(vakant — endast referens i planeringsnotat, ingen exekverad ändring)* | **Vakant (ORDER 129 audit)** | — | Sekvenshål utredd 2026-08-29 via ORDER 129. `grep -rn "ORDER 062" {frontend,documentation}` = 1 träff endast (`STRATEGIC_TRACK_MILESTONES_PROPOSAL.md:19` — "The ORDER 062-thread audit surfaced a gap"), en review-session-notat, inte en exekverbar order. `git log --all --grep="ORDER 062"` = 0 commits. Numret nämns i en planeringsnotat som inte materialiserades i kod eller egen order. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 063 | INFRA-1 + INFRA-2 scaffold — URL-hashparametrar, canonical test-poser, headless simulation harness | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. URL-hashparametrar (period, focus, distance, yaw, pitch, roi, poseId), sju canonical test-poser (roof-tegel×morning/lunch, village-strategic×lunch/dinner/evening, lit-window-dinner, sky-morning), headless simulation harness (5 Hz script + reactive scenario), Playwright runner-arkitektur. Refererad 9 gånger. Attestering: commit `b96d4a3` ("feat: ORDER 063 INFRA-2 + INFRA-1 scaffold", 2026-08-12); `frontend/src/testHarness/`, `frontend/src/simulation/__tests__/`. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 064 | INFRA-1 Playwright runner — headless Chromium, per-pose stabilitetsväntan, RGB-läsning | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. Headless Chromium, 1280×720 viewport, per-pose stabilitetsväntan (`window.__nxHarness.ticks ≥ 2`), RGB-läsning, diagnostikmeddelanden på fel. Dev-script `npm run test:visual` (INTE wired i `npm test`). Refererad 2 gånger. Attestering: commit `844319e` ("feat: ORDER 064 — INFRA-1 playwright runner + test:visual script", 2026-08-12); `frontend/scripts/visual-regression.mjs`, `frontend/src/testHarness/pixelSampler.ts`. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 065 | *(fortsatt okänd — spår finns men innehåll ej återvinnbart)* | **Fortsatt okänd (ORDER 129 audit)** | — | Sekvenshål utredd 2026-08-29 via ORDER 129 — spår finns men klassificeringen kan inte fastställas. `grep -rn "ORDER 065" {frontend,documentation}` = 1 träff endast (`ACES_MODEL_FINDINGS.md:95` — `(ORDER 061 opening, ORDER 065, ORDER 067)`, referenslista utan innehåll). `git log --all --grep="ORDER 065"` = 0 commits. Kan vara reserverat och aldrig utfärdat, eller utfärdat verbalt och committat under annat nummer (066 ligger tidsmässigt nära). Klassificeras inte som Vakant — spår finns i en beslutsdokumentation som listar det som en händelse — men innehållet är inte återvinnbart. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 066 | Calibration quad — validering av ACES filmic tone mapper + sRGB-encode-pipeline | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. `MeshBasicMaterial` grå plan (R=G=B=128) i view-space, validering av ACES filmic tone mapper + sRGB-encode-pipeline, analytisk reproduktion av tre.js pipeline-steg, oprovocerad exposure 0,6-normalisering i RRTAndODTFit. Ersatte sky-morning referenspose. Refererad 7 gånger. Attestering: commit `300fba9` ("feat: ORDER 066 — calibration quad + analytic-vs-measured verified", 2026-08-12); `frontend/src/testHarness/CalibrationQuad.tsx`, `calibration.ts`. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 067 | Calibration-quad threshold godkännande + fresh-page runner-fix | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. Intervall `{r,g,b: [158, 162]}`; toleransprincip (subpixel jitter + LSB-avrundning, INTE framtida three.js-ändringar); runner-fix (fresh Playwright-sida per pose så hash-only URL-byte inte bär över tidigare pose:s kalibreringskväd). Refererad 5 gånger. Attestering: commit `db95e0f` ("feat: ORDER 067 — calibration-quad threshold approved + fresh-page runner", 2026-08-12); `frontend/src/testHarness/visualPoses.ts` — ranges committade. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 068 | Harness-kameraposering validering — pitch clamp enforcement + PNG per pose | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. `pitch` clamp enforcement (`pitch ≥ PITCH_MIN = 0,314`), silent-clamping → loud-errors, sex poser pitch flip från negativt till positivt, PNG per pose med ROI-rektangel ritad. Root cause: ORDER 067-poser läste sky (kamera var under mark). Refererad 1 gång. Attestering: commit `384f8b2` ("fix: ORDER 068 — harness clamp + positive pitches + PNG per pose", 2026-08-12); `frontend/src/camera/CameraContext.tsx` + `visualPoses` mirror. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 069 | ROI-korrigeringar + ACES-modell fynd (exposure 1/0,6 pre-RRTAndODTFit) | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. Roof-tegel ROI minskad från 40×40 till 12×24, `village-strategic-lunch` borttagen (blandad yta vid vidsikt), `lit-window` förflyttad. **ACES-fynd:** tone mapper exposure normalisering × (1/0,6) pre-RRTAndODTFit (ej dokumenterad), `MeshStandardMaterial` diffuse ÷ π Lambertian-BRDF. Net: tidigare analys över-predikterade ljushet ~1,9×. Rekommendation: ändra inte takfärgerna utan modellgrund. Refererad 3 gånger. Attestering: commit `d51d07a` ("fix+docs: ORDER 069 — ROI corrections + ACES model findings", 2026-08-12); `documentation/blueprints/ACES_MODEL_FINDINGS_ORDER_069.md`. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 070 | Sex visual-regression thresholds godkända + CI (M0 DoD enforceable) | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. Sex ranges godkända: calibration-quad [158,162], roof-tegel-lunch R[118,124] G[31,37] B[18,24] ±3, roof-tegel-morning R[59,65] G[8,14] B[4,10] ±3, village-strategic-{dinner,evening} R[3,15] G[5,15] B[6,16], lit-window-dinner R[242,248] G[219,225] B[166,172] ±3. **M0 DoD enforceable på CI.** `.github/workflows/ci.yml` tillagd (build + visual-regression jobs). Refererad 5 gånger. Attestering: commit `26f15cb` ("feat: ORDER 070 — six visual-regression thresholds landed + CI", 2026-08-12); `frontend/src/testHarness/visualPoses.ts`, `.github/workflows/ci.yml`. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 071 | *(fortsatt okänd — inline `PixelSampleProbe`-throttle-justering, ingen egen commit)* | **Fortsatt okänd (ORDER 129 audit)** | — | Sekvenshål utredd 2026-08-29 via ORDER 129 — spår finns i kod men klassificeringen kan inte fastställas. Refererad i `PixelSampleProbe.tsx:32` ("ORDER 071 — throttle lowered from every 8th frame to every 2nd") och `ACES_MODEL_FINDINGS.md`. `git log --all --grep="ORDER 071"` = 0 commits. Klassificeras inte som Vakant eftersom kodändringen existerar; klassificeras inte som Reference only eftersom ingen commit-signatur pekar på egen körning. Sannolikt en inline-justering som fick ordernummer i ett kommentarsfält men aldrig fick egen order-genomgång — möjligen del av ORDER 072-arbetet. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 072 | Lit-window uniform ROI + variansmetrik — cross-platform fragility detection | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. ROI förflyttad från (296, 362, 8×8) till (506, 409, 25×25) (100% enhetlig 625 pixlar båda plattformar), `expectUniform` boolean per pose, per-kanal varians (max−min) publicerad, stabilitetsväntan 2→6 ticks (ProceduralFacades day→night swap 4-frame throttle). **Cross-platform fragility detection.** Refererad 8 gånger. Attestering: commit `5153a60` ("feat: ORDER 072 — lit-window uniform ROI + variance metric + doc", 2026-08-12); `frontend/src/testHarness/pixelSampler.ts`, `PixelSampleProbe.tsx`, `visualPoses` mirror. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 073 | *(vakant — endast typkommentarer, ingen exekverad order)* | **Vakant (ORDER 129 audit)** | — | Sekvenshål utredd 2026-08-29 via ORDER 129. Numret nämns i M3-dokumentation som datakläss-egenskap (idle-period ingredient/staff cost accrued). `grep -rn "ORDER 073" {frontend,documentation}` = träffar endast i typkommentarer i `reducer.ts`/`cashReading.ts` (dokumentation, inte order-exekverande). `git log --all --grep="ORDER 073"` = 0 commits. Ingen exekverad ändring är signerad med numret. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 074 | Service-close ledger-posting för collapse-vägen (M3 bugfix) | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-12) | Utredd 2026-08-29 via ORDER 129. Bugfix för ORDER 073 M3: `collapse.fireCollapse` skippade `postServiceSummaryLines` → dag 2+ service-avslutning droppade revenue+ingredient ledger-rader. Fix: flytt av `postServiceSummaryLines` till `cashReading.ts` (circ-dep undvikelse), anrop före day-reset i collapse-vägen, tillägg straggler resets. M3-test tightened: reconciliation ≥55% → 0,98–1,02 + drift < 1500 SEK. Refererad 10 gånger. Attestering: commit `48a9d29` ("fix: ORDER 074 — collapse close now posts service summary ledger lines", 2026-08-12); `frontend/src/simulation/collapse.ts`, `cashReading.ts`. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 075 | M3 drift baseline + M2 Activity Model Report öppnad (6-activity-katalog) | **Reference only (ORDER 129 audit)** | Verbal order (Anders, 2026-08-13) | Utredd 2026-08-29 via ORDER 129. Drift baseline tracking (3-day 1135 SEK), 7-day probe (non-linear sign-flip dag 5 → day-5-mechanism för framtid), M2 Activity Model Report öppnad med 6-activity-katalog (train-service, runner-shift, local-sourcing, wine-tasting, guest-chef, compost-audit). DoD-verifiering, `MorningActivityPanel` UI-form. Refererad 22 gånger. Attestering: commit `1f16fc7` ("docs+test: ORDER 075 — drift baseline tracked + M2 report gate opened", 2026-08-13); `documentation/blueprints/ACES_MODEL_FINDINGS.md` uppdatering, `M2_ACTIVITY_MODEL_REPORT_ORDER_075.md`. Fullständig utredning i `GAP_AUDIT_017_075.md`. |
| 153 | Rename lokal `StaffRole` → `StationRole` i innRoom + nightClubRoom (ORDER 152 §1) | **Executed 2026-08-30** | **Own document** | Mekanisk rename per ORDER 152 §1 (fyra roller i sim, stationer per rum). Två filer rörda: `frontend/src/strategic/scene/innRoom.ts:94` `export type StaffRole = 'kitchen'\|'hallService'\|'host'\|'breakfast'\|'rooms'` → `export type StationRole = …`; `frontend/src/strategic/scene/nightClubRoom.ts:121` `export type StaffRole = 'bar'\|'door'\|'floor'\|'dj'` → `export type StationRole = …`. `StaffStation.role`-fältet i respektive fil (rad 156 resp. rad 180) pekas om till `StationRole`. Kommentar bifogad på var typ om varför bytet (station = plats, inte anställning). **Verifikation:** grep bekräftar att inga externa filer importerar dessa lokala StaffRole-typer (`businessRoom.ts:50, 52` importerar rumsmodulerna som namespaces men refererar aldrig `Inn.StaffRole` / `NightClub.StaffRole`). Ingen namnkollision kvar med sim-lagrets `types.ts:10 StaffRole = värd\|servitör\|kock\|lärling`. **§DoD:** typecheck grön. `git diff main..HEAD` = 2 filer (~10 rader förändring) + registerrad. Egen gren `order-153` från main. |
| 152 | Personalrollernas vokabulär — Vision Owner-beslut på ORDER 151:s fyra spår | **Executed 2026-08-30** | **Own document** | Beslutsdokument (ORDER 139-mönstret: beslut här, implementering i egna order). Ingen kod ändrad (`git diff main..HEAD -- frontend/` = tomt). `documentation/architecture/ORDER_152_PERSONALROLLER_BESLUT.md`. **Fyra beslut:** (1) **Fyra roller i sim, stationer per rum.** `värd/servitör/kock/lärling` bär lön, arbetsbörda, rykte — förblir fyra. Rummens `staffStations` är platser, inte anställningar. Lokala `StaffRole`-deklarationer i `innRoom.ts:94` och `nightClubRoom.ts:121` döps om till `StationRole`. (2) **Kontraktet äger sim→rum-mappningen.** Ny `stationFor(role, class): RoomStation \| null` i `businessRoom.ts`; `InteriorStaff.tsx` slutar räkna ut positioner ur layout och frågar kontraktet i stället. (3) **ROLE_COLOUR vinner över STAFF_UNIFORMS.** Rumsfilernas duplicerade uniform-palett var Designs förslag och prövades aldrig mot silhuettbandet; `ROLE_COLOUR` är kalibrerad genom ORDER 123+127. Rumsfilernas STAFF_UNIFORMS tas bort; silhuett-kontrast-checkarna läser `ROLE_COLOUR`. (4) **Lärlingens hem + STAFF_ROLE_ORDER = två buggar, inte designval.** `INTERIOR.staffHomes` (layout.ts:69) får lärling-post; `STAFF_ROLE_ORDER` (model.ts:40) `['värd', 'servitör', 'kock', 'servitör']` → `[…, 'lärling']` så `makeStaff(4)` ger 1H+1S+1C+1L i stället för 1H+2S+1C. **Rekommenderad ordningsföljd (§5):** 153 (rename), 156 (lärling-fix), 155 (ROLE_COLOUR), 154 (stationFor). **Kvarvarande VO-input krävs** för 154 (24-cellers mappningstabell + eventuellt klass-medveten TeamPanel-hyrning) och 156 (lärlingens position i restaurangen). Egen gren `order-152` från main. |
| 151 | Kartlägg personalrollernas vokabulär — sim vs rumsfiler vs strings | **Executed 2026-08-30** | **Own document** | Ren research, ingen kod ändrad (`git diff main..HEAD -- frontend/src/` = tomt). `documentation/architecture/ORDER_151_PERSONALROLLER_KARTLAGGNING.md`. Följer ORDER 150 §5-öppningen om per-klass staff-role→station-mapping och format-mönstret från ORDER 140:s förarbete (karta först, beslut i egen ordning). **Fyra ordböcker kartlagda:** (a) Sim `StaffRole = värd\|servitör\|kock\|lärling` (`types.ts:10`); (b) `INTERIOR.staffHomes = {värd, servitör, kock}` (layout.ts:69, saknar lärling); (c) sex rumsfiler var för sig med egna station-vokabulär (restaurantRoom: host/server/chef; brewpubRoom: barkeep/brewer/cook/runner; wineBarRoom: sommelier/dj/cook/runner; innRoom: 5 lokal-typade roller + 12 station-ids; foodTruckRoom: window/cook; nightClubRoom: 4 lokal-typade roller + 5 station-ids); (d) UI-labels (`ROLE_LABEL`, `roleLabel`, `roleDescription`, `PREP_ITEM_FOR_ROLE`, `ROLE_COLOUR`) — samtliga fyra värden mot sim-rollerna. **Två kollisioner funna:** (1) `innRoom.ts:94` och `nightClubRoom.ts:121` REDEKLARERAR `export type StaffRole` lokalt med värden som inte överlappar sim-lagrets (5 respektive 4 andra strängar); typkonflikten är tyst eftersom modulerna är separata och sim aldrig importerar från dem. (2) `model.ts:40` `STAFF_ROLE_ORDER = ['värd', 'servitör', 'kock', 'servitör']` — `lärling` saknas i initial-teamet, servitör dubblerad (avsiktligt: 1H+2S+1C som första fyra). **InteriorStaff-läsning kartlagd:** `InteriorStaff.tsx:139-163` beräknar fyra positioner från `layout.entrance/bar.worldPosition/centre` (restaurangens shape via OBB) — läser INTE någon rumsfils `staffStations` och INTE `businessRoom.stations` (ORDER 149:s kontraktsfält). Kanalen sim→rum finns alltså inte i kod; det är fyra restaurangs-hårdkodade positioner klass-agnostiskt. Rumsfilernas `staffStations` är monterade i geometri men okonsumerade av personalvisualiseringen. **Ingen ROLE_STATION_MAP** i koden. §5 sammanfattar kollisioner, §7 listar fyra öppna beslutsspår (StaffRole-utökning, ägarskap för mappningen, STAFF_UNIFORMS vs ROLE_COLOUR, lärling-gapet i staffHomes). Egen gren `order-151` från main. |
| 150 | Fixa interiorLayout-gapet — InteriorGuests läser platser från businessRoom-kontraktet | **Executed 2026-08-30** | **Own document** | Löser ORDER 149:s öppna gap generellt (inte per klass). Ny module-singleton `businessRoomRef` i `frontend/src/strategic/scene/interiorSharedState.ts` — samma pattern som `staffPositionsRef`. Bär `{businessClass, seats, standing, stations, entrance, waitingSpot, capacity}` i värld-XZ. **Skrivare:** `RestaurantScene.tsx` och `BrewpubScene.tsx` — efter att rummet placerats anropar de `resolveWorldPositions(room)` och skriver ref.current, med matchande cleanup (skriver null bara om nuvarande post är egen klass så scener som byter mellan sig inte trampar på varandras ref). **Läsare:** `InteriorGuests.tsx` — `targetFor` tar nu `seats: SlotXZ[]` som parameter (ny signatur); `useFrame` väljer `businessRoomRef.current.seats` när `roomChan.businessClass === sim.businessClass && seats.length > 0`, annars fallback på `layout.seats` (bakåtkompatibel för äldre kod). **Dev-only exponering:** `window.__nxBusinessRoomRef`, `window.__nxGuestPositions`, `window.__nxSeatSource`, `window.__nxSeatSourceLength` — samma tree-shakade `import.meta.env.DEV`-guard som `__nxSimState`. **Playwright-verifiering (`frontend/scripts/order150-seat-placement.mjs`, `reports/order150/seat-placement.json`):** injicerar N seated-gäster med seatIndex 0..N−1, snapp:ar positionsRef[id].cx/cz till `businessRoomRef.seats[seatIndex]` (kringgår ease-flakighet i headless chromium), läser tillbaka. **Resultat per klass:** ölkrogen: seatsInContract=20, source=`businessRoomContract`, sourceLength=20, distinctActualPositions=20, maxDist=0.000 m. kvarterskrogen: seatsInContract=16, source=`businessRoomContract`, sourceLength=16, distinctActualPositions=16, maxDist=0.000 m. Kvarterskrogens 16 därför oförändrade beteendemässigt — gästerna landar nu på KONTRAKTETS positioner (identiska med restaurantens layout eftersom `restaurantRoom.seats` bygger samma möbel-koordinater som interiorLayout gjorde). **Konsekvens för framtida klasser:** gästgiveriets 100 platser och nattklubbens 24 stolar (+126 occupancyAreas) kommer att fungera samma dag scenerna monteras — ingen ny fix per klass. **InteriorStaff:** använder inte `.seats` (bara `.entrance/.bar/.centre` som är byggnads-agnostiska via OBB); ingen migration behövd i denna order. Per-klass staff-role→station-mapping (brewpubs `barkeep/brewer/cook/runner` vs restaurangens `host/server/chef` vs simulationens `värd/servitör/kock/lärling`) blir egen order när det behövs. **§DoD:** typecheck grön, full svit **1044/1044** grön. `git diff main..HEAD` visar 4 filer + playwright-script + report + registerändring. Egen gren `order-150` från main. |
| 149 | Montera ölkrogen via businessRoom-kontraktet — samma mönster som ORDER 144 | **Executed 2026-08-30** | **Own document** | Lägger om `BrewpubScene.tsx` från direktmontering (`createBrewpubRoom()` + `updateBrewpubRoom(room, phase)`) till kontraktet i `businessRoom.ts`: `createRoom('ölkrogen', {width: layout.width, depth: layout.depth}) + updateRoom(room, 0)`. Nu går alla scener för klasser som har `hasSeats` genom samma två anrop, precis som RestaurantScene efter ORDER 144. Motiv i filens header: monteringskod mot rumsfilerna direkt får sex specialfall för sex saker som gör samma sak, och specialfall är där fel gömmer sig (`businessRoom.ts §Varför`). **`brewpubRoom.ts` tillägg:** ny `ZONE_FLOORS`-export som backas av samma `COLOUR.floorDining/floorBrew/floorKitchen`-strängar geometrin monterar från (single source of truth per CLAUDE.md "Mätningar mot det de beskriver"). Kontraktets `measureRoom(room)` rapporterar nu `floorZones: 3` för ölkrogen i stället för 0 (`businessRoom.ts:347` läser `mod.ZONE_FLOORS ?? []`). **Playwright-verifiering (`frontend/scripts/order149-brewpub-mount.mjs`, `reports/order149/mount-report.json`):** startade Vite, öppnade `#playtest=1&business=olkrogen&preset=myBusiness&period=lunch` (och sedan `period=evening`), dismissed name-overlay via `window.__nxSetBusinessName`. Dynamisk import av `brewpubRoom.ts` + `businessRoom.ts` + `InteriorGuests.tsx` (`GUEST_COLOUR`) + `silhouetteContrast.zones.ts` (`FLOOR_ZONES_BY_BUSINESS`) körde i browser-kontext. **Resultat:** `sim.businessClass=ölkrogen`, `contract.seats.length=20`, `capacity=20`, `standing=8` (ej i cap), `measureRoom.floorZones=3`, `resolveWorldPositions.seats.length=20`. **Silhuettbandet mot ölkrogens tre golvzoner:** 9 par (seated/dining/paying × dining/brew/kitchen), samtliga inom `[1.8, 3.6]` (räckvidd 1.842–3.023, min på matsalsgolvet som ORDER 127 §5 justerade för). `ZONE_FLOORS` i brewpubRoom har samma färger som `FLOOR_ZONES_BY_BUSINESS['ölkrogen']` i `silhouetteContrast.zones.ts` — assertion `zonesReplicaAgrees=true`; den duplicerade tabellen kvar från ORDER 123 §5, konsolidering till import blir egen ordning. Injicerade 20 fake `seated`-gäster i sim-state (`seated=20, seatedIds=20`); skärmdumpar i lunchljus + kvällsljus (`brewpub-day-lunch.png`, `brewpub-evening.png`). **Känd pre-existerande gap:** `InteriorGuests` läser `usePlayerBusinessInterior().seats` som fortfarande ger restaurangens 16 positioner — gäst 17–20 hamnar på seats[0] fallback i den vyn. Kontraktet har 20 seat-positioner (bevisat i probe); interiorLayout-anpassning för ölkrogen är separat följdorder. **§DoD:** typecheck grön, full svit **1044/1044** grön. `git diff main..HEAD` visar `brewpubRoom.ts` (ZONE_FLOORS-export, ~15 rader), `BrewpubScene.tsx` (30 rader omskrivning), nytt playwright-script + report + registerändringar. Egen gren `order-149` från main. |
| 148 | Utöka mätregeln i CLAUDE.md — "Mätningar mot det de beskriver" | **Executed 2026-08-30** | **Own document** | Dokumentation, ingen kod rörd (`git diff main..HEAD -- frontend/src/` = tomt). Vision Owner-beslut 2026-08-30: sektionen `## Mätningar mot renderad geometri` (införd av ORDER 138) döps om till `## Mätningar mot det de beskriver` och regelns tillämpningsområde vidgas från rendering till rendering/simulering/specifikation. Regel-text: "En mätning ska läsa samma källa som det den påstår sig beskriva — rendering, simulering eller specifikation. Går det inte ska mätningen redovisa avvikelsen och vilken skillnad den kan ge." De tre operativa kraven (import från samma modul; replikering kräver verifieringsrutin; rapport refererar till samma import eller redovisar avvikelsen) står oförändrade. Motiveringen utökas från tre till fem fall: ORDER 128 (`FLOOR_COLOUR` var skyltblocket, inte interiörgolvet), ORDER 132 (3 156 fönster mätta mot spelarens 1 381), ORDER 135 (OSM-taggens `width` mot `ROLE_SPECS`), **ORDER 145/146** (DEV-observationen läste `state='seated'` som är transient (4 sim-sek) i stället för `seatedIds.length` — kostade två dygns utredning på ett fel som inte fanns), **ORDER 143** (Designs `occupancyAreas` gav 43 personer där specen räknat 15; funnet av Design i egen leverans). Avslutande mening: "de två sista visar att regeln inte bara gäller geometri. En sim-signal och en kapacitetsspec kan vara lika mycket 'rätt tal om fel sak'." **§DoD:** typecheck ej relevant (ren doc-ändring, ingen `.ts`/`.tsx` rörd). Egen gren `order-148` från main. |
| 147 | DevPanel breakdown-suffix + stäng ORDER 124 — Vision Owner-beslut Alt B från ORDER 146 §6 | **Executed 2026-08-30** | **Own document** | Genomför ORDER 146:s rekommendation (Alt B): DevPanel:s `seatStr` utökad med `(S:x O:y D:z P:w)`-breakdown efter `seated=N/cap` för icke-foodtruck-klasser. Foodtruck lämnas oförändrad (dess `seatedLive` räknar `state='eating'`; underskiktet skulle bara duplicera signalen). Etiketten `seated` behållen — den är rätt (`seatedIds.length`, upptagen-räkning). Breakdown gör underskiktet synligt så observatör ser att `S:0` är gäster som hunnit → ordering/dining/paying, inte gäster som saknas. **`DevPanel.tsx` ändringar (25 rader):** ny inline `seatBreakdown`-räkning (loop över `sim.guests`, en pass, fyra räknare), appended till `seatStr` (rad 178) och till `console.info`-format (rad 195). Konsollens `lastLoggedRef`-nyckel utökad med `seatBreakdown` så log:en fångar sub-övergångar (gäst går från `seated → ordering` utan att `seatedLive` ändras). Motivering i kod pekar på ORDER 146-rapporten. Live-verifiering via `frontend/scripts/order146-devpanel-verify.mjs` (samma script som ORDER 146 använde): DevPanel-strängen vid t=~526s visar nu `... waiting=X queue=Y scene=Z seated=16/16 (S:1 O:4 D:11 P:0)` — samma underskikt syns. **ORDER 124 stängs som Executed** — grundorsak till `seated=0/16`-observationen fastställd genom ORDER 145+146 som signal-artefakt (transient `state='seated'` räknad som "sittande"). Sim-lagret fungerar korrekt. `Fynd 1 (seatedIds osynk med guests)` inte reproducerad; `On break`-fyndet (arrivals-under-prep) står kvar som separat spår för framtida order om det återobserveras med rätt signal. **§DoD:** typecheck grön, full svit **1044/1044** grön (ingen ny test — breakdown är UI-detalj utan mekanik-effekt; playwright-verifiering i order146-scriptet räcker som bevis), CI grön. `git diff main..HEAD` visar endast DevPanel.tsx-utökning (25 rader) + registerändringar. Egen gren `order-147` från main. |
| 146 | Observation med rätt signal + DevPanel-panelfrågan — reproduktion av ORDER 124-fyndet + verifiering av DevPanels seat-räknare | **Executed 2026-08-30** | **Own document** | `documentation/architecture/ORDER_146_OBSERVATION_OCH_DEVPANEL.md`. Utredning, ingen kod ändrad (`git diff main..HEAD -- frontend/src/` = tomt). **§1a observation:** ORDER 124-observationen `seated=0/16 waiting=4` reproduceras EXAKT ur ORDER 145-tick-loggen — vid t=550,8s var `seatedIds=16, waitingIds=4, stateCount.seated=0` (13 dining + 1 ordering + 1 paying + 1 annat). Alla 16 platser var faktiskt fyllda; `state='seated'`-räknaren var 0 för att gästerna hunnit gå vidare. **§1b panelfråga besvarad:** DevPanel visar redan `sim.seatedIds.length` sedan ORDER 097 (2026-08-15). Live-verifiering via `frontend/scripts/order146-devpanel-verify.mjs` — startade Vite, körde sim till waiting>0, läste faktisk DOM-text via TreeWalker. Vid t=526s: DevPanel-sträng `... waiting=5 queue=2 scene=8 seated=16/16` (seatedIds=16, stateSeated=1). Panelen visar RÄTT signal. **Slutsats:** ORDER 124-observationen "`seated=0/16` i DEV-panelen" kan **INTE reproduceras** från nuvarande kod. Möjliga rötter: (a) observationen läste annan signal (konsol-log som räknar `state='seated'`, test-utskrift, eller annan panel) och citerade som "DevPanel"; (b) `state.seatedIds` var osynk med `guests` vid observationstillfället (ORDER 124 §6:s "Fynd 1", inte reproducerad); (c) DevPanel-koden ändrades pre-ORDER-097. **Läsbarhetsförbättring befogad ändå:** etiketten `seated` sammanblandas lätt med `state='seated'`. Rapport föreslår tre alternativ (A: byt till `atTable=16/16`, B: visa breakdown `seated=16/16 (S:0 O:4 D:11 P:1)`, C: bara dokumentera nuvarande semantik). Rekommendation: **Alternativ B** — behåll etiketten (bakåtkompatibel), lägg till breakdown så läsaren ser båda skikten. Genomförande i egen order. **Rad 124 uppdaterad** med rekommendation om observationens rot. Egen gren `order-146` från main. |
| 145 | Utredning: seated cap:ar vid 5 medan waiting når 6 — premissen falsk pga fel mätsignal | **Executed 2026-08-30** | **Own document** | `documentation/architecture/ORDER_145_SEATED_CAP_UTREDNING.md`. Utredning, ingen kod ändrad (`git diff main..HEAD -- frontend/src/` = tomt). **RAKT SVAR: ingen av spåren håller. Premissen faller.** Playwright-mätning per tick (`frontend/scripts/order145-seated-cap-audit.mjs`, 20-min lunchservice, 6000 ticks): `max seatedIds.length = 16/16` (alla platser fylls); `max waitingIds.length = 6`; **ticks där waiting > 0 OCH free > 0: 0**. Kön uppstår ENDAST när alla 16 platser är fyllda, töms omedelbart när plats blir ledig. Sim-lagret uppfyller kontraktet. **Grundorsak till felaktig diagnos:** ORDER 144-mätningen räknade `guests.filter(g.state === 'seated').length` som är **transient** per `service.ts:243` (`seated`-tillståndet stannar 4 sim-sek innan → `ordering`). Rätt signal är `state.seatedIds.length` (eller `state='seated' + 'ordering' + 'dining' + 'paying'`). **Spår (a) `deriveStaffAction`:** faller. När waiting>0 kör personalen `order` (45×) + `checkback` (24×) + `idle` (6×) — inte `greet`/`seat`, för alla 16 platser är fyllda. Rätt beteende. **Spår (b) sätt-logik:** faller. `tickGuests` waiting-branch (rad 220-241) anropar SJÄLV `findFreeSeat` + `setGuestSeated` — oberoende av staff-task. Gäster tar sig själva till bord så snart plats blir ledig. **Konsekvens för ORDER 124:** dev-observationen 2026-08-29 "seated=0/16" bör ånyo prövas med `seatedIds`-signalen — `seated=0` kan betyda "alla gäster är i ordering/dining/paying" (normal drift) snarare än "ingen har suttit ner" (verkligt fel). Rad 124 uppdaterad med rekommendation. **§DoD:** typecheck grön, ingen kod rörd, full data i `frontend/reports/order145/tick-log.json` (240 samples × capacity/seatedIds/waitingIds/stateCount/staff × 3). Egen gren `order-145` från main. |
| 144 | Kvarterskrogen monterad via businessRoom-kontraktet — Restaurant.tsx ersatt, twoLayouts löst | **Executed 2026-08-30** | **Own document (mount)** | Första verksamhetsklass monterad via `businessRoom.ts`-kontraktet. **§Mount:** ny `frontend/src/strategic/scene/RestaurantScene.tsx` (kopia av BrewpubScene-mönstret från ORDER 125, men använder `createRoom('kvarterskrogen', ...)` + `updateRoom(room, 0)` via kontraktet istället för direkt `createRestaurantRoom` — det var hela poängen med kontraktet per LEVERANS.md §Monteringsordning). Villkorat på `sim.businessClass === 'kvarterskrogen'`. Placering via `layout.centre` + `-layout.worldAngle` från `usePlayerBusinessInterior()`. Mount:ad i `StrategicScene.tsx` bredvid `<BrewpubScene />`. **§twoLayouts löst:** `RESTAURANT_INTERIOR.bar/kitchen/tables/staffHomes` borttagna ur `content/grythyttan.ts` — grep-verifierat att endast döda `Restaurant.tsx` + `RestaurantActors.tsx` (aldrig JSX-mount:ade) läste dem. Båda döda filerna raderade i samma commit. `interiorLayout.ts` är nu enda-sanningen. **§seated-mätning (ORDER 124-hypotes):** playwright `scripts/order144-seated-waiting.mjs` körd före OCH efter mount, samma 20-min lunchservice, 6000 ticks à 5 Hz, batch 25/`page.evaluate` för realistisk körtid. Resultat före: max seated 5/16 (93 samples med seated>0). Efter: max seated 5/16 (92 samples). **Praktiskt identiskt — Designs twoLayouts-hypotes för ORDER 124 faller.** Rad 124 uppdaterad. Grundorsak till `seated=0` som spelaren observerat på dev-server 2026-08-29 är fortfarande ofastställd — nästa utredning bör titta på §2 (a) `deriveStaffAction`. **§Screenshot:** `frontend/reports/order144/kvarterskrogen-16-kuvert.png` (16/16 seated injicerade via `__nxSimState`-mutation, preset=business). **§Radtypecheck-avvikelser:** RestaurantScene.tsx är ny (ingen deviation från handoff — det är monterings-glue). Handoff/scene byte-identiska. **§DoD:** typecheck grön, full svit **1044/1044** grön, BusinessClass orörd (fyra nycklar från ORDER 140), rummen faktiskt monterade (första gången med businessRoom-kontraktet). `git diff main..HEAD` visar new RestaurantScene.tsx, RESTAURANT_INTERIOR-fält raderade i grythyttan.ts, Restaurant.tsx + RestaurantActors.tsx raderade, StrategicScene mount-line, playwright-scripts + reports. Egen gren `order-144` från main. |
| 143 | Uppdatering av `businessRoom.ts` + `nightClubRoom.ts` från leverans 2026-08-30 12:45 | **Executed 2026-08-30** | **Own document (leverans-placering)** | Två ändrade filer från `nexus-design-2026-08-30-1245/` — övriga 8 var byte-identiska mot handoff/ (ORDER 142) och lämnades orörda. **Uppdaterade i handoff/ (2 filer):** `businessRoom.ts` 12 001 → **13 775 B** (+1 774 B), `nightClubRoom.ts` 44 719 → **44 910 B** (+191 B). **Uppdaterade i scene/ (2 filer):** samma två, byte-identiska med handoff-källan. **businessRoom.ts-diff:** kontraktet känner nu alla sex klasser — `RoomClass`-typen utökad med `'nattklubben'`, `MODULES`/`FACTORY`/`MEASURE`/`UPDATE` fyra rader per, ny `occupancyAreas: any[]` på `BusinessRoom`-interface, uppdaterad `capacity`-dokumentation (foodtrucken 0, nattklubben 150 varav 126 i occupancyAreas, gästgiveriet 100 mot TOTAL_SEATS=16), error-meddelande i `createRoom` utökat. **`import * as NightClub from './nightClubRoom'`** tillagd — transitivt krav som gjorde att `nightClubRoom.ts` **måste** kopieras till scene/ tillsammans med businessRoom-uppdateringen. **`scene/nightClubRoom.ts` ligger oanvänd** i scene/ tills klassen byggs — samma läge som `figureProps.ts` idag (finns men monteras inte). `BusinessClass` saknar `'nattklubben'` så inget kan välja klassen; runtime-imports leder inte till mount. **nightClubRoom.ts-diff (+191 B):** fyra kalibreringar i `occupancyAreas` — `barFront.bounds` krympt från 61 m² (43 personer) till ~24 m² (~15 personer) med förklarande kommentar; `loungeEdge.bounds` X-räckvidd trimmad till 11,4 m; `entryHall.bounds` X+Z trimmad från symbolräknat till explicit `[7.4, 12.2, -3.1, 3.1]`; `entryHall.density` 0,62 → 0,64. Alla är kapacitets-räkne-korrigeringar från Designs visuella verifikation — ingen geometri renderas annorlunda. **Typecheck-avvikelser (ORDER 143):** 4 TS-fel i nightClubRoom.ts krävde samma minimum-touch-patchar som ORDER 142 — (1) rad 358 `maxRatio` → `_maxRatio` (unused param); (2-3) rad 1018-1019 `o.geometry!` + `boundingBox!` non-null-assertions (samma mönster som `restaurantRoom.ts:899-900`). Alla dokumenterade som ORDER 143-avvikelser i koden. **BusinessClass orört:** `'kvarterskrogen' \| 'foodtrucken' \| 'gästgiveriet' \| 'ölkrogen'` oförändrad. **blockingFlags()-utfall efter uppdatering:** 8 stoppande flaggor av 51 totalt (från 5/41 i ORDER 142) — nya: `nattklubben/businessClass`, `nattklubben/standingState`, `nattklubben/capacityModel`. Övriga 5 oförändrade. **§DoD:** typecheck grön, full svit **1044/1044** grön, inga nya BusinessClass-nycklar, rummen INTE monterade. Egen gren `order-143` från main. |
| 142 | Placering av Design-paketet 2026-08-30 — 10 filer i `handoff/`, 8 kopior till `scene/`, kontraktet `businessRoom.ts` på plats | **Executed 2026-08-30** | **Own document (leverans-placering)** | Placerar Claude Designs leverans från `leverans/LEVERANS.md` per Vision Owners instruktion. **Filer i handoff/:** 10 .ts från `leverans/` (byte-verifierade mot LEVERANS.md-tabellen, summa 364 880 B): `brewpubRoom.ts` 35 113, `businessRoom.ts` 12 001 (NY, gemensamt rumskontrakt), `figureProps.ts` 30 148, `figureRig.ts` 29 800, `foodTruckRoom.ts` 52 526 (kraftigt reviderad — tre fordonsvarianter, körläge), `innRoom.ts` 66 020, `nightClubRoom.ts` 44 719 (NY, sjätte klassen), `restaurantRoom.ts` 39 433, `silhouetteContrast.zones.ts` 11 996 (reviderad — bestämd form, iterativ signatur), `wineBarRoom.ts` 43 124. Historiska ORDER-briefer i handoff/ (`ORDER-gaster-och-personal.md`, `ORDER-olkrogen-brief.md`) bevarade som spårbar kontext. **Kopior till scene/:** 8 av 10 filer — `businessRoom.ts`, `restaurantRoom.ts`, `brewpubRoom.ts` (byte-identisk med scen-versionen från ORDER 125, no-op), `wineBarRoom.ts`, `innRoom.ts`, `foodTruckRoom.ts`, `figureProps.ts`, `silhouetteContrast.zones.ts` — alla byte-identiska med handoff-källan. **Uteslutna per Vision Owner-instruktion:** `nightClubRoom.ts` (klassen `'nattklubben'` finns inte i BusinessClass; kontraktet `businessRoom.ts` känner den inte heller — kräver §5-blockerare + fyra rader i MODULES/FACTORY/MEASURE/UPDATE per LEVERANS §2), `figureRig.ts` (redan i scene/ patchad av ORDER 123). **figureRig-diff-rapport:** `leverans/figureRig.ts` (29 800 B) vs `scene/figureRig.ts` (patchad) skiljer **endast** i ORDER 123-patchen — crown `thetaLength 1,15 → Math.PI/2`, cache-nyckel `head:crown → head:crown:hemi`, plus 8 kommentarrader. Ingen övrig skillnad; scene/-versionen bevaras för att inte tappa patchen. Gamla `handoff/figureRig.ts` (31 677 B) från ORDER 121 skiljer sig 1 877 B mot leverans (29 800 B) — Design har städat sin källa sedan förmiddagen. **Typecheck-avvikelser (§ORDER 142 dokumenterade i kod):** 5 TS-fel i Design-koden krävde minimal-touch-patchar i scene/-kopiorna: (1) `three-augmentations.d.ts` utökad med `material?: Material \| Material[]` på Object3D (fixar `figureProps.ts:647`); (2) `figureProps.ts:379` `options?` → `_options?` (unused param); (3-4) `figureProps.ts:465,499` `sh.material.side` → `(sh.material as THREE.Material).side` (Mesh.material är typad Material \| Material[]); (5-6) `restaurantRoom.ts:899-900` non-null-assertions för `o.geometry!` + `boundingBox!` (augmentation gör dem optional; call-site vet via `isMesh`-guard). Alla dokumenterade som ORDER 142-avvikelser i respektive fil. **BusinessClass orört:** `types.ts` `'kvarterskrogen' \| 'foodtrucken' \| 'gästgiveriet' \| 'ölkrogen'` oförändrad (per DoD). **`blockingFlags()`-utfall (LEVERANS §2 föreskriven första körning):** 5 stoppande flaggor av 41 totalt — (1) `kvarterskrogen/twoLayouts` två oförenliga matsalar (RESTAURANT_INTERIOR vs interiorLayout); (2) `vinbaren/businessClass` klassen saknas; (3) `foodtrucken/businessClass` ingen placeringspunkt (hasSeats=false, computePlayerBusinessInterior null); (4) `foodtrucken/routeState` inga rutter/schema; (5) `foodtrucken/counterOrder` gästtillståndsmaskin saknar orderingAtCounter/awaitingCollection. Rapport från `order142BlockingFlags.test.ts`. **§DoD:** filerna på plats, typecheck grön, full svit **1044/1044** grön (ett nytt test), inga nya BusinessClass-nycklar, rummen monterade INTE. Egen gren `order-142` från main. |
| 141 | SD-004 §3.3 preciseras — höjdlåset 1,70 m gäller personal; gäster bär `heightMult` från arketyp | **Executed 2026-08-30** | Verbal order (Anders, 2026-08-30) | Documentation-only. Endast tillägg i `documentation/foundation/SUPERSEDING_DIRECTIVE_004.md` §3.3 under den befintliga 2026-08-29-preciseringen (ORDER 122). Ingen kod rörd (`git diff main..HEAD -- frontend/` = tomt). **Preciseringen:** SD-004 §3.3 sade tidigare "ingen höjdskillnad mellan dem". Vision Owner-beslut 2026-08-30 gör den delen personal-specifik — personalens tre roller ska vara inbördes lika (1,70 m); silhuettskillnad mellan värd, servitör, kock och lärling bärs av form och färg, inte storlek. Gäster bär `heightMult` från sin arketyp. **Motiv (i preciseringen):** silhuettkontraktet skiljer gäst från personal via axelbredd (0,46 mot 0,40 m) och hjässans färg — höjden bar aldrig den skillnaden. Utan preciseringen kan inga gäst-arketyper med annan storlek portas: t.ex. `child`-arketypen med `heightMult 0,72` (ORDER 121 §4-flaggan) skulle bli en 1,22-meter kopia av en vuxen med samma axlar snarare än ett läsbart barn. **Följdorder:** implementering av `heightMult`-läsning i `figureRig.ts`/`InteriorGuests` sker i egen order — den här stänger bara direktiv-frågan. Personalens 1,70 m-lås förblir hårt kodat. Egen gren `order-141` från main. |
| 140 | Vokabulärbyte till bestämd form — `BusinessClass`-nycklar per ORDER 139 §1 | **Executed 2026-08-30** | **Own document (implementering)** | Rent namnbyte, ingen mekanik ändrad. Bytet omfattar alla `BusinessClass`-värden + relaterade konsumenter. **Mappning:** `restaurant` → `kvarterskrogen`, `värdshus` → `gästgiveriet`, `foodtruck` → `foodtrucken`, `ölkrogen` oförändrad. **Filer rörda (produktionskod):** `types.ts` (`BusinessClass`-typen + `BankLoanTier`-typen), `business/businessClass.ts` (`BUSINESS_CLASS_CONFIG`-nycklar), `scene/silhouetteContrast.ts` (`FLOOR_ZONES_BY_BUSINESS`-nycklar), `simulation/service.ts` (`BACKGROUND_TASKS_BY_BUSINESS`-nycklar), `content/strings.sv.ts` (`businessClass`-labels), `testHarness/urlParams.ts` (`parseBusiness` + type-signatur), `simulation/bankMeeting.ts` + `businessProfile.ts` (`KnowledgeClass` + `LoanTier`-värden som mappar till businessClass), `ui/DollhouseFrame.tsx` (`PLACEHOLDER_LABELS`-nycklar), plus alla string-jämförelser (`businessClass === '...'`). **Tester uppdaterade:** `businessClass.test.ts`, `DollhouseFrame.test.tsx`, `foodtruckGuestBana.test.ts` (falskt-positivt path-fix), `order137BackgroundWork.test.ts` (via baseline-JSON-uppdatering). **`bimodality-baseline.json`** nycklar uppdaterade så testet fortfarande hittar matchande cells. **Aliaser bevarade:** `parseBusiness` accepterar gamla namn som aliaser (`restaurant` / `foodtruck` / `värdshus`) så äldre playtest-URL:er inte tystas — externa bookmarks fortsätter fungera. **Metod:** sed på quoted string literals + `business=X`-URL-patterns, sedan TS-typcheck som säkerhetsnät (Record-nycklar utan citattecken fångades som TS-fel och fixades). **§1 DoD grep-verifiering:** noll förekomster av `'restaurant'`, `'värdshus'`, `'foodtruck'` som businessClass-värden i `frontend/src/`. En kvarvarande `'foodtruck'` är folder-path (`ui/foodtruck/`) — inte businessClass-värde. **§2 DoD:** alla fyra klasser går att välja via `#playtest=1&business=kvarterskrogen|foodtrucken|gästgiveriet|ölkrogen` + gamla aliaser fortsätter fungera. **§3 DoD:** full svit **1043/1043** grön; typecheck grön. **§4 DoD:** ingen mekanik ändrad — bara nyckelnamn. `git diff main..HEAD` visar 40 filer i `frontend/src/` + 1 JSON-baseline. Egen gren `order-140` från main. |
| 139 | Sexklassutkastet blir beslut — Vision Owner-beslut 2026-08-30 om namn, storlek, kvarterskrogens roll och vinbarens byggnad | **Executed 2026-08-30** | Documentation-only | Endast dokumentationsändring: `documentation/foundation/vision/UTKAST_SEX_VERKSAMHETSKLASSER.md` skrivs om till struktur-beslut. Ingen kod rörd (`git diff main..HEAD -- frontend/` = tomt). **§1 namn i bestämd form genomgående:** foodtrucken, kvarterskrogen, ölkrogen, vinbaren, gästgiveriet, nattklubben. **§2 kvarterskrogen = dagens 16-platsare:** dagens `Restaurant.tsx` blir kvarterskrogen och behåller 16 kuvert + befintlig kalibrering (ORDER 121, 123, 127, 128, 132, 137). Design-levererad `handoff/restaurantRoom.ts` (16 platser) är dess rum. **§3 vinbaren egen byggnad:** 2026-08-29-beslutet om att dagens lokal blir vinbaren **utgår** — Design har byggt `restaurantRoom.ts` och `wineBarRoom.ts` som separata rum. Kapacitetsändring 16→20 i dagens lokal utgår, loungeytor/DJ byggs i `wineBarRoom.ts`. **§4 progression:** 16 → 20 → 20 → 100 → 150 + foodtrucken (mobil). Tidigare 60-platsaren utgår. Sjunde klass kan tillkomma om steget 20→100 behöver fyllas. **Vad som INTE görs:** `BusinessClass`-typen i `types.ts` (`restaurant | foodtruck | värdshus | ölkrogen`) rörs inte här — vokabulärbytet är egen order. Talen (säkerhetsnivåer, marknadsandelar) väntar fortfarande på INFRA-2. Paviljong-innehåll väntar på Vision Owner-arbete. Nattklubbens rumsfil ännu ej i `handoff/`. Filnamn behåller `UTKAST_`-prefix per Vision Owner-instruktion; ev. rename är egen operation. Egen gren `order-139` från main. |
| 138 | CLAUDE.md-regel — "Mätningar mot renderad geometri" (Vision Owner-beslut 2026-08-30 per ORDER 136 §5) | **Executed 2026-08-30** | Documentation-only | Endast dokumentationsändring: nytt avsnitt i `CLAUDE.md` mellan §Renderregler och §Regler för Claude Code. Ingen kod rörd. **Regeln:** en mätning som påstår något om vad spelaren ser ska läsa samma källa som renderingen, eller uttryckligen redovisa avvikelsen och vilken skillnad den kan ge. Tre operativa krav: (1) konstanter importeras från samma modul renderingen läser dem ur, inte replikeras med "rimliga defaults" eller läses från angränsande källor (OSM-tagg, asset-metadata, spec-fil) som renderingen inte konsulterar; (2) om replikering är oundviklig — t.ex. `.mjs`-script som inte kan importera TypeScript — dokumentera avvikelsen i header och länka till kanoniska modulen, verifieringsrutin väger tyngre än antagande; (3) rapporter refererar till samma import eller redovisar avvikelsen, annars räknas de som obestyrkt. **Motivering (i regeln):** ORDER 128 (silhuett-kontrastband mot skyltblocket istället för golvet), ORDER 132 (fönster-mätning räknade 3 156 vs spelarens 1 381 efter WINDOW_KINDS-filter), ORDER 135 (vägbredd mot OSM-tagg istället för `ROLE_SPECS`). Alla tre gav "korrekt data om fel yta" — farligare än ingen data. Egen gren `order-138` från main. `git diff main..HEAD -- frontend/` = tomt. |
| 137 | Bakgrundsarbetet — implementering av ORDER 134 alternativ B (namngivna bg-uppgifter håller mittspannet) | **Executed 2026-08-30** | **Own document** | `documentation/architecture/ORDER_137_BAKGRUNDSARBETET.md`. **§2 implementation:** fyra namngivna bakgrundsuppgifter (`misEnPlace`, `dish`, `restock`, `clean`) tillagda i `types.ts` `TaskType` och `economics.ts` `TASK_BASE_TICKS` (5-8 ticks per uppgift). I `service.ts`: `BACKGROUND_TASKS_BY_BUSINESS` map — restaurant/värdshus har alla fyra, foodtruck/ölkrogen tomma (§2.3 utanför scope). `pickBackgroundTaskFor` roterar deterministiskt genom typerna (staff-id-hash + simTime/20-fönster). `beginBackgroundTask` kör som direct-tasks men utan guest-target. **§2.2 preemption:** ny `anyDirectTaskAvailable`-check i början av `tickStaff`; om någon staff är i bg-task OCH direct-task är tillgänglig, avbryts bg-tasken samma tick via `completeStaffTask` följt av task-selection. Workload-loopen skiljer bg- från direct-tasks: direct behåller original grow-rate 0,05/s; bg **målsöker** en steady-state 0,4 med approach-rate 0,5/s. Motivering (i kod): §3 förbjuder ansiktsband-trösklar (0,95/0,7), inte rate per task-typ — bg-arbete är faktiskt mindre pressat än att servera en gäst. **§4/5 resultat (order137BackgroundWork.test.ts):** ORDER 134-svepet omkört, jämfört mot committed baseline i `frontend/reports/order137/bimodality-baseline.json`. Restaurant midMass **8,4% → 31,0% (+22,6 pp)** för staff=2/3/4. Värdshus 9,1% → 30,4% (+21,3 pp). Foodtruck 32,0% → 31,5% (−0,5 pp inom tolerans, inga bg-tasks konfigurerade). Ölkrogen 8,4% → 7,6% (−0,7 pp, utanför scope). **§2.2 preempt-invariant:** över hela 8-min-servicen på 2400 ticks: 0/261 waiting-ticks hade också bg-task; max sammanhängande brott = 0 ticks. **§3 verifiering:** `deriveFaces.ts` orörd (0,95/0,7 oförändrade), `arrivals.ts` orörd, `businessClass.ts` `capacityFor` orörd. Presentationslagret (scene/, ui/) helt orört per DoD 7. **DoD 8:** typecheck grön, full svit 1039 → **1042/1042** grön (4 nya tester), CI grön. **ORDER 134 §6-alternativet stängt** — rad 134 uppdaterad från "tre alternativ öppna" till "Alt B genomförd av ORDER 137". Per-fas-bandfrågan (ORDER 131 §6.2) kan nu prövas när fördelningen har en mitt. Egen gren `order-137` från main. |
| 136 | Smalare vägar-mätning + CLAUDE.md-regelförslag — Vision Owner ville se svensk-norm-alternativ mot dagens `ROLE_SPECS` | **Executed 2026-08-30** | **Own document** | `documentation/architecture/ORDER_136_SMALARE_VAGAR_MATNING.md`. Utredning, ingen rättelse. **§3-verifiering:** ingen `ROLE_SPECS` committad (`git diff main..HEAD -- frontend/src/strategic/content/roadRoles.ts` = tomt); ingen guard lagd; ingen produktionskod rörd; `CLAUDE.md` orörd (regelförslaget är text i rapporten, inte lagt in än). **§2.1 norm-jämförelse:** dagens ROLE_SPECS är +1,7 m för primary och +1,9 m för main jämfört med svensk VGU-norm (Rv 244 renderas 13,2 m envelope mot norm 11,5). secondary/local/village/residential är i grov paritet med norm. Roadroles-header säger själv "Visual target, tuned against the localhost screenshots — not a legal survey claim". **§2.2 kollisionssvep:** tre regimer — CURRENT, ALT_A (sidewalks primary/main 1,6/1,5→1,0), ALT_B (primary 10→7, main 9→7, sidewalks krympta). **Kritiskt fynd:** varken ALT_A eller ALT_B minskar antalet kollisioner — kvarstår 32 i alla tre. Endast värsta djup minskar (4,36 → 3,86 → 3,06 m). **Trigger-uppdelning:** 19 av 32 fall (59%) är strukturella (vägens polyline går fysiskt genom byggnadens polygon eller korsar dess edges); 13 är widthOnly (bara envelope-räknare). Bredd är oskyldig för de 19; de 13 kräver envelope < 1-2 m för att stängas. **§2.3 kvarvarande fall vid ALT_B:** listat topp 10, djup 1,28-3,06 m. `vw-pra-19n` (worst), `vw-nyg-3`, `vw-jarn-9` — fall 3-9 påverkas inte alls av bredd-ändring. **§2.4 tre skärmdumpar:** `frontend/reports/order136/roads-{current,alt-a,alt-b}.png` — samma vy (focus 380,15, distans 150, pitch 1,0). Genererade via temporär patch i arbetsträdet + `git checkout -- roadRoles.ts` mellan körningar; patchen committad inte. **§5 CLAUDE.md-regelförslag:** rapport-text för nytt "Mätningar mot renderad geometri"-avsnitt — mätningar ska läsa samma källa som rendering, eller explicit dokumentera avvikelsen. Motivering: tre gånger på två dagar (ORDER 128 golvfärgen, ORDER 132 fönstren, ORDER 135 vägbredden) har mätning läst annan geometri än rendering; alla tre gav "korrekt data om fel yta". Vision Owner beslutar om regeln antas. **§7 följdorderförslag (uppslag, ej rekommendation):** (1) polygon-guard i `OsmRoads.tsx` analog med ORDER 132 — enda åtgärden som strukturellt löser problemet; (2) bredd-justering närmare norm — minskar djup men löser inte antal; (3) OSM-uppströmskorrigering av polylinjer genom hus. **DoD:** alla nio uppfyllda. Egen gren `order-136` från main. |
| 135 | Mäter mätningen samma geometri som renderas? — utredning efter Vision Owner obs 2026-08-30 att ORDER 133:s slutsats inte stämmer med dev-servern | **Executed 2026-08-30** | **Own document** | `documentation/architecture/ORDER_135_MATNING_VS_RENDERING.md`. Utredning, ingen rättelse. `git diff main..HEAD -- frontend/src/` = tomt. **Rakt svar:** Nej. ORDER 133 använde `road.width` (OSM-tagg) om finns, annars kind-defaults (`defaultRoadWidth()` i `order130-map-measurements.mjs`). Renderingen använder `ROLE_SPECS[roleFor(road)].width` i `frontend/src/strategic/content/roadRoles.ts:104` + valfri `sidewalkWidth`, aktiverat i `OsmRoads.tsx:172` (`const half = spec.width / 2`). **OSM-taggens `width` ignoreras helt av renderingen.** Skillnaden är genomgående: primary Rv 244 renderar 13,2 m envelope (10 m + 1,6 m × 2), ORDER 133 mätte 8 m mittlinje — differens +5,2 m per sida. Alla 6 primary-segment, alla 6 main-segment, samtliga secondary_connector och village_street skiljer sig med 3-7 m från mätningen. **§2.3 tre-regims-mätning:** A. ORDER 133 (som rapporterat): 37 byggnader, värsta 3,48 m. B. Renderad carriageway: 30 byggnader, värsta 2,86 m. C. Renderad envelope (med trottoar): **32 byggnader, värsta 4,36 m**. **§2.4:** 3 nya kollisioner i C som ORDER 133 missade helt (`vw-pra-18` apartments 2,57m mot Lokavägen, `w1422745011` 1,83m mot Kyrkogatan, m.fl.). 8 fall som ORDER 133 rapporterade (kringliggande Prästgatan) försvinner i renderingen — de var falska på grund av att OSM-taggen 12 m aldrig når renderingen. **Reviderade slutsatser:** (1) "89 % är verkliga smågränsöverlappningar under 1 m" är **falskt** — 8 av topp 10 är över 1,5 m i envelope-mätningen, värsta 4,36 m; (2) "kod-parametrar är inte huvudorsak" är **falskt** — `ROLE_SPECS`-tabellen ÄR huvudorsaken; (3) "OSM-datafel driver 4 av 37" (Prästgatan 12 m) är **falskt** — Prästgatan påverkar aldrig renderingen. **§6 följdorderförslag (som uppslag):** (1) uppdatera mätskriptet att läsa `ROLE_SPECS`-bredder, kör om baseline; (2) polygon-guard i `OsmRoads.tsx` analog med ORDER 132:s windowsFor-guard (klippa envelope inkl. sidewalk mot polygon, inte bara centerline); (3) OSM-fel-taggar (12/13 m på residential/living_street) kan rapporteras uppströms — påverkar inte rendering men förbättrar data-integritet. Ingen rekommenderas. **DoD:** alla nio uppfyllda. Egen gren `order-135` från main. Rad 133 uppdaterad med not om reviderad slutsats. |
| 134 | Bimodaliteten — utredning av ORDER 131 §6.1 (staff.workload är antingen ~0 eller ~1) | **Executed 2026-08-30** | **Own document** | `documentation/architecture/ORDER_134_BIMODALITETEN.md`. Utredning, ingen kalibrering. `git diff main..HEAD -- frontend/src/` = tomt utom ny testfil. **Metod:** `frontend/src/strategic/business/__tests__/order134Bimodality.test.ts` — 4 verksamheter × 3 staffCount × 50 seeds = 600 unika sim-körningar, ~11 s körtid. staffCount ändrad via officiell `SET_POLICY`-action (triggerar `needsStaffRebuild` per `reducer.ts:2251`). **§2.1 — ankomster vs tilldelning:** ankomstfrekvens jämn (0,022 arrivals/tick restaurant, 0,013 foodtruck ≈ Poisson med låg varians, ingen klungighet). **Tudelningen ligger i tilldelningen, inte ankomsterna.** Rate-konstanter (workload +0,25/s upp när aktiv, −0,15/s ned när idle) driver pinning: så snart task tar tag går workload till 1 och stannar tills kön är tömd (vilket den nästan aldrig är i restaurang under aktiv service). **§2.2 — lägeslängder:** restaurang HÖG-läge p50 = **286 s (~4,8 min)** — mer än halva 8-min-servicen. Foodtruck HÖG p50 = **10 s** — 30× kortare. MITT-läge p50 = 8-9 s för alla (övergångstiden är strukturellt kort). **§2.3 — staffCount-svep:** restaurant midMass 8,4% (staff=2) → 8,3% (staff=3) → 8,3% (staff=4) — **< 0,2 procentenhet skillnad**. Foodtruck 32,0% oavsett count. **Bemanning löser inte formen** — varje ny person bidrar med samma bimodala kurva. Notera: `makeStaff` accepterar bara 2\|3\|4 — modellen begränsar bemanning strukturellt. **§2.4 — foodtrucken:** ja bimodal men mycket mindre (32% mittmassa vs 8% för restaurang). Kortare lägeslängder matchar task-flödet (varje order 20-30s expedierad ≈ workload-cykel 10s). **Slutsats (§4):** blandad — foodtrucken är trogen (diskreta korta tasks), restaurang är förenklad (verkliga kök har bakgrundsarbete som håller workload i 0,3-0,6 mellan direkta tasks; simmen saknar detta lager). **Tre alternativ till Vision Owner (rapportens §8):** (A) acceptera bimodalitet som modellens sanning, ansiktsvokabulär beskriver två lägen; **(B) bygg ut task-modell med bakgrundsarbete** — **VALD av Vision Owner 2026-08-30, genomförd av ORDER 137**: restaurant midMass 8,4% → 31,0% (+22,6 pp), foodtruck 32,0% → 31,5% (oförändrad); (C) tvåfas-läsning där `hurried` mäter tid-i-hög över fönster. **§6 följdorder-not:** ORDER 131 §6-punkt 2 (per-fas-band, dinner vs lunch +8-16pp) **avråds** tills bimodaliteten är designbeslutad — per-fas-band flyttar bara gränsen mellan två dominerande lägen, löser inte formen. **§3-verifiering (förbud):** `deriveFaces.ts` orörd (0,95 + 0,7 oförändrade), `arrivals.ts` orörd, inget värde föreslås. **§5 DoD:** alla tio uppfyllda. Typecheck grön, full svit 1038 → 1039/1039 grön (ett nytt test). Egen gren `order-134` från main. |
| 133 | Vägarnas bredd — utredning av ORDER 130 §3.1-fyndets 37 hus-vs-vägar-överlapp | **Executed 2026-08-30 — slutsatser REVIDERADE av ORDER 135 (2026-08-30)** | **Own document** | ⚠️ **Slutsatserna nedan är fel:** mätningen använde OSM-taggar respektive kind-defaults, men renderingen använder `ROLE_SPECS[roleFor(road)].width` + sidewalk från `roadRoles.ts` — en helt annan tabell. ORDER 135 räknade om och fann 32 kollisioner med värsta 4,36 m (inte 37 med värsta 3,48 m), och de "verkliga" fallen är mestadels 1,5-4 m intrusion (inte < 1 m). Se `documentation/blueprints/ORDER_135_MATNING_VS_RENDERING.md`. Ursprungliga rapporten kvar för spårbarhet men slutsatserna om placering/data/parameter är inte pålitliga. — `documentation/architecture/ORDER_133_VAGARNAS_BREDD.md`. Mätning, ingen rättelse (ingen vägbredd ändrad, ingen byggnad flyttad, ingen OSM-data redigerad). `git diff main..HEAD -- frontend/src/` = tomt. **§2.1:** 12 m-bredden på living_street kommer **direkt från OSM-datan** — `w122157691` Prästgatan har `width: 12` i `grythyttan-osm.json`. Vår preprocessing översätter, uppfinner inte. Ytterligare tre residential-vägar (`w287143007`, `w1329020079`, `w1329020081`) med `width` 10/10/13 m — sannolikt OSM-datafel (living_street-norm är 3-5m, residential 4-6m). **§2.2:** bredd per kind rapporterad. secondary/tertiary rätt-taggade (12/12+7/7 vägar, alla ≤ 1,5× norm). residential 25/53 taggade (4 utstickare). service 1/134 taggade (defaultar till 3m — rimligt). path/track/cycleway/footway 0 taggade (defaults 1-2m matchar norm). Kod-defaults inte fel. **§2.3:** Baseline 37 → Normed 33 → **4 kollisioner försvinner** med kind-normer, alla samlade kring Prästgatan (`w869907976`, `w869907977`, `vw-bv-4`, `vw-pra-8`). **§2.4:** 33 kvarvarande listade individuellt i rapportens §5 — värsta `vw-nyg-3` (1,98m) mot tertiary(5,5m), `vw-jarn-9` (1,75m) mot residential(5m). 13 fall under 0,09m kant-i-kant. **Slutsats (hypotes, ej diagnos):** ~11% OSM-taggfel (4/37), ~89% verkliga smågränsöverlappningar i tätbebyggda kvarter (33/37). Kod-parametrar och placering (transform) är INTE huvudorsaker. **§5 om något inte går:** Ja, Prästgatan är sannolikt OSM-fel. Rekommenderar tre alternativ till Vision Owner: (A) respektera OSM, (B) normalisera i preprocessing, (C) rätta uppströms i OpenStreetMap — designfråga utanför denna orders scope. **§4 DoD:** alla nio uppfyllda. Mätskript `frontend/scripts/order133-road-width-audit.mjs`, rapport i blueprints/, typecheck grön, full svit orörd (inga nya tester — bara mätning). ORDER 130 §6-punkt 2 är utredd — rad 130 uppdaterad med not. Egen gren `order-133` från main. |
| 132 | Fönstren mot polygonen — polygon-guard i `windowsFor()` (löser ORDER 130 §3.2-fyndet) | **Executed 2026-08-30** | **Own document** | `documentation/architecture/ORDER_132_FONSTREN_MOT_POLYGONEN.md`. Följdorder till ORDER 130 §6-förslag 1. **§2 lösning:** ny import av `inside` från `procgen/geom.ts` + `inFootprint`-hjälpare i `windowsFor(b: Extruded)` som kasserar fönster vars XZ-projektion faller utanför `b.building.poly`. Samma **strategi** som ORDER 058 §1 (buildFacade) — den itererar polygonens kanter och undviker felet strukturellt; här behålls OBB-iterationen (för att OBB används på fler ställen — tak, ridge, volym) och utfallen kasseras efteråt per ORDER 132 §2 ("uppfinn ingen ny strategi"). **§3 orört:** polygonerna (OSM-data), OBB-beräkningen (`orientedBbox` i `geom.ts`), `buildFacade` (LOD 0/1) — `git diff main..HEAD -- frontend/src/lib/facade/ frontend/src/strategic/procgen/geom.ts` = tomt. Hus-mot-väg-felet (ORDER 130 §3.1) rörs inte — egen order, annan orsak. **§4-verifiering:** ny mätscript `frontend/scripts/order132-verify.mjs` filtrerar med samma `WINDOW_KINDS`-set som produktionen (`house, detached, residential, apartments, hotel, school, university, train_station, commercial`) och rapporterar per-byggnad före/efter. **Resultat:** 149 hus i WINDOW_KINDS, 2 556 fönster genererade → 1 175 kvar efter guard → **1 381 droppade** (54% av genererade var utanför polygonen). **0 byggnader tömda på fönster** — DoD-punkt 4 uppfylld. Skillnaden mot ORDER 130:s 3 156-siffra är att ORDER 130 mätte alla 338 byggnader inklusive de som inte renderar fönster i produktion (shed, outbuilding, church, yes m.fl.); ORDER 132:s siffra är den relevanta för spelaren. **§4-punkt 5 skärmdumpar:** `frontend/reports/order132/worst-window-{before,after}.png` — samma vy som ORDER 130 (`w193810921` universitetet @ (407, −89), distans 80m). Before: fönster hänger 20-30 m från fasaden; after: bara fönster som ligger inom polygonen. **§4-punkt 4 test:** `frontend/src/strategic/scene/__tests__/windowGuard.test.ts` — 3 tester som hävdar (a) 0 byggnader har alla fönster droppade, (b) representativa hustyper (house/residential/apartments/hotel/school/university) har ≥ 1 fönster kvar, (c) totalen droppade > 1000 (sanity att guarden är aktiv). Testet läser `guardVerify.json` — beror på att verifieringen körts först. **DoD 7:** typecheck grön, full svit **1035 → 1038/1038** grön (3 nya tester), CI grön. **DoD 8:** ORDER 130-radens rapporterade fynd "88% av byggnaderna har fönster utanför fasad" är nu stängt — rad 130 uppdaterad med not om ORDER 132-lösningen. Egen gren `order-132` från main. |
| 131 | Load-svep per verksamhet — 200 seeds × 2 services × 4 verksamheter, underlag till per-verksamhet-band-frågan | **Executed 2026-08-30** | **Own document** | `documentation/architecture/ORDER_131_LOAD_SVEP.md`. Mätning, ingen kalibrering. Bygger vidare på ORDER 111 §5 (`loadMeasurement.test.ts`) från 3 punkter till fullständig fördelning. **Metod:** `frontend/src/strategic/business/__tests__/order131LoadSweep.test.ts` — 200 seeds × (lunch, dinner) × (restaurant, foodtruck, värdshus, ölkrogen) = 1600 unika sim-körningar, 460 000 samples per cell, körtid ~25 s. Dinner-mätning kräver att simmen först tickas igenom en primär-lunch (2 min) tills `state.day.period === 'afternoon'` (reducer.ts:992-guard). **§5-svar:** NEJ, bandet 0,95 beskriver inte alla — restaurant/värdshus/ölkrogen är över 0,95 i **56–70%** av tiden, foodtruck i **17–33%**. Restaurant och ölkrogen statistiskt identiska (bekräftar ORDER 125 §4). **Bimodal fördelning:** staff är antingen ~0 eller ~1, sällan i mellanspann — formskillnad ORDER 111 antydde bekräftas. **Per-fas (§3):** alla fyra har högre workload på dinner än lunch; foodtruck-skillnaden störst (+0,32 p50). Restaurant qMax dinner = 6,9 (störst pressent). Breakfast-fas för värdshus inte mätt (breakfast triggras internt av overnight-cykel, kräver fullt dygn). **§4-verifiering:** `deriveFaces.ts` byte-identisk med main (`diff` tomt), 0,95/0,7-tröskelvärden orörda, ingen `capacityFor`-siffra ändrad, ingen sim-lager-kod rörd, inget värde föreslås. `git diff main..HEAD -- frontend/src/` visar endast ny testfil. **§6 DoD:** alla nio uppfyllda — 200 seeds redovisade, sex percentiler (p10/25/50/75/90/99), andel över 0,95 och 0,7, histogram i rapport + JSON, per-fas-fördelning, svepet incheckat som körbart vitest-test (`npx vitest run order131LoadSweep`), grep-check tröskel-tabell orörd, typecheck grön, registerrad i samma commit. **Följdorder-uppslag (§6):** (1) bimodalitetsdiskussion — design-fråga om arrivals-schemat är för spikigt — **utredd av ORDER 134 (2026-08-30)**: tudelningen ligger i tilldelningen (inte i arrivals som är jämna 0,022/tick), staffCount ändrar inte formen (< 0,2 pp mellan 2/3/4), restaurang HÖG-perioder = 5 min, foodtruck 10 s. Tre alternativ till Vision Owner (acceptera / bygg ut task-modell / tvåfas-läsning). (2) per-fas-band-frågan (lunch vs dinner +8–16 procentenheter i `>0,95`) — **avråds av ORDER 134** tills bimodaliteten är designbeslutad; per-fas-band flyttar bara gränsen mellan två dominerande lägen, löser inte formen. Ingen rekommenderas här — rapporten är underlag. **Kroppsrapport:** `documentation/blueprints/ORDER_131_LOAD_SVEP.md`. **Data:** `frontend/reports/order131/loadSweep.json` (config + 8 celler med percentiler + histogram). Egen gren `order-131` från main. |
| 130 | Kartan mäts — hus mot vägar, fönster utanför fasad (utredning per Vision Owner obs 2026-08-29) | **Executed 2026-08-29** | **Own document** | `documentation/architecture/ORDER_130_KARTAN_MATS.md`. Mätning, ingen rättelse. Ingen geometri/transform ändrad (`git diff main..HEAD -- frontend/src/` visar bara nya `scripts/order130-*.mjs`). **Resultat — §7 utlöst:** **297/338 byggnader (88%) har minst ett fönster utanför sin polygon** — 3 156 fönster totalt hänger fritt, upp till 36,3 m från fasaden. Detta är inte enstaka fall utan systematiskt fel. **Rotorsak identifierad utan att rätta:** `windowsFor()` i `OsmBuildings.tsx:1152` placerar fönster på OBB-facen (`ridgeW`/`ridgeD`), inte på polygon-edgen. För varje icke-perfekt-rektangulärt hus hänger fönstret där OBB-facen sticker ut. Samma guard som ORDER 058 §1 lade för `buildFacade`-vägen saknas i OsmBuildings LOD-2. **§3.1 hus mot vägar:** 37/338 (10,9%) byggnader skär minst en vägbanan. Fördelat spritt över byn (inte kvarterssamlat). Vägtyp: service 24, residential 12, living_street 7. Värsta överlapp: `vw-torget-east-barn` 3,48 m in i living_street(12m). Sidoupptäckt: fyra värsta fallen involverar `living_street` med bredd 12 m — sannolikt OSM-tag-fel eller preprocessing-parameter (living_street-norm är 4-5 m). **§3.3 mönsteranalys:** hus-vs-väg-fel spritt (inte transform-typiskt växande med avstånd) — troligen dataproblem. Fönsterfel är 88% täckning — kan inte vara datafel eller transform, är genereringen. Två oberoende fel. **Playwright §4:** tre skärmdumpar i `frontend/reports/order130/` — village-overview (1200m, pitch 78°), worst-house-vs-road (`vw-torget-east-barn` @ (65,15)), worst-window-outside (`w193810921` university @ (407,-89), 36m överhäng). PITCH_MAX=1,361 rad (78°) per `GRAY_BOX_CAMERA` — ORDER 068 låser den, poserna respekterar det. **§6 DoD:** alla nio uppfyllda. Mätskript körbart (`node frontend/scripts/order130-map-measurements.mjs`), Playwright-scriptet körbart (`node frontend/scripts/order130-map-screenshots.mjs`), rapport i `documentation/blueprints/ORDER_130_KARTAN_MATS.md`. Typecheck grön; ingen produktionskod rörd så full svit påverkas inte. **Följdorder föreslagna (rapport §6):** (1) polygon-guard i `windowsFor` (låg risk, 3156 fall stängs) — **stängd av ORDER 132 (2026-08-30)**; efter guarden är WINDOW_KINDS-rendererade fönster utanför polygon = 0 (1 381 av 2 556 droppade av guarden, 0 byggnader tömda); (2) utredning av living_street 12m-bredder (design-fråga, SD-002-relaterad) — **utredd av ORDER 133 (2026-08-30)**: 12m kommer från OSM-datan direkt (`w122157691` Prästgatan). 4/37 kollisioner försvinner med kind-normer, 33 kvarstår som verkliga smågränsfall i tätbebyggda kvarter. Kod-parametrar inte huvudorsak. Vidare rekommendation (respektera OSM / normalisera preprocessing / rätta uppströms) är designfråga hos Vision Owner. Egen gren `order-130` från main. |
| 129 | De 21 hålen — audit av ORDER 017 och 054–075 per ORDER 118 fynd A2 | **Executed 2026-08-29** | **Own document** | `documentation/architecture/ORDER_129_DE_21_HALEN.md`. Docs-order, ingen produktionskod rörd (`git diff main..HEAD -- frontend/` = tomt). **Resultat:** 16 Reference only (054, 055, 056, 057, 058, 059, 063, 064, 066, 067, 068, 069, 070, 072, 074, 075) — arbete committat, ingen orderfil. 3 Vakant (017, 062, 073) — inget arbete gjordes. 2 Fortsatt okänd (065, 071) — spår finns men innehållet ej återvinnbart. Inget gissat, inga rader dolt bakom "troligen". **Mönster:** alla 16 Reference only-nummer är verbala ordrar som committades utan orderdokument — samma mönster ORDER 118 kartlade för intervallet efter 102, här bekräftat att det gäller *bakåt* också från 054. Systemförklaringen: orderboken användes bara för arkitektur/design (043–052); implementations-ordrar (053+) utfärdades muntligt och committades direkt. Detta är inte 16 enskilda hål utan ett arbetsmönster. **12 commit-sha:er verifierade** (c32930d, fdfb8e6, b96d4a3, 844319e, 300fba9, db95e0f, 384f8b2, d51d07a, 26f15cb, 5153a60, 48a9d29, 1f16fc7). **Fullständig rapport:** `documentation/architecture/GAP_AUDIT_017_075.md` med attestering per nummer och meta-mönsteranalys. Alla 21 registerrader uppdaterade från "Gap — ej utredd" till Vakant / Reference only / Fortsatt okänd i samma commit. Sortering (ORDER 118 fynd B1) orörd — egen order. Egen gren `order-129` från main per CLAUDE.md §Grenar-är-kortlivade. |
| 128 | Rättelse — rad 123:s FLOOR_COLOUR pekade på skyltblocket, inte interiörsgolvet | **Executed 2026-08-29** | Documentation-only | Endast registerändring: en mening tillagd i slutet av rad 123 som noterar att ORDER 123:s `FLOOR_COLOUR = '#a89577'` var skyltblocket i `Restaurant.tsx:108`, inte spelarens interiörsgolv (`INTERIOR_FLOOR_COLOUR = '#a08462'` i `PlayerBusiness.tsx:75`). Kalibreringen skedde alltså mot fel yta. Fel identifierat + rättat av ORDER 127 (som ersatte referensen med `FLOOR_ZONES_BY_BUSINESS`). Utan denna rättelse hade rad 123 påstått en kalibrering som inte var mot rätt yta. Egen gren `order-128` från main, ingen kod rörd. |
| 127 | Bandet blir zonmedvetet — löser ORDER 125 §7-fynden, låser upp DoD 8 | **Executed 2026-08-29** | **Own document** | `documentation/architecture/ORDER_127_BANDET_BLIR_ZONMEDVETET.md`. Lyder under SD-004 §3.3 med preciseringen 2026-08-29 (ORDER 122). **§3.1 zonregister:** ny `FLOOR_ZONES_BY_BUSINESS`-konstant i `silhouetteContrast.ts` — restaurant `['#a08462']`, värdshus `['#a08462']`, foodtruck `[]` (SVG-scene), ölkrogen `['#a49b8a', '#7d776c', '#948f84']`. Golvfärgerna som data, inte spridda konstanter. Sidoupptäckt: gamla `FLOOR_COLOUR = '#a89577'` var skyltblocket i `Restaurant.tsx:108`, inte spelarens interiörsgolv (`PlayerBusiness.tsx:75 INTERIOR_FLOOR_COLOUR = '#a08462'`). Konstanten behållen som `@deprecated` för bakåtkompatibilitet. **§3.2 uttömmande kontroll:** ny `paletteZoneCheck(hex, businessClass)` returnerar per-zon-resultat + `allInBand`-bool; färg måste hålla bandet mot **alla** zoner i klassen. Test i `paletteContrast.test.ts` iterar över (restaurant, värdshus, ölkrogen) × (roller, SEATED_STATES) — regressionsvakter bevisar att gamla servitörsfärgen `#6b6260` faktiskt fångas som brytande, inte döljs. **§3.3 palett-justering:** `ROLE_COLOUR.servitör` bytt **`#6b6260` → `#454a52`** (dark cool grey, L 0.069). Nya kontraster: restaurant 2.52:1, ölkrogen dining 3.24:1, brew 2.01:1, kitchen 2.77:1 — alla i band. Parvis ΔE bevarad: värd↔servitör 15.18, servitör↔kock 32.48, servitör↔lärling 54.21 (alla över 12-tröskeln). **§4 orört:** `brewpubRoom.ts` byte-identisk med handoff (`cmp` bekräftat, `git diff main` tomt); figurmått oförändrade; bandkonstanter oförändrade (1.8/3.6/12). **§5-beslut för `paying`:** JUSTERA (inte acceptera avvikelse). `#e8c99e` → `#edd0a3` (L 0.612 → 0.659). `dining` `#ebcda2` → `#edcfa4` samma resonemang. Motivering i kod: en 0.06-avvikelse utan behandling blir en glömd avvikelse (§5). Progressionen seated→paying flättas något men bandet är kontraktet, inte den narrativa gradienten. **§6 DoD:** alla elva uppfyllda. **DoD 9 (playwright):** `frontend/scripts/order127-brewpub-lighting.mjs` mäter ölkrogens scen i båda ljus — dagsljus 8 821 pixlar diff (bbox 915×289 CSS-px), kvällsljus 2 158 pixlar diff (bbox 915×266). Skärmdumpar `frontend/reports/order127/brewpub-{baseline,with-bodies}-{lunch,evening}.png`. **Tester:** paletteContrast.test.ts omskriven till zon-baserad iteration (48 assertions över tre klasser × fyra roller + fyra SEATED_STATES). `brewpubContrast.test.ts` raderad (redundant efter zon-iteration). Silhouettetester oförändrade. Full svit 1042 → **1034/1034** grön (netto −8 tester efter att 25 brewpubContrast redundant-check ersatts av 40 uttömmande zon-tester + 8 nya regressionsvakter). Typecheck + build gröna. **ORDER 125:s öppna fynd stängda:** rad 125 uppdaterad — §7-fynden lösta av palett-justeringen; DoD 8 uppfylld via ORDER 127:s playwright-verifikation. Följdorder-not på ORDER 125-raden om "DoD 8 uppskjuten" utgår. |
| 126 | Städning efter presentationsarbetet — registerhål, filer i roten, motstridigt utkast | **Executed 2026-08-29** | **Own document** | `documentation/architecture/ORDER_126_STADNING_EFTER_PRESENTATIONSARBETET.md`. Docs- och filhanterings-order, ingen produktionskod rörd. **§2 registerhål stängt:** rad 121 utökad med not om `frontend/src/strategic/scene/three-augmentations.d.ts` — projektbred TypeScript-augmentation av `THREE.Object3D` med valfria `isMesh?`/`geometry?` som `@types/three` inte exponerar. Samma defektmönster ORDER 118 kartlade (kod i main utan registerrad), här upptäckt samma dag. **§3 filer i roten:** `ORDER_121_KROPPAR_I_SCENEN .md` (mellanslag före ändelsen) borttagen efter `git log --all --` verifierade 0 träffar i historiken; `BRIEF_DESIGN_OLKROGEN.md` befinns redan borta från roten (flyttades fysiskt till `handoff/ORDER-olkrogen-brief.md` under ORDER 125-placeringen, den filen ligger committad på `order-125`-grenen — innehåll ej förlorat, §7 andra clausul uppfyllt). `ORDER_RAKNAREN_OCH_KONSOLEN.md` lämnad orörd. **§4 utkasten:** `UTKAST_SEX_VERKSAMHETSKLASSER.md` flyttad från roten till `documentation/foundation/vision/`. Äldre `UTKAST_VERKSAMHETSKLASSER.md` (premiss "fem restauranger i olika storlek") märkt som ersatt 2026-08-29 via header-not, substans orörd — samma mönster som `SUPERSEDING_DIRECTIVE_003.md` v1.0. **§5 avgränsning:** inga beslut fattas, 21 nummerhål (ORDER 118 fynd A2) orörda, sortering (fynd B1) orörd, `handoff/` orörd. **§6 DoD:** alla sju uppfyllda — registerrad 121 utökad, §3-filer borttagna efter historik-verifikation, RAKNAREN orörd, sexklassutkastet placerat + gamla märkt, `git status --short` visar bara `ORDER_RAKNAREN_OCH_KONSOLEN.md` som otrackad i roten, `git diff main..HEAD -- frontend/` tomt (ingen produktionskod rörd), registerpost 126 i samma commit. Egen gren `order-126` från main per CLAUDE.md §Grenar-är-kortlivade. |
| 125 | Ölkrogen — ny verksamhetsklass som rum (20 platser + bryggeri i samma rum) per SD-004 | **Executed 2026-08-29** — §7-fynden lösta av ORDER 127 (zonmedvetet band + palett-justering); DoD 8 uppfylld via ORDER 127:s playwright-verifikation | **Own document** | `documentation/architecture/ORDER_125_OLKROGEN.md`. Lyder under SD-004. Källa: `handoff/brewpubRoom.ts` (Claude Design, 900 rader) + `handoff/ORDER-olkrogen-brief.md`. **§2 kopia:** `brewpubRoom.ts` läggs som `frontend/src/strategic/scene/brewpubRoom.ts`, ingen import från `handoff/`. Avvikelser dokumenteras (jfr ORDER 121). **§3 klassen:** `BusinessClass` utökas med ölkrogen; `capacityFor = 20`; rummet monteras när klassen är vald (parallelt med `Restaurant.tsx`); `resolveWorldPositions()` används — sim-lagret gör inte om transformen. **§4 avgränsning:** ingen egen mekanik, ölkrogen beter sig som restaurangen; ingen omdöpning av dagens restaurang; inga beslut om progression/kunskapskoppling; `ui/foodtruck/` + `figureRig.ts` orörda. **§5 fem flaggor från Design:** `businessClass` (åtgärdas här), `counterOrder`, `standing`, `brewPhase`, `kitchenStations` (fyra kräver ingen åtgärd i den här ordern). **§6 DoD (13 punkter):** brewpubRoom.ts placerad, ölkrogen valbar, resolveWorldPositions används, figurer via figureRig, walkPath/exitPath test, measureBrewpubRoom mot MIN_WIDTH/MIN_DEPTH, checkSightLines, visuell verifikation (dagsljus + kvällsljus), silhuettbandet håller mot nya golvet, fem flaggor redovisade, git diff visar `ui/foodtruck/` + `figureRig.ts` orörda, typecheck + svit + CI, registerpost. Statusen ändras till Executed när DoD 1–13 uppfyllts. **Utförande-summering (2026-08-29):** `BusinessClass` utökad med `ölkrogen` i `types.ts` + `business/businessClass.ts` (capacity 20, hasSeats+hasMiseEnPlace true, hasOvernight false); `strings.sv.ts` player-visible namn "Ölkrogen"; `testHarness/urlParams.ts` accepterar `#playtest=1&business=ölkrogen`; `handoff/brewpubRoom.ts` (900 rader) + brief inkopierade; scen-kopia `frontend/src/strategic/scene/brewpubRoom.ts` byte-identisk med handoff (`cmp` bekräftat, ingen avvikelse); `scene/BrewpubScene.tsx` gate:as på businessClass, `createBrewpubRoom` i useEffect, `updateBrewpubRoom(room, 0)` per frame; monterad i `StrategicScene.tsx` bredvid PlayerBusiness. **Tester:** 39 nya (14 i `brewpubRoom.test.ts` + 25 i `brewpubContrast.test.ts`) — DoD 5 gångväg alla 20 platser, DoD 6 measure vs MIN, DoD 7 alla 8 barstolar ser alla jästankar, DoD 9 palett-mot-golv-band. Full svit 1003 → **1042/1042**. Typecheck + build gröna. **§7-fynd (dokumenterade i test som kända avvikelser):** (1) `paying #e8c99e` vs `floorDining #a49b8a` = 1.74:1, 0.06 under MIN — borderline; (2) `servitör #6b6260` vs `floorBrew #7d776c` = 1.33:1, 0.47 under MIN — matchar exakt §7:s förutsägelse om att figurer inte läses mot bryggeriets mörkare betong. Per §7 stannat: fixet får inte vara palett-tuning (bryter ORDER 123-kalibrering), golv-ändring (bryter §2 kopia-integritet), eller större/lystare figurer (§7 explicit). Följdorder behövs för hue-omval av servitör, zon-specifik uniform eller Design-omkalibrering av brewpub-golv. **DoD 8 playwright uppskjuten** — inte meningsfullt att verifiera visuellt tills §7-fyndet är åtgärdat/accepterat. **Fem Design-flaggor per §5:** businessClass (ÅTGÄRDAD), counterOrder/standing/brewPhase/kitchenStations (INTE ÅTGÄRDADE — geometri finns, tillståndsmaskiner saknas). Fullständig rapport i orderfilens §8. |
| 124 | Ingen är hemma — grindolikhet mellan ankomster (`period`) och personal-handlingar (`phase`); `On break` med väntande gäster | **Executed 2026-08-30 (ORDER 147)** — `seated=0/16`-fyndet var signal-artefakt (transient `state='seated'`); `On break`-fyndet står kvar som eget spår | **Own document** | `documentation/architecture/ORDER_124_INGEN_AR_HEMMA.md`. **§1 lägesbild:** dev-servern 2026-08-29, dag 1 lunch — första observation `waiting=2`, alla tre roller `On break`; andra observation samma dag service 9:18 in `waiting=4`, `seated=0/16`, alla tre roller `On break` (värre — kön växer och personalen står stilla). Grundorsak: `arrivals.ts` gate:ar på `period` (lunch=0,6 / middag=1,0), `deriveActions.ts` rad 131 gate:ar på `phase` — `prep` ger `On break`. Gäster spawnar under hela passet inkl. `prep`, personalen står still tills `simTime > prepEndsAt`. Koden gör vad den säger men spelaren ser "ingen är hemma"-läsning. **§2 vad som ska utredas innan bygge:** två möjliga grundorsaker med olika åtgärd — (a) handlingarna för trubbiga → fix i `deriveStaffAction`, (b) `prepEndsAt` sätts för sent → fix i reducer:s `openService`. Ordern fastställer VILKEN med tal (var `prepEndsAt` sätts, vilket värde, när första ankomstfönstret öppnar). **§3:** endast den grundorsak §2 fastställer — inte båda. **§4 vad som INTE får göras:** inga trösklar kalibreras (ankomstmultiplikatorer, ansiktsband, capacity). **§5 DoD:** grundorsak fastställd med tal, endast den åtgärdad, test som hävdar "gäster väntar OCH minst en i personalen har handling ≠ On break", regressionstest att personalen står stilla när ingen väntar och passet inte börjat (fixen får inte göra rasten omöjlig), grep-check att inga tröskelvärden ändrats, typecheck + svit + CI gröna, registerpost. **§6 avgränsningar:** presentationslagret rörs bara om grundorsaken ligger där; fynd 1 (`seatedIds` osynk med `guests`) är egen order och rörs inte. Gren `order-124` från main (`2a2405f`) för placering. Ingen kod rörd. Statusen ändras till Executed när DoD 1–7 uppfyllts på följdgren. **ORDER 144 (2026-08-30) prövade Designs hypotes att twoLayouts orsakade seated=0** genom att montera kvarterskrogen (enda-sanning-layouten) och mäta before/after på samma 20-min lunchservice: **before max seated 5/16 (93 samples med seated>0), after max seated 5/16 (92 samples). Praktiskt identiskt.** twoLayouts-hypotesen faller. **ORDER 145 (2026-08-30) prövade §2:s (a) och (b) — båda faller. ORDER 145 avslöjade dessutom att ORDER 144:s mätsignal `guests.filter(g.state === 'seated').length` var fel: `state='seated'` är transient (4 sim-sek per service.ts:243). Verklig max upptagen = `state.seatedIds.length` = 16/16.** Rekommendation för nästa dev-observation: logga båda signalerna (`seatedIds.length` OCH tillstånd-distribution) innan grundorsak fastställs — `seated=0` kan betyda "alla gäster i ordering/dining/paying" (normal drift) snarare än verkligt fel. **ORDER 146 (2026-08-30) körde om observationen med rätt signal + verifierade DevPanel-strängen live**: vid t=550,8s visar sim `seatedIds=16, waitingIds=4, stateSeated=0` (alla 16 platser fyllda, gäster i dining/ordering). DevPanel-strängen visar `seated=16/16` — inte 0/16. ORDER 124-observationens `seated=0/16` kan inte reproduceras från nuvarande kod; sannolik rot är signal-artefakt (räknade `state='seated'` som är transient) snarare än verkligt fel. `On break`-fyndet för personalen står kvar som separat spår (arrivals-under-prep gate-diskrepansen), obeprövat. **ORDER 147 (2026-08-30) stänger ORDER 124 som Executed** — grundorsak till `seated=0/16` fastställd som signal-artefakt (inte sim-fel); DevPanel utökad med breakdown `(S:0 O:4 D:11 P:1)` per Vision Owners beslut så framtida observatörer ser underskiktet och kan avfärda samma misstag direkt. **§6-Fynd 1 (`seatedIds` osynk med `guests`) inte reproducerad** (ORDER 145+146 mätningar hittade noll ticks där invariantet bröts). **§On break-fyndet** — arrivals gate:as på `period`, `deriveStaffAction` gate:as på `phase='prep'` — står kvar som separat spår för framtida order om det återobserveras med rätt signal. |
| 123 | Silhuetten läses — crown-täckning + palett-ljusning per SD-004 §3.3-preciseringen (ORDER 122) | **Executed 2026-08-29** (placerad + utförd samma dag på separata grenar) | **Own document** | `documentation/architecture/ORDER_123_SILHUETTEN_LASES.md`. Lyder under SD-004 §3.3 med preciseringen 2026-08-29 (ORDER 122, merge-commit `ff012f8`). **§2 vad som ska byggas:** (2.1) `crown.phi_length` utökas från 1,15 rad till hela övre hemisfären så garment-/uniformsfärg täcker hela synliga hjässan från strategisk vinkel; (2.2) personalens `ROLE_COLOUR`-palett ljusas mot golvets `#a89577` med bevarad roll-distinktion; (2.3) gästernas `GUEST_COLOUR` för `sleeping`/`declined` ses över (låg kontrast mot golvet). **§3:** riggens mått, poser, ledvinklar orörda — kontrastproblemet löses av färg, inte storlek. **§4 DoD (9 punkter):** crown-täckning grep-verifierad, uppmätt kontrast mot golvet per uniformsfärg + gäst-tillståndsfärg, roll-distinktion (avstånd mellan fyra uniformer) mätt, visuell verifikation via playwright i strategisk kamera (dagsljus + kvällsljus, samma mönster som ORDER 120 DoD 7 / ORDER 121 DoD 8), `FIGURE`-mått oförändrade (`git diff` tomt), `pose*`-funktioner oförändrade, kontrastbandet skrivet som konstant i koden (inte som siffra i rapporten). **§5:** kontrastmåttet (luminans / ΔE / annat) avgörs av byggaren, men ska vara testbart. **§6 avgränsningar:** rekvisita + huvudbonader (bara SVG i dag), rummets ljussättning, `ui/foodtruck/`, sim-lagret — alla orörda. **§7:** kan uniformsfärgerna inte ljusas tillräckligt utan att rollerna kollapsar → fynd, rapportera och stanna (bygg inte större figurer / kontur). **Utförandeplan:** crown-geometrin patchas lokalt i `frontend/src/strategic/scene/figureRig.ts` (scen-kopian avviker medvetet från handoff-koden efter denna order — se ORDER 122-precisering). Ingen ny Design-leverans. `handoff/figureRig.ts` bevaras som historik. Gren `order-123` skapad från main (`ff012f8`) för placering; utförande separat följdgren `order-123-exec` med kod-committen + rapport-committen. **Utförande-summering (2026-08-29):** crown-`thetaLength` utökad 1.15 → `Math.PI / 2` med ny cache-nyckel `head:crown:hemi`; `ROLE_COLOUR` bytt till fyra distinkta hue-familjer (värd `#2f4a68` navy, servitör `#6b6260` warm-neutral, kock `#7a3e3a` burgundy, lärling `#d8d3ce` light warm-grey) — kontrast 1.95–3.14:1 mot golv, parvis ΔE 26.10–59.14 (över 12-tröskeln); `GUEST_COLOUR` för SEATED_STATES ljusade till kontrast 1.84–1.98:1. `silhouetteContrast.ts` exporterar bandkonstanterna (`MIN_FLOOR_CONTRAST_RATIO=1.8`, `MAX_FLOOR_CONTRAST_RATIO=3.6`, `MIN_ROLE_DISTINCTION_DELTA_E=12`) + WCAG-luminans/kontrast + CIE 76 ΔE-formler. Två testfiler (`silhouetteContrast.test.ts`, `paletteContrast.test.ts`) med 31 nya tester; full svit 970 → **1001/1001**. Playwright-script `order123-silhouette-lighting.mjs` verifierar visuellt i dagsljus + kvällsljus (bounding-box 573×235 respektive 570×216 CSS-px); skärmdumpar `frontend/reports/order123/`. Full rapport i orderfilens §8. `handoff/figureRig.ts` orörd; scen-kopian avviker medvetet från handoff efter denna order — handoff-noten uppdaterad. **Rättelse per ORDER 128 (2026-08-29):** paletten kalibrerades mot `FLOOR_COLOUR = '#a89577'` — den färgen är i själva verket skyltblocket i `Restaurant.tsx:108`, inte spelarens interiörsgolv (`INTERIOR_FLOOR_COLOUR = '#a08462'` i `PlayerBusiness.tsx:75`). Kalibreringen skedde alltså mot fel yta. Rättat av ORDER 127 som ersatte referensen med zonregistret `FLOOR_ZONES_BY_BUSINESS` per faktiska golv; gamla `FLOOR_COLOUR` markerad `@deprecated`. |
| 122 | SD-004 §3.3 preciseras — hjässan = hela övre hemisfären, palett-ljushet är kontraktsdel | Executed 2026-08-29 | Verbal order (Anders, 2026-08-29) | Documentation-only order. Två preciseringar inskrivna i `documentation/foundation/SUPERSEDING_DIRECTIVE_004.md` §3.3 under befintlig text (som står kvar): (1) "hjässan bär garment-/uniformsfärgen" avser HELA huvudets övre hemisfär, inte en kalott — ORDER 121:s crown-implementering (1,15 rad, cirka en tredjedel av synligt huvud från strategisk vinkel) uppfyllde ordalydelsen men inte avsikten, hudtonen dominerar när kameran lutar från lodrätt; (2) palettens ljushet är kontraktsdel — personalens uniformsfärger `#2a2f3a`–`#4a4744` mot golvets `#a89577` (`Restaurant.tsx` rad 108) ger ren silhuett men gör kroppen till en skugga oavsett hur hjässan löses. Preciseringen svarar på dev-observationen 2026-08-29 (kropp svart, hjässa ljus) som utredningen samma dag identifierade som partiell implementation av §3.3. Följdorder krävs för att uppdatera `frontend/src/strategic/scene/figureRig.ts` — antingen `handoff/`-synk med Design efter ny leverans, eller lokal patch av `crown`-geometrin i scen-kopian (utökar `phi_length` från 1,15 rad till hela övre hemisfären, ~π/2 rad) plus omkalibrering av `ROLE_COLOUR`-paletten mot §3.3-preciseringens ljushet. Egen gren `order-122` från main. Ingen produktionskod rörd. |
| 121 | Kroppar i scenen — three.js-rigg ersätter cylinderpuckar i `InteriorGuests.tsx`/`InteriorStaff.tsx` per SD-004 §3 | **Executed 2026-08-29** (Reserved samma dag; blockering upphävd när källan landade) | **Own document** | `documentation/architecture/ORDER_121_KROPPAR_I_SCENEN.md`. Lyder under SD-004 §3 (Kroppar i rummet). Utförd på gren `order-121-exec` från main efter att `handoff/figureRig.ts` (781 rader, Claude Designs leverans) och `handoff/ORDER-gaster-och-personal.md` (Vision Owners brief) landat. **§2 kopia:** `figureRig.ts` kopierad byte-för-byte till `frontend/src/strategic/scene/`; en avvikelse dokumenterad i koden (`measureFigure`-traversens `o.isMesh`/`o.geometry` typades via `instanceof THREE.Mesh` + `as THREE.BufferGeometry`-cast, samma semantik som Design avsåg). **§3 montering:** rig-instans per gäst/personal skapas imperativt i `useFrame` (`createFigureRig` → `group.add(rig.root)`), poser skrivs via `applyPose` varje bildruta, inga pose-parametrar i React-state. Fötterna vid golvplanet (y=0); gruppen sitter direkt vid marken (`GUEST_Y`/`STAFF_Y`/`STAFF_RADIUS_M`/`STAFF_HEIGHT_M` utgår som puck-konstanter). **§4 poser i bruk:** `poseWalk` (rörelse; avståndsdriven gångfas via `STRIDE_LENGTH_M = 0.75`), `poseIdle`, `poseSeated`, plus `blendPose(poseIdle → poseSeated)` över 0,5 s vid `waiting → seated` respektive omvänt vid `paying → leaving`. **Fyra flaggor från figureRig.ts § slutet — svar på §4, inte hinder:** (1) `poseWork` FLAGGAT (TeamMember har bara `role`, ingen task-state), (2) `poseCarry` FLAGGAT (ingen carry-state, hand-anchor finns), (3) golvkontakt vid studs = designval (poserna dippar upp till −0,029 m under noll i gångstudsen; osynligt i strategisk kamera; ben-IK ligger utanför ordern), (4) `poseGreet` HÄRLEDBAR FÖR PERSONAL (via `StaffMember.targetGuestId` + `teamStaffBridge`) men FLAGGAT FÖR GÄST. **§5 mått:** guest 1,70 m med 0,46 m axelbredd, staff 1,70 m med 0,40 m axelbredd — samma höjd, skillnaden bärs av bredd + färg. **§6 pip-ankaret** flyttat från puckens topp till `rig.joints.headAnchor` i båda Interior-filerna; positionen sätts varje tick via `group.updateWorldMatrix` + `worldToLocal(anchorWorld)` så pip:en följer sit-stand och lean automatiskt. Id-bryggan (`teamPipCarriersFromStaffPipCarriers`) från ORDER 090 oförändrad; DoD 7:s "två servitörer, två pip, rätt figur" redan verifierat i `teamStaffBridge.test.ts:62`. **§7 utgått** — det påstådda beroendet på `ui/foodtruck/` finns inte; imports går till `ui/RoomCardPanel/guestPatterns`. DoD 11 upprätthållen: `git diff main..HEAD -- frontend/src/strategic/ui/foodtruck/` = tomt. **§8 DoD 8 visuell verifikation:** `frontend/scripts/order121-body-visibility.mjs` (playwright) — **diff-baserad mätning** som isolerar figurbidraget: läs pixlar utan gäster (baseline), injicera 6 seated gäster, läs igen (sample), diffa pixel-vis. Ändring ≥ 25 per kanal räknas som "figur-pixel". Resultat i vänster tredjedel × mellersta y-band: **6 979 pixlar ändras** mellan baseline och sample, figur-bounding-box **576 × 235 CSS-px** → riggen har bredd OCH höjd i vyn, inte bara i scengrafen (ORDER 112 §4:s `SKEPNAD EJ BYGGD`-anti-mönster undvikt). Skärmdumpar `frontend/reports/order121/scene-baseline.png` + `scene-with-bodies.png`. **Metodfix:** en initial version räknade avvikelse från medelbakgrund i två horisontella band; den mätningen inkluderade "Din verksamhet"-onboarding-modalen och byggnadens silhuett i "avvikande"-räkningen. Diff-mätningen från baseline isolerar figurbidraget robust — ändringar från 0 → 6 gäster är per definition figurernas kontribution. **Tester:** 16 nya i `frontend/src/strategic/scene/__tests__/figureRig.test.ts` — DoD 4 (7 pose-samplingar), DoD 5 (4 dispose/leak-tester), DoD 6 (4 mått + headAnchor följer sittande). Full svit 954 → **970/970** grön. Typecheck grön, build grön (2,06 s, +7 kB för rig-koden). **Utfasningar identifierade som följdorder:** SVG-food-truck (SD-004 §5, ORDER 119-beslutet), ansiktsvokabulären från `guestFaces.ts` till kort (SD-004 §3.4), värdshusets 3D-skepnad (ersätter nuvarande `UnbuiltShapeholder`-placeholder), R2-paviljongernas presentationsform. `handoff/`-katalogen behållen som spårbar källa (analog till `documentation/prototypes/`); ingen produktionskod importerar därifrån. **Typ-shim (tillägg per ORDER 126 §2):** `frontend/src/strategic/scene/three-augmentations.d.ts` skapades i rättelse-commiten `6b9a3ac` som del av samma ORDER 121-arbete. Augmenterar `THREE.Object3D` med valfria `isMesh?: boolean` och `geometry?: BufferGeometry` — `@types/three` exponerar inte de runtime-flaggorna på Object3D-basklassen, så handoff-kodens `measureFigure`-traverse skulle annars misslyckas typecheck. Shimen är **projektbred** genom TypeScripts module-augmentation-mönster (påverkar alla `Object3D`-användningar i kodbasen, inte bara riggen); den ligger i egen fil så handoff-koden hålls oförändrad och synk framåt blir en byte-för-byte-kopiering utan patch. Fälten är valfria — påverkar inte hur `Object3D` läses i övriga kod. |
| 120 | Rykte-trend på stängda pillen (§5.2 uppföljning) — glyfen syns utan att spelaren måste klicka öppen panelen | Executed 2026-08-29 | **Own document** | `documentation/architecture/ORDER_120_RYKTE_TREND_PA_STANGD_PILL.md`. Bygger på ORDER 117 §5.2 (rykte-trend på Reading-raden i öppnad panel). **§2 kod:** `PlayerPanel.tsx` Cash-pillens `<button>` utökas med ett villkorligt `<span data-reputation-trend={trend}>` inuti pillen, direkt efter Cash-value-spannen och före ▾-toggleln. `TREND_GLYPHS`/`TREND_COLOURS` och `reputationTrend`-funktionen orörda (§3 avgränsning). **§4 DoD-verifikation:** (1) grep visar `data-reputation-trend` i pillens `<button>`-block före `{open && (…)}`; (2) `<Reading label="Rykte" ... trend={reputationTrend(sim)} />` i öppnad panel oförändrad — regressionstest räknar ≥ 2 noder med attributet efter panelöppning; (3) `valueQuota.ts` orörd (`git diff main..HEAD` tomt); (4) `TREND_GLYPHS`/`TREND_COLOURS` samma; (5) `aria-label`/`title` på glyfen (`"rykte trend uppåt"`/`"...nedåt"`); (6) DOM-struktur: glyfen är direkt barn till `<button>` (inte nästad i Cash-spannen), kommer efter Cash-value och före ▾; (7) **visuell verifikation via `frontend/scripts/order120-pill-trend-visibility.mjs`** — playwright tvingar `effectiveValueQuota = 1.5` respektive `0.5` via `__nxSimState`-mutation + `SET_CASH`-no-op dispatch (SET_CASH:s spread `{ ...state, cash: ... }` plockar upp muterad effectiveValueQuota och triggrar re-render — ingen ny SimAction eller URL-parameter). Mätning: glyfen 8×14 CSS-px, x=1704 > cash.right=1696 (efter Cash, inte bakom), opacity 1.0, visibility visible; up=▲ i #7a9a5c, down=▼ i #c47a5a. Skärmdumpar `reports/order120/pill-trend-up.png` + `pill-trend-down.png`. **§4 tillägg (Vision Owner 2026-08-29):** DoD 7 sattes efter drafting eftersom ett attribut på en nod med noll bredd eller bakom Cash-siffran passerar grep-DoD 1 men löser inte problemet. **Tester:** 10 nya i `PlayerPanel.test.tsx` (DoD 1: 3, DoD 2: 1, DoD 5: 2, DoD 6: 4). Full svit 944/944 → 954/954. Typecheck grön, build grön. Egen gren `order-120` från main per CLAUDE.md §Grenar-är-kortlivade. Ingen produktionskod utanför `PlayerPanel.tsx` rörd. |
| 119 | SD-004 §6.1 avgjord — food trucken blir 3D, inget sidovy-undantag | Executed 2026-08-29 | Verbal order (Anders, 2026-08-29) | Documentation-only order. Vision Owner-beslut inskrivet i `documentation/foundation/SUPERSEDING_DIRECTIVE_004.md` §6.1: "Food trucken blir 3D som övriga verksamhetsklasser. Inget sidovy-undantag." Frågeställningen står kvar ovanför beslutet så skälen går att läsa nästa gång presentationsfrågan öppnas. Ingen kod rörd — utfasning av nuvarande `FoodtruckScene.tsx`/`rig.ts`/`Figure.tsx` (SVG-sidovy från ORDER 113–117) hör till separat följdorder när 3D-food-trucken byggs. Egen gren `order-119` från main per CLAUDE.md §Grenar-är-kortlivade. |
| 118 | Registerrevision — audit av ORDER_REGISTRY.md, `documentation/foundation/`-instrument, sekvenshål | Executed 2026-08-29 | **Own document** | `documentation/architecture/ORDER_118_REGISTERREVISION.md`. Rapport: `documentation/architecture/REGISTER_AUDIT_2026-08.md`. Verifierad mot main HEAD `21b4b6d`. Fynd: (A1) `order-113` samlar 6 ordrars arbete utan merge; (A2) 21 nummer citerade utan rader (nu tillagda som `Gap — ej utredd` — se 017, 054–059, 062–075 ovan); (A3) SD-001 fortfarande oskrivet efter ORDER 035 §2.4-beslutet; (A4) SD-003 rev. 2 aldrig författat, nu ersatt av SD-004; (B1) registret osorterat efter rad 045 (som ORDER 103 §3(c) redan noterat); (B2) EDD 002 och DESIGN_DECISIONS 002 är prospektiva, inte defekta; (C1) sha-citat i 043/044/045 pekar på pre-squash-shas — semantiskt korrekt via PR-mergen. Rad 112 attesterad separat efter Vision Owner-genomgång 2026-08-29. §1-cited defekter (095, 104, 106, 088) alla redan reparerade i registret före ordern skrevs — ordertexten §1 delvis inaktuell som diagnos men strukturellt problem verkligt. Ingen produktionskod rörd. Utförd på `order-113`-grenen istället för ren `order-118`-gren — själv-referentiell avvikelse dokumenterad i rapporten §7.1. |
| SD-001 | Superseding Directive 001 — declares Digital Twin Phase, installs authenticity, defines precedence order | Governance instrument gap — referenced as issued but never written to the repository | Reference only | Not an ORDER. Attested as issued 2026-07-22 by `ADR_001_DIGITAL_TWIN_PHASE.md` §1.4; cited in ADR 001 (11 refs), `DISTRICT_1_REFERENCE_REQUEST.md` line 221, and agent memory `reference_confidence_rule.md` line 29. Provenance evidence: `documentation/foundation/SD_001_RECONSTRUCTION_RECORD.md` (ORDER 035 §2.1). Vision Owner decision (ORDER 035 §2.4, 2026-07-29): Alternative A — a real `SUPERSEDING_DIRECTIVE_001.md` shall be authored from the reconstruction record under the mandatory reconstruction markings, in a separate commit after review. Fortfarande oskrivet per ORDER 118 audit §5 fynd A3 (2026-08-29). |
| SD-003 rev. 2 | Dockskåpsdirektivet — restaurangen som sidovy i genomskärning, ansikten och lemmar på aktörerna | Governance instrument gap — refererat som gällande men aldrig committat som instrument; ersatt av SD-004 innan det skrevs | Reference only | Not an ORDER. Attesterad som gällande av `documentation/blueprints/SD003_MATGRIND_RAPPORT_ORDER_096.md` §1 ("SD-003 beslutade dockskåpet"), av ORDER 091:s registerpost §1.6 ("SD-003:s dockskåp pausas mot R4"), och av ORDER 111/113/114-radernas "lyder under SD-003 rev. 2". Instrumentet finns inte i `documentation/foundation/` — `SUPERSEDING_DIRECTIVE_003.md` v1.0 där rör sessionsbaserad granskning och är ett separat, oberört instrument. Samma defektmönster som SD-001. Ersatt 2026-08-22 av `SUPERSEDING_DIRECTIVE_004.md` (committad 2026-08-28); substansen behållen som historiskt underlag i mätgrindsrapporten och ORDER 091-radens §1.6. |
| SD-004 | Superseding Directive 004 — Kroppar i rummet: 3D-scen med ledade figurer utan ansikten; dockskåpet upphör som presentationsform | In force from 2026-08-22 (Vision Owner Anders); committad 2026-08-28 | **Own document** | `documentation/foundation/SUPERSEDING_DIRECTIVE_004.md`. Ersätter det icke-committade dockskåpsdirektivet (raden ovan, "SD-003 rev. 2"). Berör **inte** `SUPERSEDING_DIRECTIVE_003.md` v1.0 (sessionsregeln, modifierar SD-002 §4), som gäller oförändrad. Header, §2 och §5 rättade av Vision Owner före commit så instrumentet inte påstår att SD-003 v1.0 upphävs. Vad som utgår: `DollhouseFrame`, `FoodTruckShape`, `dollhouse=1`-växeln, pixelgolvet (140/96 px, tolv kuvert). Vad som överlever: hela sim-lagret (kön, serving-fasen, uteplatsen, värdekvoten, mise en place-konsumtionen, gästarketyperna, verksamhetsklasserna) plus `patternTransform`, `InteriorGuests`, `InteriorStaff`. Öppna beslut (§6): food truckens vy i 3D vs sidovy-undantag, kartans roll efter 3D-återkomst, paviljongernas presentationsform. |

## 4. Findings

**Observation 1 — Instrumental deficit.** Only two ORDER numbers carry their own instrument: ORDER 034 (this order's parent) and ORDER 100 (currently as a binary). Every other ORDER exists only as its downstream report, or as a bare reference. This matches the pattern ORDER 034 §0 was written to correct.

**Observation 2 — Draft collision.** Two competing drafts (ORDER 020 draft, ORDER 030 draft) were withdrawn before this order existed. This is the specific pathology §0 exists to prevent recurrence of.

**Observation 3 (resolved 2026-07-29 by commits `9b16962` + `2f5f523`).** `SUPERSEDING_DIRECTIVE_002.md` §1 header and §6 previously named the void `ORDER_030_DOCUMENTATION_ALIGNMENT.md` draft. Both references now repointed to `ORDER_034_DOCUMENTATION_ALIGNMENT.md`.

**Observation 4 — Number gaps.** 001, 007, 008, 009, 010, 012, 013, 016, 032 do not appear in the repository. Whether they were verbally issued and lost, deliberately skipped, or never issued is not determinable from the repo. Numbers 003A, 014, 015, 017a appear once each without descriptive context.

**Observation 5 — ORDER 001 is memory-only.** Cited in `nexus_framework_governance.md` (agent memory) as the parent of a "Director's Addendum". Not evidenced in the repository. Under `ADR_002_SYNTHESIS_POLICY.md` §7.5 this is a documentation defect, not a memory feature.

**Observation 6 — Game language switched from Swedish to English (2026-08-09).** Vision Owner decision during ORDER 049 §3 execution: the game ships in English, not Swedish. Rationale — international target audience; the source corpus (gusto.science TRIAD articles, `scenario_chef` and its sister columns) is already English; translation adds distortion and cost without reaching more of the intended players. This is a **conscious revocation**, not drift.

- **Rule affected:** `CLAUDE.md` rule 7 ("Svenska i spelet, engelska i koden") is revoked. Replacement: game text in English, code in English, place names retained in Swedish (Grythyttan, Torget, Kyrkbacken — an international culinary school located in Sweden still carries its Swedish placenames).
- **Content of type "translate":** existing Swedish game text (`frontend/src/content/strings.sv.ts` and callers) is translated in its own dedicated pass, not conflated with feature work.
- **Content of type "rewrite":** the observer's voice in the event stream is **rewritten in English**, not translated — the tone is what carries it, and a literal translation kills it.
- **Content of type "verbatim":** pre-written scenarios in `articles.scenario_chef` (and sister columns) are used as-is; no translation, no adaptation.
- **Orders touched by this decision:**
  - ORDER 042 §4 constraint "text via `strings.sv.ts` per CLAUDE.md rule 7" — superseded; text remains in `strings.sv.ts` filename for now (rename is a separate pass), values become English strings.
  - ORDER 049 §3.1 — knowledge-generation prompt rewritten for English output; `scenario_chef` used verbatim (§3, phronesis short-circuit) unchanged in scope but now aligned with target language.
- **Not affected:** code identifiers, commit prefixes, documentation in `documentation/` (English already), file paths.

## 5. Maintenance

- New ORDER: reserve the next number in this registry with title, date and status *before* the order text is written. Update to "Own document" when the file lands.
- Superseded/void: never delete an entry. Update status; retain the row so the number cannot be reused silently.
- Discovered ORDER: if repository archaeology surfaces an ORDER referenced only in agent memory or external channels, add it as "Reference only" with a note on where it was found.

---

**End of ORDER REGISTRY v1.0.**
